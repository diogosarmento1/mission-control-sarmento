{"html":"<!DOCTYPE html>\n<html lang=\"pt-PT\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\n<title>Mission Control · Ourivesaria Sarmento</title>\n<script src=\"https://cdn.jsdelivr.net/npm/chart.js@4.5.0/dist/chart.umd.js\" integrity=\"sha384-iU8HYtnGQ8Cy4zl7gbNMOhsDTTKX02BTXptVP/vqAWIaTfM7isw76iyZCsjL2eVi\" crossorigin=\"anonymous\"></script>\n<style>\n:root{\n  color-scheme: light;\n  --bg:#fff; --panel:#fff; --soft:#faf9f7; --line:#e6e3dd;\n  --ink:#141416; --ink-2:#63605a; --ink-3:#9d9890;\n  --gold:#9c7c3c; --sand:#ddd7ca;\n  --pos:#4a7c59; --neg:#a4604f; --warn:#a6791f; --info:#3a5d80;\n  --pos-bg:#e6efe8; --neg-bg:#f6e6e1; --warn-bg:#f8ecd0; --info-bg:#e7eef4; --neu-bg:#f1efe9;\n  --gold-bg:#f3ead9;\n  /* tons mais fortes, só para preencher o quadrado inteiro dos cartões kanban — pedido pelo\n     Diogo 2026-07-29, \"não se percebe quase\" com os tons claros de cima */\n  --pos-bg2:#b7cbbd; --neg-bg2:#dbbfb9; --warn-bg2:#dbc9a5; --info-bg2:#b0bfcc;\n  --gold-bg2:#d7cbb1; --neu-bg2:#d8d6d3;\n  --sans:-apple-system,BlinkMacSystemFont,\"Segoe UI\",Helvetica,Arial,sans-serif;\n  --display:\"Futura\",\"Avenir Next\",\"Century Gothic\",\"Trebuchet MS\",var(--sans);\n}\n*{box-sizing:border-box}\nhtml,body{margin:0;padding:0}\nbody{background:var(--bg);color:var(--ink);font-size:14px;line-height:1.55;\n  font-family:var(--sans);-webkit-font-smoothing:antialiased}\n.shell{display:flex;min-height:100vh}\n\n/* ---------- sidebar ---------- */\n.side{width:202px;flex:0 0 202px;background:var(--soft);border-right:1px solid var(--line);\n  padding:26px 0 26px;position:sticky;top:0;height:100vh;display:flex;flex-direction:column;\n  overflow-y:auto}\n.logo{padding:0 18px 22px;text-align:center}\n.logo img{width:124px;max-width:100%;display:inline-block}\n.logo .wm{font-family:var(--display);font-size:21px;letter-spacing:4px;font-weight:600}\n.logo .wm small{display:block;font-size:9.5px;letter-spacing:3px;color:var(--ink-2);\n  margin-top:4px;font-weight:400}\n.side nav{padding:0 14px;flex:1}\n.side a{display:block;padding:10px 14px;color:var(--ink-2);text-decoration:none;font-size:13.5px;\n  cursor:pointer;border-left:2px solid transparent;letter-spacing:.2px}\n.side a:hover{color:var(--ink)}\n.side a.on{color:var(--ink);font-weight:600;border-left-color:var(--gold);background:#fff}\n/* títulos de secção — só organização visual, nunca escondem páginas */\n.side .nav-sec{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:1.3px;\n  color:var(--ink-3);padding:18px 14px 5px;user-select:none}\n.side .nav-sec:first-child{padding-top:4px}\n.side .foot{padding:16px 26px 0;margin:10px 14px 0;border-top:1px solid var(--line);\n  font-size:11px;color:var(--ink-3);line-height:1.6}\n\n/* ---------- main ---------- */\n.main{flex:1;min-width:0;padding:30px 32px 70px;max-width:1120px}\n.top{margin-bottom:26px}\n.top h2{margin:0;font-family:var(--display);font-size:20px;font-weight:600;\n  letter-spacing:2.4px;text-transform:uppercase}\n.top .desc{font-size:13px;color:var(--ink-3);margin-top:7px;max-width:660px}\n\n/* filtro */\n.filter{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-bottom:22px;\n  padding-bottom:18px;border-bottom:1px solid var(--line)}\n.filter button{font-family:var(--sans);font-size:12.5px;padding:6px 13px;border-radius:20px;\n  border:1px solid var(--line);background:#fff;color:var(--ink-2);cursor:pointer}\n.filter button:hover{border-color:var(--ink-3);color:var(--ink)}\n.filter button.on{background:var(--ink);border-color:var(--ink);color:#fff;font-weight:600}\n.filter .sep{width:1px;height:20px;background:var(--line);margin:0 4px}\n.filter label{font-size:12px;color:var(--ink-3)}\n.filter input[type=date]{font-family:var(--sans);font-size:12.5px;padding:5px 8px;\n  border:1px solid var(--line);border-radius:6px;color:var(--ink);background:#fff}\n\n/* kpi */\n.kpis{display:grid;grid-template-columns:repeat(auto-fit,minmax(175px,1fr));gap:1px;\n  background:var(--line);border:1px solid var(--line);border-radius:2px;overflow:hidden;\n  margin-bottom:24px}\n.kpi{background:var(--panel);padding:17px 19px 17px 22px;box-shadow:inset 3px 0 0 transparent}\n.kpi.tone-pos{box-shadow:inset 3px 0 0 var(--pos)}\n.kpi.tone-warn{box-shadow:inset 3px 0 0 var(--warn)}\n.kpi.tone-neg{box-shadow:inset 3px 0 0 var(--neg)}\n.kpi.tone-info{box-shadow:inset 3px 0 0 var(--info)}\n.kpi .lab{font-size:11px;color:var(--ink-3);text-transform:uppercase;letter-spacing:1px}\n.kpi .val{font-family:var(--display);font-size:28px;font-weight:600;margin:7px 0 3px;\n  letter-spacing:-.3px;line-height:1.1}\n.kpi .note{font-size:11.5px;color:var(--ink-3)}\n.delta{font-size:12px;font-weight:600}\n.delta.up{color:var(--pos)} .delta.dn{color:var(--neg)} .delta.flat{color:var(--ink-3)}\n\n/* etiquetas de estado (semáforo: verde=bom, âmbar=aviso, vermelho=mau, azul=info, cinza=neutro) */\n.pill{display:inline-block;font-size:11px;font-weight:600;padding:3px 10px;border-radius:20px;\n  white-space:nowrap}\n.pill.pos{background:var(--pos-bg);color:var(--pos)}\n.pill.warn{background:var(--warn-bg);color:var(--warn)}\n.pill.neg{background:var(--neg-bg);color:var(--neg)}\n.pill.info{background:var(--info-bg);color:var(--info)}\n.pill.neu{background:var(--neu-bg);color:var(--ink-2)}\n\n.card{background:var(--panel);border:1px solid var(--line);border-radius:2px;\n  padding:20px 22px;margin-bottom:14px}\n.card h3{font-size:11.5px;margin:0 0 3px;font-weight:700;text-transform:uppercase;\n  letter-spacing:1.2px}\n.card .hint{font-size:12px;color:var(--ink-3);margin:0 0 16px}\n.g2{display:grid;grid-template-columns:repeat(auto-fit,minmax(330px,1fr));gap:14px;margin-bottom:14px}\n.g2 .card{margin-bottom:0}\n.chartbox{position:relative;height:245px}\n\n/* bars */\n.bar-row{margin-bottom:12px}\n.bar-row:last-child{margin-bottom:0}\n.bar-top{display:flex;justify-content:space-between;font-size:13px;margin-bottom:5px;gap:12px}\n.bar-top b{font-weight:600;color:var(--ink-2);font-variant-numeric:tabular-nums;white-space:nowrap}\n.bar-lab{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}\n.track{height:5px;background:#f2efe9;overflow:hidden}\n.fill{height:100%;background:var(--ink)}\n.fill.lt{background:var(--sand)}\n\ntable{width:100%;border-collapse:collapse;font-size:13px}\nth{text-align:left;font-size:10.5px;color:var(--ink-3);font-weight:600;text-transform:uppercase;\n  letter-spacing:.8px;border-bottom:1px solid var(--line);padding:0 10px 9px 0}\ntd{padding:10px 10px 10px 0;border-bottom:1px solid #f4f2ee}\ntr:last-child td{border-bottom:none}\ntd.num,th.num{text-align:right;font-variant-numeric:tabular-nums}\ntd.num:last-child,th.num:last-child{padding-right:0}\ntr.clk{cursor:pointer}\ntr.clk:hover td{background:var(--soft)}\n.rate{font-weight:600}\n.rate.up{color:var(--pos)} .rate.dn{color:var(--neg)}\n\n/* títulos de secção */\n.sec{font-size:11.5px;font-weight:700;text-transform:uppercase;letter-spacing:1.4px;\n  margin:0 0 4px;color:var(--ink)}\n.sec-d{font-size:12px;color:var(--ink-3);margin:0 0 13px}\n\n/* insights */\n.ins{margin-bottom:24px}\n.ins .item{background:var(--soft);border:1px solid var(--line);border-radius:2px;\n  padding:17px 20px;margin-bottom:9px;display:flex;gap:15px}\n.ins .mark{width:2px;align-self:stretch;background:var(--sand);flex:0 0 2px}\n.ins .mark.al{background:var(--neg)} .ins .mark.op{background:var(--pos)}\n.ins h4{margin:0 0 5px;font-size:13.5px;font-weight:700}\n.ins p{margin:0;font-size:13px;color:var(--ink-2)}\n.ins p b{color:var(--ink)}\n\n.pending{background:var(--soft);border:1px dashed #dcd7cd;border-radius:2px;\n  padding:24px 24px;color:var(--ink-2);font-size:13px;line-height:1.7}\n.pending h4{margin:0 0 9px;font-size:11.5px;color:var(--ink);font-weight:700;\n  text-transform:uppercase;letter-spacing:1.2px}\n.pending p{margin:0}\n.pending ul{margin:10px 0 0;padding-left:18px}\n.pending li{margin-bottom:7px}\n\n/* formulário de leads */\n.form{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:13px}\n.field label{display:block;font-size:11px;color:var(--ink-3);text-transform:uppercase;\n  letter-spacing:.8px;margin-bottom:5px}\n.field input,.field select,.field textarea{width:100%;font-family:var(--sans);font-size:13.5px;\n  padding:9px 11px;border:1px solid var(--line);border-radius:2px;background:#fff;color:var(--ink)}\n.field textarea{resize:vertical;min-height:42px}\n.field.wide{grid-column:1/-1}\n.btn{font-family:var(--sans);font-size:13px;font-weight:600;padding:10px 20px;border-radius:2px;\n  border:1px solid var(--ink);background:var(--ink);color:#fff;cursor:pointer}\n.btn:hover{background:#000}\n.btn.ghost{background:#fff;color:var(--ink-2);border-color:var(--line);font-weight:500}\n.btn.ghost:hover{border-color:var(--ink-3);color:var(--ink)}\n.btn.sm{padding:5px 11px;font-size:11.5px;font-weight:500}\n.actions{display:flex;gap:9px;margin-top:16px;flex-wrap:wrap;align-items:center}\n.msg{font-size:12.5px;color:var(--pos);font-weight:600}\n.chip{display:inline-block;font-size:11px;padding:3px 9px;border:1px solid var(--line);\n  border-radius:20px;cursor:pointer;color:var(--ink-2);background:#fff}\n.chip.aberta{border-color:var(--gold);color:var(--gold);font-weight:600}\n.chip.contactada{border-color:var(--ink-3);color:var(--ink-2)}\n.chip.fechada{border-color:var(--pos);color:var(--pos);font-weight:600}\n\n/* pipeline */\n.pipe{display:grid;grid-template-columns:repeat(auto-fit,minmax(120px,1fr));gap:1px;\n  background:var(--line);border:1px solid var(--line);margin-bottom:20px}\n.pipe .st{background:#fff;padding:13px 15px;cursor:pointer}\n.pipe .st:hover{background:var(--soft)}\n.pipe .st.on{background:var(--ink)}\n.pipe .st.on .n,.pipe .st.on .t{color:#fff}\n.pipe .st .n{font-family:var(--display);font-size:23px;font-weight:600;line-height:1.1}\n.pipe .st .t{font-size:11px;color:var(--ink-3);text-transform:uppercase;letter-spacing:.8px;\n  margin-top:3px}\n\n/* barra de adicionar */\n.addbar{display:flex;justify-content:space-between;align-items:center;gap:12px;\n  margin-bottom:14px;flex-wrap:wrap}\n.addbar h3{font-size:11.5px;margin:0;font-weight:700;text-transform:uppercase;letter-spacing:1.2px}\n.btn.plus{padding:8px 16px;font-size:13px}\n.btn.plus span{font-size:16px;line-height:1;margin-right:5px;vertical-align:-1px}\n.panel-add{border:1px solid var(--line);background:var(--soft);padding:19px 21px;margin-bottom:14px}\n\n/* etiquetas de origem */\n.tagO{display:inline-block;font-size:10.5px;padding:2px 8px;border-radius:20px;\n  border:1px solid var(--line);color:var(--ink-2);background:#fff;white-space:nowrap}\n.tagO.Site{border-color:#b9c9d8;color:#3a5d80}\n.tagO.WhatsApp{border-color:#b2d3bd;color:var(--pos)}\n.tagO.Instagram{border-color:#dcc3cf;color:#8d5470}\n.tagO.Loja{border-color:var(--sand);color:var(--gold)}\n.tagO.Fornecedor{border-color:var(--sand);color:var(--gold)}\n.tagO.Hotel{border-color:#b9c9d8;color:#3a5d80}\n.tagO.Outro{border-color:var(--line);color:var(--ink-2)}\n\n/* kanban de leads */\n.hint{font-size:12px;color:var(--ink-3);margin:0 0 12px}\n.kanban{display:grid;grid-auto-flow:column;grid-auto-columns:236px;gap:12px;\n  overflow-x:auto;padding-bottom:8px}\n.kcol{background:var(--soft);border:1px solid var(--line);display:flex;flex-direction:column;\n  min-height:80px}\n.kcol-h{display:flex;justify-content:space-between;align-items:center;padding:10px 12px;\n  font-size:11.5px;font-weight:700;text-transform:uppercase;letter-spacing:.6px;\n  color:var(--ink-2);border-bottom:1px solid var(--line)}\n.kcol-n{background:#fff;border:1px solid var(--line);border-radius:20px;padding:1px 8px;\n  font-size:11px;font-weight:600;color:var(--ink-3)}\n.kcol-b{padding:9px;display:flex;flex-direction:column;gap:9px;overflow-y:auto;max-height:520px;\n  transition:background .12s}\n.kcol-b.dragover{background:var(--sand);outline:2px dashed var(--gold);outline-offset:-2px}\n.kempty{font-size:12px;color:var(--ink-3);text-align:center;padding:14px 0}\n.kcard{background:#fff;border:1px solid var(--line);padding:11px 12px;font-size:12.5px}\n.kcard[draggable=\"true\"]{cursor:grab}\n.kcard.dragging{opacity:.4}\n/* quadrado do cartão pintado por inteiro com a cor do estado (pedido pelo Diogo 2026-07-29\n   — a versão anterior só tinha uma risca fina à esquerda, depois um tom claro a mais;\n   estes -bg2 são tons bem mais fortes, só para os cartões kanban) */\n.kcard.tone-pos{background:var(--pos-bg2);border-color:var(--pos)}\n.kcard.tone-warn{background:var(--warn-bg2);border-color:var(--warn)}\n.kcard.tone-neg{background:var(--neg-bg2);border-color:var(--neg)}\n.kcard.tone-info{background:var(--info-bg2);border-color:var(--info)}\n.kcard.tone-gold{background:var(--gold-bg2);border-color:var(--gold)}\n.kcard.tone-neu{background:var(--neu-bg2);border-color:var(--ink-3)}\n.kc-top{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:6px}\n.kx{border:none;background:none;color:var(--ink-3);cursor:pointer;font-size:15px;line-height:1;\n  padding:0 2px}\n.kx:hover{color:var(--al,#a4604f)}\n.kc-nome{font-weight:600;color:var(--ink);margin-bottom:2px}\n.kc-prod{color:var(--ink-2);margin-bottom:3px}\n.kc-val{font-weight:600;color:var(--gold);margin-bottom:3px}\n.kc-contacto{color:#9d9890;font-size:11px;margin-bottom:5px}\n.kc-badges{display:flex;flex-wrap:wrap;gap:5px;margin-bottom:7px}\n.kbadge{display:inline-block;font-size:10px;padding:2px 7px;border-radius:20px;font-weight:600}\n.kbadge.av{background:var(--warn-bg);color:var(--warn)}\n.kbadge.al{background:var(--neg-bg);color:var(--neg)}\n.kbadge.ok{background:var(--pos-bg);color:var(--pos)}\n.kbadge.in{background:var(--info-bg);color:var(--info)}\n\n/* pontinho de estado nas colunas do kanban (semáforo: cinza=em curso, verde=fechado bem, vermelho=perdido) */\n.kdot{display:inline-block;width:7px;height:7px;border-radius:50%;margin-right:6px;\n  background:var(--ink-3);vertical-align:1px}\n.kdot.pos{background:var(--pos)}\n.kdot.neg{background:var(--neg)}\n.kdot.warn{background:var(--warn)}\n.kdot.info{background:var(--info)}\n.kdot.gold{background:var(--gold)}\n.kdot.neu{background:var(--ink-3)}\n/* urgência das tarefas do Calendário (mesma lógica de cor: vermelho=atrasada, âmbar=urgente) */\ntr.urg-neg td.tdata{color:var(--neg);font-weight:600}\ntr.urg-warn td.tdata{color:var(--warn);font-weight:600}\n.kc-acoes{display:flex;justify-content:space-between;align-items:center;gap:6px}\n\n/* calendário */\n.datescom{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:1px;\n  background:var(--line);border:1px solid var(--line)}\n.dc{background:#fff;padding:13px 15px}\n.dc-n{font-size:12.5px;font-weight:600;color:var(--ink)}\n.dc-d{font-size:11px;color:var(--ink-3);text-transform:capitalize;margin-top:2px}\n.dc-f{font-size:11.5px;color:var(--gold);font-weight:600;margin-top:5px}\n\n/* check de faturas */\n.chk{width:19px;height:19px;border:1.5px solid var(--ink-3);border-radius:3px;display:inline-block;\n  cursor:pointer;position:relative;vertical-align:-4px;background:#fff}\n.chk:hover{border-color:var(--ink)}\n.chk.ro{cursor:default}\n.chk.ro:hover{border-color:var(--ink-3)}\n.chk.on{background:var(--pos);border-color:var(--pos)}\n.chk.on:after{content:\"\";position:absolute;left:5.5px;top:1.5px;width:5px;height:10px;\n  border:solid #fff;border-width:0 2px 2px 0;transform:rotate(45deg)}\ntr.paga td{color:var(--ink-3)}\ntr.vencida td.venc{color:var(--neg);font-weight:600}\n\n.fb-leg{margin-top:14px;font-size:12px;color:var(--ink-3);display:flex;gap:18px;flex-wrap:wrap}\n.fb-leg .sw{display:inline-block;width:9px;height:9px;background:var(--ink);margin-right:6px}\n.fb-leg .sw.lt{background:var(--sand)}\n.empty{color:var(--ink-3);font-size:13px;padding:10px 0}\n.err{background:#fbf1ee;border:1px solid #ecd8d1;padding:16px 18px;color:var(--neg);\n  font-size:13px;line-height:1.6}\n#load{padding:100px 20px;text-align:center;color:var(--ink-3);font-size:13px}\n.spin{width:22px;height:22px;border:2px solid var(--line);border-top-color:var(--ink);\n  border-radius:50%;animation:sp .8s linear infinite;margin:0 auto 15px}\n@keyframes sp{to{transform:rotate(360deg)}}\n\n/* documento de orçamento (só aparece ao imprimir) */\n#orc-print{display:none}\n.doc{max-width:720px;margin:0 auto;padding:26px 30px;color:#141416;font-family:var(--sans)}\n.doc-top{display:flex;justify-content:space-between;align-items:flex-start;gap:30px;\n  padding-bottom:20px;border-bottom:1.5px solid #141416;margin-bottom:24px}\n.doc-top img{width:118px}\n.doc-emp{font-size:11.5px;color:#63605a;text-align:right;line-height:1.7}\n.doc h1{font-family:var(--display);font-size:16px;letter-spacing:2.4px;text-transform:uppercase;\n  margin:0 0 3px}\n.doc .ref{font-size:11.5px;color:#9d9890}\n.doc .para{margin:22px 0 6px;font-size:11px;text-transform:uppercase;letter-spacing:1.1px;\n  color:#9d9890;font-weight:700}\n.doc table{width:100%;border-collapse:collapse;font-size:12.5px;margin-top:8px}\n.doc th{border-bottom:1px solid #141416;padding:0 8px 7px 0;font-size:10.5px;\n  text-transform:uppercase;letter-spacing:.8px;color:#63605a;text-align:left}\n.doc td{padding:9px 8px 9px 0;border-bottom:1px solid #eae7e1}\n.doc .tot td{border-top:1.5px solid #141416;border-bottom:none;font-weight:700;font-size:15px;\n  padding-top:12px}\n.doc .fim{margin-top:30px;font-size:11.5px;color:#63605a;line-height:1.75;\n  border-top:1px solid #eae7e1;padding-top:16px}\n@media print{\n  body{background:#fff}\n  .shell{display:none!important}\n  #orc-print{display:block!important}\n  @page{margin:14mm}\n}\n\n/* janelas estreitas: barra lateral mais fina, mas sempre à esquerda */\n@media(max-width:1000px){\n  .side{width:172px;flex:0 0 172px}\n  .logo img{width:104px}\n  .side a{padding:9px 12px;font-size:13px}\n  .main{padding:26px 22px 60px}\n}\n/* só em ecrãs muito pequenos (telemóvel) é que passa para cima */\n@media(max-width:560px){\n  .shell{flex-direction:column}\n  .side{width:100%;flex:none;height:auto;position:static;border-right:none;\n    border-bottom:1px solid var(--line);padding:18px 0 8px;overflow:visible}\n  .logo{padding:0 16px 14px} .logo img{width:96px}\n  .side nav{display:flex;gap:2px;overflow-x:auto;padding:0 12px}\n  .side a{white-space:nowrap;border-left:none;border-bottom:2px solid transparent;padding:8px 12px}\n  .side a.on{border-left:none;border-bottom-color:var(--gold);background:none}\n  .side .foot{display:none}\n  .main{padding:20px 14px 44px}\n}\n</style>\n</head>\n<body>\n<div class=\"shell\">\n\n<aside class=\"side\">\n  <div class=\"logo\">\n    <img id=\"lg\" src=\"__LOGO__\" alt=\"Ourivesaria Sarmento\"\n         onerror=\"this.style.display='none';document.getElementById('wm').style.display='block'\">\n    <div class=\"wm\" id=\"wm\" style=\"display:none\">SARMENTO<small>OURIVESARIA</small></div>\n  </div>\n  <nav id=\"nav\"></nav>\n  <div class=\"foot\" id=\"foot\"></div>\n</aside>\n\n<main class=\"main\">\n  <div id=\"load\"><div class=\"spin\"></div>A carregar&hellip;</div>\n  <div id=\"page\" style=\"display:none\">\n    <div class=\"top\"><h2 id=\"ptitle\"></h2><div class=\"desc\" id=\"pdesc\"></div></div>\n    <div id=\"body\"></div>\n  </div>\n</main>\n\n</div>\n<div id=\"orc-print\"></div>\n<script>\nvar SNAPSHOT = __DATA__;\nvar SNAP_DATE = \"__SNAPDATE__\";\nvar DRIVE='mcp__57570782-19f3-4a70-bfb9-3bbcc7aa8ace__read_file_content';\nvar SHEETS={visitas:'1QZeS6tK_Av6LmKzC2RcG45X_D2iO38qqlHKQFt4J9n4',\n  vendas:'1dmz7fnpNXOk_yEYo_9ZTFbc2w680NROUX5rhqkwkNyo',\n  trafego:'1i9mrqb_3wiyWGURvAaJ-NLi8juwp9mZ-Pao8V7WGRCs',\n  paginas:'1KmgM9_U1GvLp6vF7oEqQf3kUCVW8Wyc7UZQVZE6SwwA',\n  paginasDet:'1Xo9sBw7TLjou6JD-1Pdpg7AevXGMs_vByQFe3Cuoxrg',\n  origens:'1AVy0vrtVHu1wvfdDzLuUHOuE7CNisUVXucoZtbdDDzE',\n  resumoDia:'1PtJuI5sYdZmildG4Fc-Rr9CyK8IeCup-AuOM3KUUKqs',\n  fornFaturas:'1KdQfgaaOBcquxfJjGFAgvlm2BZx3TqR9zuIS7EEV23A'};\n/* link direto para abrir a folha de faturas a fornecedores (partilhada com quem a Ourivesaria\n   Sarmento convidar como editor — a app só lê, quem edita é diretamente na folha) */\nvar FORN_FATURAS_URL='https://docs.google.com/spreadsheets/d/1KdQfgaaOBcquxfJjGFAgvlm2BZx3TqR9zuIS7EEV23A/edit';\n\n/* ---------- utils ---------- */\nfunction esc(s){return String(s==null?'':s).replace(/[&<>\"]/g,function(c){\n  return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c];});}\nfunction nf(n,d){d=d||0;return (n==null||isNaN(n))?'—':Number(n).toLocaleString('pt-PT',\n  {minimumFractionDigits:d,maximumFractionDigits:d});}\nfunction eur(n){return nf(n,0)+' €';}\nfunction pct(a,b){return b>0?(a/b*100):0;}\nfunction iso(d){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+\n  String(d.getDate()).padStart(2,'0');}\n\nfunction parseTable(txt){\n  if(!txt) return [];\n  var lines=txt.split('\\n').filter(function(l){return l.trim().indexOf('|')===0;});\n  var rows=lines.map(function(l){\n    return l.trim().replace(/^\\|/,'').replace(/\\|$/,'').split('|')\n      .map(function(x){return x.replace(/\\\\([_*`])/g,'$1').trim();});});\n  var isSep=function(r){return r.every(function(x){return /^:?-+:?$/.test(x);});};\n  var isBlank=function(r){return r.every(function(x){return x==='';});};\n  var s=0; while(s<rows.length&&(isBlank(rows[s])||isSep(rows[s]))) s++;\n  if(s>=rows.length) return [];\n  var head=rows[s],out=[];\n  for(var i=s+1;i<rows.length;i++){\n    var r=rows[i]; if(isBlank(r)||isSep(r)) continue;\n    var o={}; for(var j=0;j<head.length;j++) o[head[j]]=r[j]!==undefined?r[j]:'';\n    out.push(o);\n  }\n  return out;\n}\nfunction col(row,cands){\n  var keys=Object.keys(row);\n  for(var i=0;i<cands.length;i++)\n    for(var k=0;k<keys.length;k++)\n      if(keys[k].toLowerCase().indexOf(cands[i].toLowerCase())===0) return row[keys[k]];\n  return '';\n}\nfunction parseDate(s){\n  if(!s) return null; s=String(s).trim();\n  var m=s.match(/^(\\d{4})-(\\d{2})-(\\d{2})/); if(m) return new Date(+m[1],+m[2]-1,+m[3]);\n  m=s.match(/^(\\d{1,2})\\/(\\d{1,2})\\/(\\d{4})/); if(m) return new Date(+m[3],+m[1]-1,+m[2]);\n  var d=new Date(s); return isNaN(d)?null:d;\n}\nvar MES=['Jan','Fev','Mar','Abr','Mai','Jun','Jul','Ago','Set','Out','Nov','Dez'];\nvar DIAS=['Domingo','Segunda','Terça','Quarta','Quinta','Sexta','Sábado'];\nfunction mkey(d){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0');}\nfunction mlabel(k){var p=k.split('-');return MES[+p[1]-1]+(p[1]==='01'?' '+p[0].slice(2):'');}\n\nvar PAIS_MAP=[[/portugal|portugu/i,'Portugal'],[/estados unidos|eua|u\\.?s\\.?a/i,'Estados Unidos'],\n [/fran[çc]/i,'França'],[/alema|german/i,'Alemanha'],[/it[áa]lia|italian/i,'Itália'],\n [/espanh|spain/i,'Espanha'],[/brasil|brazil/i,'Brasil'],[/m[ée]xic/i,'México'],\n [/chin/i,'China'],[/inglaterra|reino unido|brit/i,'Reino Unido'],[/b[ée]lg/i,'Bélgica'],\n [/su[ée]cia|swed/i,'Suécia'],[/gr[ée]cia|greek/i,'Grécia'],[/russ/i,'Rússia'],\n [/israel/i,'Israel'],[/canad/i,'Canadá'],[/angol/i,'Angola'],\n [/holand|pa[íi]ses baixos|dutch/i,'Países Baixos'],[/su[íi][çc]|swiss/i,'Suíça']];\nfunction normPais(v){\n  v=(v||'').trim(); if(!v||/^n[ãa]o sei/i.test(v)) return 'Não indicado';\n  for(var i=0;i<PAIS_MAP.length;i++) if(PAIS_MAP[i][0].test(v)) return PAIS_MAP[i][1];\n  return v.charAt(0).toUpperCase()+v.slice(1).toLowerCase();\n}\nvar INTERESSES=['Fios, Colares e Pendentes','Alianças de Casamento','Anéis de Noivado',\n  'Anéis','Brincos','Pulseiras','Pratas','Relógios'];\nfunction splitOptions(v,known){\n  v=(v||'').trim(); if(!v) return [];\n  var found=[],rest=v;\n  known.slice().sort(function(a,b){return b.length-a.length;}).forEach(function(k){\n    var i=rest.toLowerCase().indexOf(k.toLowerCase());\n    if(i>=0){found.push(k);rest=rest.slice(0,i)+rest.slice(i+k.length);}});\n  rest.split(',').forEach(function(p){\n    p=p.trim().replace(/^[,\\s]+|[,\\s]+$/g,''); if(p.length>1) found.push('Outros');});\n  return found.filter(function(x,i,a){return a.indexOf(x)===i;});\n}\nfunction normMaterial(v){\n  v=(v||'').trim(); if(!v) return [];\n  var low=v.toLowerCase(),out=[];\n  if(/prata dourada|prata dou/.test(low)) out.push('Prata Dourada');\n  if(/\\bprata\\b/.test(low.replace(/prata dourada|prata dou\\.?/g,''))) out.push('Prata');\n  if(/ouro/.test(low)) out.push('Ouro');\n  if(/a[çc]o/.test(low)) out.push('Aço');\n  if(/len[çc]o|seda/.test(low)) out.push('Lenços / Seda');\n  if(/rel[óo]gio/.test(low)) out.push('Relógios');\n  if(!out.length) out.push('Outros');\n  return out;\n}\nvar DIAMANTE_CATS=[\n  [/^brincos?\\b/,'Brincos'],[/^argolas/,'Argolas'],[/^anel/,'Anel'],[/^colar/,'Colar'],\n  [/^escravas?\\b/,'Escravas'],[/^pulseira/,'Pulseira'],[/^fio\\b/,'Fio / Corrente'],\n  [/^(berloque|cora[çc][ãa]o)/,'Berloque'],[/^(alfinete|pin\\b)/,'Alfinete'],\n  [/^cru(z|cifixo)/,'Cruz'],[/^alian[çc]as?\\b/,'Aliança'],[/^medalha/,'Medalha'],\n  [/^bot[õo]es/,'Botões de Punho'],[/^(conserto|servi[çc]os?\\/?restauro)/,'Conserto'],\n  [/^ter[çc]o\\b/,'Terço'],[/^pendente/,'Pendente'],\n  /* peças de decoração/mesa em prata — presentes corporativos, molduras, castiçais, etc. */\n  [/^(moldura|concha|prato|casticais|candelabros|cesto|cafeteira|colher|chapa|caravela|caixinha|caneca|cubo|leiteira|cinzeiro|base|pica|frasco|apert|esticadores|porta|argola\\b|faca|caneta)/,\n    'Prataria e Presentes']\n];\n/* prefixos de quantidade que escondiam a categoria real (ex: \"PAR DE ALIANÇAS\" só tinha \"alianças\"\n   reconhecido depois de tirar o \"par de\") */\nvar DIAMANTE_PREFIXOS_IGNORAR=[/^par de\\s+/,/^par\\s+/,/^conjunto de\\s+/,/^conjunto\\s+/,/^set\\s+/];\nfunction categoriaDiamante(desc){\n  var d=(desc||'').trim().toLowerCase();\n  for(var i=0;i<DIAMANTE_PREFIXOS_IGNORAR.length;i++){\n    var d2=d.replace(DIAMANTE_PREFIXOS_IGNORAR[i],'');\n    if(d2!==d){ d=d2; break; }\n  }\n  for(var j=0;j<DIAMANTE_CATS.length;j++) if(DIAMANTE_CATS[j][0].test(d)) return DIAMANTE_CATS[j][1];\n  return 'Outras';\n}\nfunction materialDiamante(desc){\n  var d=(desc||'').toLowerCase();\n  if(/prata dourada|prata dou/.test(d)) return 'Prata Dourada';\n  if(/\\bprata\\b/.test(d)) return 'Prata';\n  if(/\\bouro\\b/.test(d)) return 'Ouro';\n  if(/\\ba[çc]o\\b/.test(d)) return 'Aço';\n  return 'Não identificado';\n}\nfunction normMotivo(v){\n  v=(v||'').trim(); if(!v) return '';\n  var low=v.toLowerCase();\n  if(/gostou num tipo de material/.test(low)) return 'Queria noutro material';\n  if(/n[ãa]o encontrou/.test(low)) return 'Não encontrou o que queria';\n  if(/n[ãa]o temos|n[ãa]o t[íi]nhamos/.test(low)) return 'Não temos o produto';\n  if(/s[óo] a ver|estava s[óo]/.test(low)) return 'Estava só a ver';\n  if(/vai pensar/.test(low)) return 'Vai pensar';\n  if(/pre[çc]o|or[çc]amento/.test(low)) return 'Preço';\n  if(/comprou|conserto/.test(low)) return '';\n  return v.charAt(0).toUpperCase()+v.slice(1).toLowerCase();\n}\nfunction isSim(v){return /^sim/i.test((v||'').trim());}\nfunction nPessoas(v){\n  v=String(v||'').trim();\n  if(/^\\d+\\+/.test(v)) return parseInt(v,10);\n  var n=parseInt(v,10); return (isNaN(n)||n<=0)?1:n;\n}\nfunction tally(a){var m={};a.forEach(function(k){if(k)m[k]=(m[k]||0)+1;});\n  return Object.keys(m).map(function(k){return{k:k,v:m[k]};}).sort(function(x,y){return y.v-x.v;});}\n\n/* ---------- widgets ---------- */\nfunction kpi(l,v,n,delta,tom){\n  return '<div class=\"kpi'+(tom?' tone-'+tom:'')+'\"><div class=\"lab\">'+esc(l)+'</div><div class=\"val\">'+v+'</div>'+\n    '<div class=\"note\">'+(delta||'')+(delta&&n?' · ':'')+esc(n||'')+'</div></div>';\n}\nfunction deltaTag(atual,anterior,sufixo){\n  if(anterior==null||anterior===0) return '<span class=\"delta flat\">sem comparação</span>';\n  var d=(atual-anterior)/Math.abs(anterior)*100;\n  var cls=Math.abs(d)<1?'flat':d>0?'up':'dn';\n  var seta=Math.abs(d)<1?'=':d>0?'▲':'▼';\n  return '<span class=\"delta '+cls+'\">'+seta+' '+nf(Math.abs(d),0)+'%'+(sufixo||'')+'</span>';\n}\n/* paleta variada para diferenciar categorias em listas/gráficos (cada nome tem sempre a mesma cor) */\nvar PALETA=['#9c7c3c','#4a7c59','#3a5d80','#a4604f','#7d6a9e','#4f8fa6','#a6791f','#6b7d4f'];\nfunction corPorLabel(s){\n  var h=0; s=String(s||'');\n  for(var i=0;i<s.length;i++) h=(h*31+s.charCodeAt(i))|0;\n  return PALETA[Math.abs(h)%PALETA.length];\n}\nfunction barList(items,fmt){\n  if(!items||!items.length) return '<div class=\"empty\">Sem dados neste período.</div>';\n  var mx=Math.max.apply(null,items.map(function(i){return i.v;}))||1;\n  return items.map(function(i){\n    return '<div class=\"bar-row\"><div class=\"bar-top\"><span class=\"bar-lab\">'+esc(i.k)+\n      '</span><b>'+(fmt?fmt(i):nf(i.v))+'</b></div><div class=\"track\"><div class=\"fill\" style=\"width:'+\n      (i.v/mx*100).toFixed(1)+'%;background:'+corPorLabel(i.k)+'\"></div></div></div>';}).join('');\n}\nfunction convTable(rows,head,media){\n  if(!rows||!rows.length) return '<div class=\"empty\">Ainda sem volume suficiente.</div>';\n  return '<table><thead><tr><th>'+esc(head)+'</th><th class=\"num\">Visitas</th>'+\n    '<th class=\"num\">Compraram</th></tr></thead><tbody>'+rows.map(function(t){\n      var cls=t.p>=media+8?'up':t.p<=media-8?'dn':'';\n      return '<tr><td>'+esc(t.k)+'</td><td class=\"num\">'+t.n+'</td><td class=\"num\"><span class=\"rate '+\n        cls+'\">'+nf(t.p,0)+'%</span></td></tr>';}).join('')+'</tbody></table>';\n}\nfunction insight(kind,titulo,texto){\n  return '<div class=\"item\"><div class=\"mark '+kind+'\"></div><div><h4>'+esc(titulo)+\n    '</h4><p>'+texto+'</p></div></div>';\n}\nfunction pendingBox(t,p){return '<div class=\"pending\"><h4>'+esc(t)+'</h4><p>'+p+'</p></div>';}\n\n/* ---------- gráficos ---------- */\nvar CHARTS={},PENDING={};\nfunction chart(id,cfg){PENDING[id]=cfg;}\nfunction fallbackChart(canvas,cfg){\n  var box=canvas.parentNode; if(!box) return;\n  var labels=(cfg.data&&cfg.data.labels)||[], ds=(cfg.data&&cfg.data.datasets)||[];\n  var isPct=!!(cfg.options&&cfg.options.scales&&cfg.options.scales.y&&\n               cfg.options.scales.y.ticks&&cfg.options.scales.y.ticks.callback);\n  var mx=0; ds.forEach(function(d){(d.data||[]).forEach(function(v){if(+v>mx)mx=+v;});});\n  if(!mx) mx=1;\n  var h='';\n  labels.forEach(function(lab,i){\n    h+='<div class=\"bar-row\"><div class=\"bar-top\"><span class=\"bar-lab\">'+esc(lab)+'</span><b>'+\n      ds.map(function(d){var v=(d.data||[])[i];return nf(v,isPct?1:0)+(isPct?'%':'');}).join(' · ')+\n      '</b></div>';\n    ds.forEach(function(d,j){\n      var v=+((d.data||[])[i])||0;\n      h+='<div class=\"track\" style=\"margin-bottom:2px\"><div class=\"fill'+(j?' lt':'')+\n         '\" style=\"width:'+(v/mx*100).toFixed(1)+'%\"></div></div>';});\n    h+='</div>';\n  });\n  if(ds.length>1&&ds[0].label) h+='<div class=\"fb-leg\">'+ds.map(function(d,j){\n    return '<span><span class=\"sw'+(j?' lt':'')+'\"></span>'+esc(d.label||'')+'</span>';}).join('')+'</div>';\n  box.style.height='auto'; box.innerHTML=h;\n}\nfunction flush(){\n  Object.keys(PENDING).forEach(function(id){\n    var el=document.getElementById(id); if(!el) return;\n    var cfg=PENDING[id]; delete PENDING[id];\n    if(typeof Chart==='undefined'){ fallbackChart(el,cfg); return; }\n    try{\n      if(CHARTS[id]) CHARTS[id].destroy();\n      CHARTS[id]=new Chart(el.getContext('2d'),cfg);\n    }catch(e){ fallbackChart(el,cfg); }\n  });\n}\nfunction ensureChart(){\n  return new Promise(function(res){\n    if(typeof Chart!=='undefined') return res(true);\n    var s=document.createElement('script');\n    s.src='https://cdn.jsdelivr.net/npm/chart.js@4.5.0/dist/chart.umd.js';\n    s.onload=function(){res(typeof Chart!=='undefined');};\n    s.onerror=function(){res(false);};\n    document.head.appendChild(s);\n    setTimeout(function(){res(typeof Chart!=='undefined');},3500);\n  });\n}\nvar TICK={font:{size:11.5},color:'#9d9890'};\nvar BASE={responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},\n  scales:{y:{beginAtZero:true,border:{display:false},grid:{color:'#f2efe9'},ticks:TICK},\n          x:{grid:{display:false},border:{display:false},ticks:TICK}}};\nvar LEG={display:true,position:'bottom',labels:{font:{size:11.5},color:'#63605a',\n  boxWidth:9,boxHeight:9,padding:15}};\nvar INK='#141416', SAND='#ddd7ca';\n\n/* ---------- registos guardados no navegador ---------- */\nfunction store(key,fallbackKey,migrar){\n  return {\n    get:function(){\n      try{\n        var v=localStorage.getItem(key);\n        if(v) return JSON.parse(v);\n        if(fallbackKey){\n          var o=localStorage.getItem(fallbackKey);\n          if(o){ var m=(migrar||function(x){return x;})(JSON.parse(o)); this.set(m); return m; }\n        }\n      }catch(e){}\n      return [];\n    },\n    set:function(a){ try{ localStorage.setItem(key,JSON.stringify(a)); }catch(e){} }\n  };\n}\nvar ESTADOS=['nova','fornecedor','enviado','negociacao','ganha','perdida'];\nvar ESTADO_NOME={nova:'Nova',fornecedor:'Pedido a fornecedor',enviado:'Enviado ao cliente',\n                 negociacao:'Em negociação',ganha:'Ganha',perdida:'Perdida'};\n/* cor por estado (pedido pelo Diogo 2026-07-29, mesmo tratamento que já foi feito em\n   Parcerias) — sempre a mesma cor para o mesmo estado, do neutro ao verde/vermelho finais */\nvar LEAD_ESTADO_COR={nova:'neu',fornecedor:'info',enviado:'warn',negociacao:'gold',\n  ganha:'pos',perdida:'neg'};\nvar ORIGENS=['Loja','Site','WhatsApp','Instagram','Telefone'];\nvar LEAD_AVISO_DIAS=2;     // a partir daqui, avisa que a lead está parada\nvar LEAD_URGENTE_DIAS=4;\nvar LIXO_DIAS=30;   // quanto tempo uma lead/parceria apagada fica recuperável no Lixo\nfunction todayISO(){ var d=new Date();\n  return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+\n    String(d.getDate()).padStart(2,'0'); }\n/* separa uma lista em \"vivos\" e \"lixo ainda dentro do prazo\" — o que ficar de fora\n   (lixo com mais de LIXO_DIAS) é purgado em definitivo da próxima vez que a lista for gravada */\nfunction separarLixo(lista){\n  var vivos=[], lixo=[];\n  (lista||[]).forEach(function(x){\n    if(!x.apagado){ vivos.push(x); return; }\n    if(diasEntre(x.apagadoEm||todayISO())<=LIXO_DIAS) lixo.push(x);\n  });\n  return {vivos:vivos,lixo:lixo};\n}\nfunction leadsGet(){\n  var raw=null;\n  try{ raw=localStorage.getItem('sarmento_leads_v3'); }catch(e){}\n  if(raw){ try{ return JSON.parse(raw); }catch(e){} }\n  /* migrar de versões antigas, uma única vez */\n  var origem=null;\n  try{ origem=localStorage.getItem('sarmento_leads_v2')||localStorage.getItem('sarmento_leads_v1'); }\n  catch(e){}\n  var lista=[];\n  if(origem){ try{ lista=JSON.parse(origem); }catch(e){ lista=[]; } }\n  var mapaV2={nova:'nova',contactada:'nova',espera:'negociacao',ganha:'ganha',perdida:'perdida'};\n  var mapaV1={aberta:'nova',contactada:'nova',fechada:'ganha'};\n  lista=lista.map(function(x){\n    var estado=x.estado;\n    if(ESTADOS.indexOf(estado)<0) estado=mapaV2[estado]||mapaV1[estado]||'nova';\n    return Object.assign({},x,{estado:estado,origem:x.origem||'Loja',\n      atualizado:x.atualizado||x.data||todayISO()});\n  });\n  leadsSet(lista);\n  return lista;\n}\nfunction leadsSet(a){ try{ localStorage.setItem('sarmento_leads_v3',JSON.stringify(a)); }catch(e){} }\nfunction diasEntre(iso1,iso2){\n  var a=parseDate(iso1), b=iso2?parseDate(iso2):new Date();\n  if(!a||!b) return 0;\n  a=new Date(a.getFullYear(),a.getMonth(),a.getDate());\n  b=new Date(b.getFullYear(),b.getMonth(),b.getDate());\n  return Math.round((b-a)/86400000);\n}\nfunction leadAtiva(x){ return ['ganha','perdida'].indexOf(x.estado)<0; }\nfunction leadDiasParada(x){ return diasEntre(x.atualizado); }\nfunction leadVencida(x){\n  if(!x.validadeAte) return false;\n  return diasEntre(x.validadeAte)>0;\n}\n/* sarmento_faturas_v1 (localStorage) deixou de ser usado a partir de 2026-07-30 — as faturas a\n   fornecedores passaram para a Google Sheet partilhada (M.FAT, ver FORN_FATURAS_URL), para\n   ficarem visíveis a mais do que uma pessoa. */\nvar CAMPANHAS=store('sarmento_campanhas_v1');\nvar CALENDARIO=store('sarmento_calendario_v1');\n\n/* pipeline de parcerias — fornecedores para coleções exclusivas e negócios locais\n   (hotéis, etc.) para referenciamento de clientes. Inspirado no CRM de parcerias\n   B2B que o Diogo mostrou (Bounce), adaptado: sem GMV automático, valor estimado\n   é um campo manual. */\nvar PARC_ESTADOS=['lead','contactado','negociacao','fechada','ativa','perdida'];\nvar PARC_ESTADO_NOME={lead:'Lead',contactado:'Contactado',negociacao:'Em negociação',\n  fechada:'Parceria fechada',ativa:'Ativa',perdida:'Perdida'};\nvar PARC_TIPOS=['Fornecedor — coleção exclusiva','Hotel — referenciamento','Outro'];\nvar PARC_AVISO_DIAS=15;   // dias sem avanço até ser sinalizada como parada\n/* cor por estado (pedido pelo Diogo 2026-07-29, \"melhor distinção\"), reaproveitando o mesmo\n   sistema semáforo já usado em kpi()/kbadge — cada estado da pipeline tem sempre a mesma cor,\n   do neutro (lead) ao verde (ativa) e vermelho (perdida) */\nvar PARC_ESTADO_COR={lead:'neu',contactado:'info',negociacao:'warn',fechada:'gold',\n  ativa:'pos',perdida:'neg'};\nvar PARCERIAS=store('sarmento_parcerias_v1');\nfunction parceriaEmAberto(x){ return ['lead','contactado','negociacao','fechada'].indexOf(x.estado)>=0; }\nfunction parceriaDiasParada(x){ return diasEntre(x.atualizado); }\nfunction parceriaTipoTag(t){\n  t=t||'';\n  if(t.indexOf('Fornecedor')===0) return 'Fornecedor';\n  if(t.indexOf('Hotel')===0) return 'Hotel';\n  return 'Outro';\n}\n/* notas de arranque pedidas pelo Diogo em 2026-07-29 (coisas a fazer a partir de setembro).\n   Só semeia se a lista ainda nunca foi usada neste navegador (chave inexistente) — nunca\n   apaga/sobrepõe tarefas ou parcerias que já lá estejam. Como tudo aqui é localStorage\n   (por navegador/dispositivo, sem servidor), isto semeia de novo em cada sítio onde o\n   dashboard for aberto pela primeira vez — é a única forma de \"adicionar\" algo à partida. */\nfunction seedStarterData(){\n  try{\n    if(localStorage.getItem('sarmento_calendario_v1')==null){\n      CALENDARIO.set([\n        {id:'C_seed_luzesnatal',data:'2026-09-01',feito:false,\n         tarefa:'Luzes de Natal e ramos para a fachada da loja — procurar fornecedores e '+\n           'confirmar as licenças necessárias junto da Câmara Municipal de Lisboa'}\n      ]);\n    }\n    if(localStorage.getItem('sarmento_parcerias_v1')==null){\n      PARCERIAS.set([\n        {id:'P_seed_presentes',criado:todayISO(),atualizado:todayISO(),estado:'lead',\n         tipo:'Outro',nome:'Presentes corporativos — a identificar empresas',contacto:'',valor:'',\n         notas:'Preparar e enviar propostas de presentes corporativos a partir de setembro. '+\n           'Substituir por empresas concretas assim que identificadas.'},\n        {id:'P_seed_parcerias',criado:todayISO(),atualizado:todayISO(),estado:'lead',\n         tipo:'Outro',nome:'Parcerias comerciais — a identificar',contacto:'',valor:'',\n         notas:'Enviar propostas de parceria (fornecedores, hotéis, outros negócios locais) a '+\n           'partir de setembro. Substituir por parceiros concretos assim que identificados.'}\n      ]);\n    }\n  }catch(e){}\n}\n/* o Diogo apagou sem querer a parceria \"Presentes corporativos\" (semeada acima) antes de a v28\n   ter Lixo/restaurar — como já não há registo nenhum dela para recuperar, repõe-se uma vez\n   (2026-07-29) se não existir já uma parceria com esse nome. Corre só uma única vez de sempre\n   (marcador próprio), para não voltar a aparecer sozinha se o Diogo voltar a apagá-la depois\n   disto — a partir da v28 esse apagar já vai para o Lixo, recuperável durante 30 dias. */\nfunction restaurarPresentesCorporativosSeApagado(){\n  try{\n    if(localStorage.getItem('sarmento_parcerias_presentes_restaurado_v1')) return;\n    localStorage.setItem('sarmento_parcerias_presentes_restaurado_v1','1');\n    if(localStorage.getItem('sarmento_parcerias_v1')==null) return; // seedStarterData já trata disto\n    var P=PARCERIAS.get();\n    if(P.some(function(x){return /presentes corporativos/i.test(x.nome||'');})) return;\n    P.push({id:'P_seed_presentes_v2',criado:todayISO(),atualizado:todayISO(),estado:'lead',\n      tipo:'Outro',nome:'Presentes corporativos — a identificar empresas',contacto:'',valor:'',\n      notas:'Preparar e enviar propostas de presentes corporativos a partir de setembro. '+\n        'Substituir por empresas concretas assim que identificadas.'});\n    PARCERIAS.set(P);\n  }catch(e){}\n}\nvar TIPOS_CAMP=['Newsletter','SMS','Campanha de email'];\n/* objetivo de frequência, em dias, por tipo de envio */\nvar CADENCIA={'Newsletter':30,'SMS':45,'Campanha de email':30};\nvar UI={formLead:false,formFat:false,formCamp:false,formOrc:false,formTodo:false,\n        formParceria:false,\n        filtroEstado:null,filtroForn:'',orc:{itens:[]},diamRange:null,\n        clientesFiltro:null,clientesRecFiltro:null,clienteSelecionado:null};\n\nfunction euroNum(v){\n  v=String(v==null?'':v).replace(/[^\\d,.-]/g,'').replace(/\\.(?=\\d{3}\\b)/g,'').replace(',','.');\n  var n=parseFloat(v); return isNaN(n)?0:n;\n}\nfunction csvLinhas(rows,cols,tit,sep){\n  return [tit.join(sep)].concat(rows.map(function(x){\n    return cols.map(function(c){\n      var v=String(x[c]==null?'':x[c]).replace(/[\\r\\n]+/g,' ');\n      return sep===','&&/[\",]/.test(v)?'\"'+v.replace(/\"/g,'\"\"')+'\"':v;\n    }).join(sep);})).join('\\n');\n}\nfunction descarregar(texto,nome){\n  var blob=new Blob(['﻿'+texto],{type:'text/csv;charset=utf-8'});\n  var a=document.createElement('a');\n  a.href=URL.createObjectURL(blob); a.download=nome; a.click();\n}\nfunction copiar(texto,elMsg){\n  function ok(){ if(elMsg){elMsg.textContent='Copiado. Cola diretamente na folha.';} }\n  function fb(){\n    var ta=document.createElement('textarea'); ta.value=texto; document.body.appendChild(ta);\n    ta.select(); try{document.execCommand('copy');ok();}catch(e){} document.body.removeChild(ta);\n  }\n  if(navigator.clipboard&&navigator.clipboard.writeText)\n    navigator.clipboard.writeText(texto).then(ok,fb);\n  else fb();\n}\n\n/* ---------- carregamento ---------- */\nasync function loadLive(){\n  var out={};\n  await Promise.all(Object.keys(SHEETS).map(async function(k){\n    var r=await window.cowork.callMcpTool(DRIVE,{fileId:SHEETS[k]});\n    if(r.isError) throw new Error(k);\n    var d=r.structuredContent;\n    if(!d){try{d=JSON.parse(r.content[0].text);}catch(e){d={fileContent:r.content[0].text};}}\n    out[k]=parseTable(d.fileContent||d.content||'');\n  }));\n  return out;\n}\nvar DATA=null,LIVE=false,M=null,RANGE=null;\nfunction fatal(msg,det){\n  var l=document.getElementById('load');\n  if(l) l.innerHTML='<div style=\"max-width:480px;margin:0 auto;text-align:left\" class=\"err\"><b>'+\n    esc(msg)+'</b><br><br>'+esc(det||'')+'</div>';\n}\n(async function(){\n  try{\n    try{\n      if(window.cowork&&window.cowork.callMcpTool){DATA=await loadLive();LIVE=true;}\n      else DATA=SNAPSHOT;\n    }catch(e){DATA=SNAPSHOT;LIVE=false;}\n    /* histórico do Diamante vem sempre de um export manual — ainda não há sync automático */\n    DATA.diamante_vendas=SNAPSHOT.diamante_vendas;\n    DATA.diamante_fornecedor_sku=SNAPSHOT.diamante_fornecedor_sku;\n    DATA.diamante_clientes=SNAPSHOT.diamante_clientes;\n    if(!DATA||!DATA.visitas||!DATA.visitas.length)\n      return fatal('Não foi possível carregar os dados.',\n        'Verifica se as folhas continuam partilhadas com a conta ligada ao Drive.');\n    try{ await ensureChart(); }catch(e){}\n    M=buildModel(DATA);\n    RANGE=presets(M).mes;\n    seedStarterData();\n    restaurarPresentesCorporativosSeApagado();\n    renderNav();\n    go(location.hash.replace('#','')||'resumo');\n    document.getElementById('load').style.display='none';\n    document.getElementById('page').style.display='block';\n  }catch(e){ fatal('Ocorreu um erro ao construir o painel.',(e&&e.message)||String(e)); }\n})();\n\n/* ---------- modelo ---------- */\nfunction buildModel(D){\n  var M={};\n  M.V=(D.visitas||[]).map(function(r){\n    return {data:parseDate(col(r,['Carimbo'])),\n      pessoas:nPessoas(col(r,['Nº Pessoas','N Pessoas'])),\n      tipo:(col(r,['Tipo de Cliente'])||'Não indicado').trim(),\n      pais:normPais(col(r,['País','Pais'])),\n      idade:(col(r,['Idade'])||'Não indicado').trim(),\n      genero:(col(r,['Género','Genero'])||'Não indicado').trim(),\n      interesse:splitOptions(col(r,['Interesse Principal']),INTERESSES),\n      material:normMaterial(col(r,['Material Procurado'])),\n      comprou:isSim(col(r,['Comprou'])),\n      motivo:normMotivo(col(r,['Se não comprou','Se nao comprou'])),\n      contacto:isSim(col(r,['Deixou contacto']))};\n  }).filter(function(v){return v.data;});\n\n  M.E=(D.vendas||[]).map(function(r){\n    return {data:parseDate(col(r,['Data do Pedido'])),cliente:col(r,['Cliente']),\n      produtos:col(r,['Produtos']),\n      valor:parseFloat(String(col(r,['Valor Total'])).replace(',','.'))||0,\n      estado:(col(r,['Estado'])||'').trim().toLowerCase()};\n  }).filter(function(e){return e.data;});\n\n  M.T=(D.trafego||[]).map(function(r){\n    return {data:parseDate(col(r,['Data'])),\n      sessoes:parseInt(col(r,['Sessões','Sessoes']),10)||0};\n  }).filter(function(t){return t.data;});\n\n  M.P=(D.paginas||[]).map(function(r){\n    return {url:col(r,['Página','Pagina']),\n      titulo:(col(r,['Título','Titulo'])||'').replace(/\\s*-\\s*Ourivesaria Sarmento\\s*$/i,''),\n      views:parseInt(col(r,['Visualizações','Visualizacoes']),10)||0};\n  }).filter(function(p){return p.url;});\n\n  /* --- analytics detalhado (folhas 12-14) --- */\n  M.PD=(D.paginasDet||[]).map(function(r){\n    return {data:parseDate(col(r,['Data'])),url:col(r,['Página','Pagina']),\n      titulo:(col(r,['Título','Titulo'])||'').replace(/\\s*-\\s*Ourivesaria Sarmento\\s*$/i,''),\n      views:parseInt(col(r,['Visualizações','Visualizacoes']),10)||0,\n      sessoes:parseInt(col(r,['Sessões','Sessoes']),10)||0,\n      tempo:parseInt(col(r,['Tempo médio','Tempo medio']),10)||0};\n  }).filter(function(p){return p.data&&p.url;});\n\n  M.OD=(D.origens||[]).map(function(r){\n    return {data:parseDate(col(r,['Data'])),tipo:(col(r,['Tipo'])||'').trim(),\n      valor:(col(r,['Valor'])||'').trim(),\n      sessoes:parseInt(col(r,['Sessões','Sessoes']),10)||0};\n  }).filter(function(o){return o.data&&o.tipo;});\n\n  M.RD=(D.resumoDia||[]).map(function(r){\n    return {data:parseDate(col(r,['Data'])),\n      sessoes:parseInt(col(r,['Sessões','Sessoes']),10)||0,\n      views:parseInt(col(r,['Visualizações','Visualizacoes']),10)||0,\n      duracao:parseFloat(col(r,['Duração média','Duracao media']))||0,\n      engajamento:parseFloat(col(r,['Taxa de envolvimento']))||0};\n  }).filter(function(r){return r.data;});\n\n  /* --- faturas a fornecedores, numa Google Sheet partilhada (2026-07-30) — o Diogo e o\n     superior dele marcam pago/por pagar diretamente na folha (link em FORN_FATURAS_URL);\n     a app só lê, para mostrar dívida total e por fornecedor. Uma linha de exemplo fica na\n     folha para mostrar o formato e é ignorada aqui (fornecedor começa por \"Exemplo\"). */\n  M.FAT=(D.fornFaturas||[]).map(function(r,i){\n    var pagoTxt=(col(r,['Pago'])||'').trim();\n    return {id:'FAT'+i,fornecedor:(col(r,['Fornecedor'])||'').trim(),\n      numero:col(r,['Nº Fatura','No Fatura','Fatura','N Fatura']),\n      data:col(r,['Data']),vencimento:col(r,['Vencimento']),\n      valor:euroNum(col(r,['Valor'])),\n      pago:isSim(pagoTxt)||/^true$/i.test(pagoTxt),\n      notas:col(r,['Notas'])};\n  }).filter(function(x){return x.fornecedor&&!/^exemplo/i.test(x.fornecedor);});\n\n  /* --- histórico real de vendas do Diamante (export manual, 2021+) --- */\n  /* SKUs excluídos a pedido do Diogo (2026-07-28): não são vendas normais de loja\n     (renovação/encomenda especial pontual), não devem entrar nas estatísticas. */\n  var VENDAS_EXCLUIR_SKU={'FAQUEIRO01':1,'CC00000126':1};\n  var fornPorSku=D.diamante_fornecedor_sku||{};\n  M.DV=(D.diamante_vendas||[]).map(function(r){\n    return {data:parseDate(r[0]),cliente:r[1]||null,doc:r[2],sku:r[3],desc:r[4]||'',\n      qtd:r[5]||0,total:r[6]||0,\n      categoria:categoriaDiamante(r[4]),material:materialDiamante(r[4]),\n      fornecedor:tituloNome(fornPorSku[r[3]])||null};\n  }).filter(function(v){return v.data&&!VENDAS_EXCLUIR_SKU[v.sku];});\n  M.DC=(D.diamante_clientes||[]).map(function(r){\n    return {codigo:r[0],nome:r[1],tel:r[2],email:r[3],localidade:r[4],\n      c0:r[5]||0,c1:r[6]||0,c2:r[7]||0,c3:r[8]||0,c4:r[9]||0,c5:r[10]||0,criado:r[11]};\n  });\n\n  M.datas=M.V.map(function(v){return v.data;}).sort(function(a,b){return a-b;});\n  var todos=M.stats=stats(M,M.datas[0],M.datas[M.datas.length-1]);\n  M.nV=todos.nV; M.nC=todos.nC; M.conv=todos.conv;\n  M.nContacto=todos.nContacto; M.recOnline=todos.recOnline; M.Eok=todos.Eok;\n  M.sess=M.RD.length ? M.RD.reduce(function(s,r){return s+r.sessoes;},0)\n                     : M.T.reduce(function(s,t){return s+t.sessoes;},0);\n  M.segTipo=todos.segTipo; M.segPais=todos.segPais; M.segMat=todos.segMat;\n  M.segInt=todos.segInt; M.motivos=todos.motivos;\n  M.mesesK=todos.mesesK; M.porMes=todos.porMes;\n  return M;\n}\n\n/* estatísticas para um período */\nfunction stats(M,de,ate){\n  var d0=new Date(de.getFullYear(),de.getMonth(),de.getDate());\n  var d1=new Date(ate.getFullYear(),ate.getMonth(),ate.getDate(),23,59,59);\n  var S={de:d0,ate:d1};\n  var V=M.V.filter(function(v){return v.data>=d0&&v.data<=d1;});\n  var E=M.E.filter(function(e){return e.data>=d0&&e.data<=d1;});\n  S.V=V; S.E=E;\n  S.Eok=E.filter(function(e){return ['completed','processing','on-hold'].indexOf(e.estado)>=0;});\n  S.nV=V.length;\n  S.nC=V.filter(function(v){return v.comprou;}).length;\n  S.conv=pct(S.nC,S.nV);\n  S.nContacto=V.filter(function(v){return v.contacto;}).length;\n  S.recOnline=S.Eok.reduce(function(s,e){return s+e.valor;},0);\n  S.pessoas=V.reduce(function(s,v){return s+v.pessoas;},0);\n\n  function seg(f,min){\n    var m={};\n    V.forEach(function(v){var ks=f(v);(Array.isArray(ks)?ks:[ks]).forEach(function(k){\n      if(!k) return; m[k]=m[k]||{n:0,c:0}; m[k].n++; if(v.comprou) m[k].c++;});});\n    return Object.keys(m).map(function(k){return{k:k,n:m[k].n,c:m[k].c,p:pct(m[k].c,m[k].n)};})\n      .filter(function(x){return x.n>=(min||1);});\n  }\n  S.segTipo=seg(function(v){return v.tipo;},5).sort(function(a,b){return b.p-a.p;});\n  S.segPais=seg(function(v){return v.pais==='Não indicado'?null:v.pais;},4)\n    .sort(function(a,b){return b.n-a.n;});\n  S.segMat=seg(function(v){return v.material;},5).sort(function(a,b){return b.n-a.n;});\n  S.segInt=seg(function(v){return v.interesse;},3).sort(function(a,b){return b.n-a.n;});\n  /* categorias efetivamente compradas */\n  S.vendidas=tally([].concat.apply([],V.filter(function(v){return v.comprou;})\n    .map(function(v){return v.interesse;})));\n  S.materiaisVendidos=tally([].concat.apply([],V.filter(function(v){return v.comprou;})\n    .map(function(v){return v.material;})));\n  S.motivos=tally(V.filter(function(v){return !v.comprou&&v.motivo;})\n    .map(function(v){return v.motivo;}));\n  S.porDia=seg(function(v){return DIAS[v.data.getDay()];},1);\n\n  var pm={};\n  V.forEach(function(v){var k=mkey(v.data);pm[k]=pm[k]||{n:0,c:0};pm[k].n++;if(v.comprou)pm[k].c++;});\n  S.mesesK=Object.keys(pm).sort(); S.porMes=pm;\n  return S;\n}\n/* ---------- avisos de ritmo ----------\n   Compara sempre o MESMO número de dias: se hoje é dia 7, compara 1–7 deste mês\n   com 1–7 do mês anterior e com 1–7 do mesmo mês do ano passado.             */\nfunction diasNoMes(ano,mes){ return new Date(ano,mes+1,0).getDate(); }\nfunction fimSeguro(ano,mes,dia){ return new Date(ano,mes,Math.min(dia,diasNoMes(ano,mes))); }\n\nfunction ritmo(M){\n  var ult=M.datas[M.datas.length-1];\n  var ano=ult.getFullYear(), mes=ult.getMonth(), dia=ult.getDate();\n  var completo = dia===diasNoMes(ano,mes);\n  return {\n    dia:dia, mes:mes, ano:ano, completo:completo, ult:ult,\n    atual:    stats(M,new Date(ano,mes,1),ult),\n    anterior: stats(M,new Date(ano,mes-1,1),fimSeguro(ano,mes-1,dia)),\n    homologo: stats(M,new Date(ano-1,mes,1),fimSeguro(ano-1,mes,dia))\n  };\n}\n\nvar MESES_LONGOS=['janeiro','fevereiro','março','abril','maio','junho','julho','agosto',\n  'setembro','outubro','novembro','dezembro'];\nfunction avisosRitmo(M){\n  var R=ritmo(M), out=[];\n  var nomeMes=MESES_LONGOS[R.mes], ateDia=R.completo?'':' até ao dia '+R.dia;\n  var homologo = R.homologo.nV>0;\n  var base = homologo ? R.homologo : R.anterior;\n  /* \"contra X ___\"  */\n  var baseFrase = homologo ? 'no mesmo período do ano passado'\n                           : 'no mesmo período de '+MESES_LONGOS[(R.mes+11)%12];\n  /* para títulos */\n  var baseCurto = homologo ? 'o ano passado' : MESES_LONGOS[(R.mes+11)%12];\n\n  /* compras em loja */\n  if(base.nV>0){\n    var d=R.atual.nC-base.nC;\n    var p=base.nC>0?Math.abs(d)/base.nC*100:0;\n    if(d>0) out.push(['op','Estamos à frente d'+(homologo?'o ano passado':'e '+baseCurto),\n      'Já vamos <b>'+R.atual.nC+' compras</b> em '+nomeMes+ateDia+', contra '+base.nC+' '+\n      baseFrase+'. São <b>+'+nf(p,0)+'%</b>, mais '+d+(d===1?' compra':' compras')+'.']);\n    else if(d<0) out.push(['al','Estamos atrás d'+(homologo?'o ano passado':'e '+baseCurto),\n      'Vamos em <b>'+R.atual.nC+' compras</b> em '+nomeMes+ateDia+', contra '+base.nC+' '+\n      baseFrase+'. Faltam <b>'+Math.abs(d)+(Math.abs(d)===1?' compra':' compras')+\n      '</b> para igualar ('+nf(p,0)+'% abaixo).']);\n    else out.push(['in','Ao mesmo ritmo de '+baseCurto,\n      'Exatamente <b>'+R.atual.nC+' compras</b> em '+nomeMes+ateDia+', tal como '+baseFrase+'.']);\n  }\n\n  /* receita online */\n  if(base.recOnline>0||R.atual.recOnline>0){\n    var dv=R.atual.recOnline-base.recOnline;\n    if(dv<0) out.push(['al','Faltam '+eur(Math.abs(dv))+' para igualar no online',\n      'Vamos em <b>'+eur(R.atual.recOnline)+'</b>'+ateDia+', contra '+eur(base.recOnline)+\n      ' '+baseFrase+'.']);\n    else if(dv>0&&base.recOnline>0) out.push(['op','Online acima d'+\n      (homologo?'o ano passado':'e '+baseCurto),\n      '<b>'+eur(R.atual.recOnline)+'</b>'+ateDia+', mais '+eur(dv)+' do que '+baseFrase+\n      ' (+'+nf(dv/base.recOnline*100,0)+'%).']);\n  }\n\n  /* projeção do mês */\n  if(!R.completo&&R.dia>=7&&R.atual.nC>0){\n    var proj=Math.round(R.atual.nC/R.dia*diasNoMes(R.ano,R.mes));\n    out.push(['in','A este ritmo, '+nomeMes+' fecha com ~'+proj+' compras',\n      '<b>'+R.atual.nC+' compras</b> em '+R.dia+' dias dá uma média de '+\n      nf(R.atual.nC/R.dia,1)+' por dia.'+\n      (base.nC>0?' O período completo de referência teve '+base.nC+'.':'')]);\n  }\n\n  if(R.homologo.nV===0)\n    out.push(['in','Ainda não há ano anterior para comparar',\n      'O registo de visitas começou a <b>'+esc(M.datas[0].toLocaleDateString('pt-PT'))+\n      '</b>, por isso as comparações acima são com o mês anterior. A partir de <b>'+\n      esc(new Date(M.datas[0].getFullYear()+1,M.datas[0].getMonth(),M.datas[0].getDate())\n          .toLocaleDateString('pt-PT'))+'</b> passa a comparar ano a ano sozinho.']);\n  return out;\n}\n\n/* ---------- avisos de marketing ---------- */\nfunction diasDesde(d){ return Math.floor((Date.now()-d.getTime())/86400000); }\nfunction ultimoEnvio(tipo){\n  var C=CAMPANHAS.get().filter(function(x){return x.tipo===tipo&&parseDate(x.data);})\n    .sort(function(a,b){return parseDate(b.data)-parseDate(a.data);});\n  return C.length?C[0]:null;\n}\nfunction avisosMarketing(){\n  var out=[], C=CAMPANHAS.get();\n  if(!C.length){\n    out.push(['in','Sem envios registados',\n      'Assim que registares a primeira newsletter ou SMS em <b>Marketing</b>, aparece aqui um '+\n      'aviso quando estiver na altura de enviar outra.']);\n    return out;\n  }\n  TIPOS_CAMP.forEach(function(tipo){\n    var u=ultimoEnvio(tipo); if(!u) return;\n    var dias=diasDesde(parseDate(u.data)), alvo=CADENCIA[tipo]||30;\n    if(dias>alvo) out.push(['al','Está na altura de enviar '+\n      (tipo==='SMS'?'novo SMS':'nova '+tipo.toLowerCase()),\n      'A última foi há <b>'+dias+' dias</b> ('+esc(u.assunto||u.data)+'). O objetivo são '+\n      alvo+' dias — já passaram '+(dias-alvo)+' além do previsto.']);\n    else out.push(['op',tipo+' em dia',\n      'A última foi há <b>'+dias+' dias</b>. Próxima prevista daqui a '+(alvo-dias)+' dias.']);\n  });\n  /* desempenho da última newsletter face à média */\n  var comAb=C.filter(function(x){return x.tipo==='Newsletter'&&euroNum(x.aberturas)>0;})\n             .sort(function(a,b){return parseDate(b.data)-parseDate(a.data);});\n  if(comAb.length>=3){\n    var ultima=euroNum(comAb[0].aberturas);\n    var resto=comAb.slice(1);\n    var media=resto.reduce(function(s,x){return s+euroNum(x.aberturas);},0)/resto.length;\n    var dif=ultima-media;\n    if(Math.abs(dif)>=3) out.push([dif>0?'op':'al',\n      'Última newsletter '+(dif>0?'acima':'abaixo')+' da média',\n      '<b>'+nf(ultima,1)+'%</b> de aberturas, contra uma média de '+nf(media,1)+'% nas '+\n      resto.length+' anteriores.'+(dif<0?' Vale a pena rever o assunto e a hora de envio.':'')]);\n  }\n  return out;\n}\n\n/* ---------- avisos da pipeline de leads (paradas / orçamentos a vencer) ---------- */\nfunction avisosLeads(){\n  var out=[], L=leadsGet().filter(leadAtiva);\n  if(!L.length) return out;\n\n  var paradas=L.filter(function(x){return leadDiasParada(x)>=LEAD_AVISO_DIAS;});\n  var urgentes=paradas.filter(function(x){return leadDiasParada(x)>=LEAD_URGENTE_DIAS;});\n  if(paradas.length){\n    var nomes=paradas.slice().sort(function(a,b){return leadDiasParada(b)-leadDiasParada(a);})\n      .slice(0,3).map(function(x){return (x.nome||'Sem nome')+' ('+leadDiasParada(x)+'d)';}).join(', ');\n    out.push([urgentes.length?'al':'in',\n      paradas.length+(paradas.length===1?' lead parada há '+LEAD_AVISO_DIAS+'+ dias':\n        ' leads paradas há '+LEAD_AVISO_DIAS+'+ dias'),\n      'Sem avanço na pipeline: <b>'+esc(nomes)+'</b>'+(paradas.length>3?' e outras.':'.')+\n      ' Vale a pena retomar o contacto antes que arrefeçam.']);\n  }\n  var vencidas=L.filter(leadVencida);\n  if(vencidas.length){\n    var nv=vencidas.slice(0,3).map(function(x){return x.nome||'Sem nome';}).join(', ');\n    out.push(['al',vencidas.length+(vencidas.length===1?' orçamento com validade expirada':\n        ' orçamentos com validade expirada'),\n      '<b>'+esc(nv)+'</b>'+(vencidas.length>3?' e outros':'')+' já passaram do prazo de validade. '+\n      'Confirma se ainda interessa ao cliente ou fecha como perdida.']);\n  }\n  var aVencerEm3=L.filter(function(x){\n    return x.validadeAte && !leadVencida(x) && diasEntre(todayISO(),x.validadeAte)<=3;});\n  if(aVencerEm3.length)\n    out.push(['in',aVencerEm3.length+(aVencerEm3.length===1?' orçamento vence nos próximos 3 dias':\n        ' orçamentos vencem nos próximos 3 dias'),\n      'Boa altura para um lembrete ao cliente antes que a proposta caduque.']);\n  return out;\n}\n\n/* ---------- estatísticas do site para um período ---------- */\nfunction ehProduto(u){ return /^\\/produto\\//.test(u); }\nfunction ehCategoria(u){ return /^\\/categoria\\/|^\\/material\\//.test(u); }\nvar ORIGEM_NOME={'Organic Search':'Pesquisa (Google, etc.)','Organic Social':'Redes sociais',\n  'Direct':'Acesso direto','Referral':'Outro site','Unassigned':'Não identificada',\n  'AI Assistant':'Assistente de IA','Cross-network':'Rede paga','Paid Search':'Pesquisa paga',\n  'Paid Social':'Redes sociais (pago)','Email':'Email'};\nvar DISP_NOME={mobile:'Telemóvel',desktop:'Computador',tablet:'Tablet'};\n\nfunction siteStats(M,de,ate){\n  var d0=new Date(de.getFullYear(),de.getMonth(),de.getDate());\n  var d1=new Date(ate.getFullYear(),ate.getMonth(),ate.getDate(),23,59,59);\n  var S={};\n  S.rd=M.RD.filter(function(r){return r.data>=d0&&r.data<=d1;});\n  S.pd=M.PD.filter(function(r){return r.data>=d0&&r.data<=d1;});\n  S.od=M.OD.filter(function(r){return r.data>=d0&&r.data<=d1;});\n\n  S.sessoes=S.rd.reduce(function(s,r){return s+r.sessoes;},0);\n  S.views=S.rd.reduce(function(s,r){return s+r.views;},0);\n  S.duracao = S.sessoes ? S.rd.reduce(function(s,r){return s+r.duracao*r.sessoes;},0)/S.sessoes : 0;\n  S.engaj   = S.sessoes ? S.rd.reduce(function(s,r){return s+r.engajamento*r.sessoes;},0)/S.sessoes : 0;\n\n  /* páginas: agregado por URL, tempo médio ponderado por visualizações */\n  var pm={};\n  S.pd.forEach(function(r){\n    var o=pm[r.url]=pm[r.url]||{url:r.url,titulo:r.titulo,views:0,sessoes:0,tempoSoma:0};\n    o.views+=r.views; o.sessoes+=r.sessoes; o.tempoSoma+=r.tempo*r.views;\n  });\n  S.paginas=Object.keys(pm).map(function(k){\n    var o=pm[k]; return {url:o.url,titulo:o.titulo,views:o.views,sessoes:o.sessoes,\n      tempo:o.views?Math.round(o.tempoSoma/o.views):0};\n  }).sort(function(a,b){return b.views-a.views;});\n  S.produtos  =S.paginas.filter(function(p){return ehProduto(p.url);});\n  S.categorias=S.paginas.filter(function(p){return ehCategoria(p.url);});\n  S.outras    =S.paginas.filter(function(p){return !ehProduto(p.url)&&!ehCategoria(p.url);});\n\n  /* origens e dispositivos */\n  function somaTipo(tipo){\n    var m={};\n    S.od.filter(function(o){return o.tipo===tipo;}).forEach(function(o){\n      m[o.valor]=(m[o.valor]||0)+o.sessoes;});\n    return Object.keys(m).map(function(k){return{k:k,v:m[k]};})\n      .sort(function(a,b){return b.v-a.v;});\n  }\n  S.origens=somaTipo('Origem');\n  S.dispositivos=somaTipo('Dispositivo');\n\n  var rdK={};\n  S.rd.forEach(function(r){ rdK[iso(r.data)]=r; });\n  S.rdDias=Object.keys(rdK).sort();\n  S.porDia=rdK;\n  return S;\n}\n\n/* mesmo intervalo, deslocado para trás */\nfunction periodoAnterior(M,de,ate,anos){\n  var d0,d1;\n  if(anos){ d0=new Date(de.getFullYear()-1,de.getMonth(),de.getDate());\n            d1=new Date(ate.getFullYear()-1,ate.getMonth(),ate.getDate()); }\n  else { var dur=ate-de+86400000;\n         d1=new Date(de.getTime()-86400000); d0=new Date(d1.getTime()-dur+86400000); }\n  return stats(M,d0,d1);\n}\n\n/* ---------- navegação ---------- */\nvar NAV=[\n  ['resumo','Resumo'],\n  ['vendas','Vendas'],\n  ['loja','Registo de atendimento'],\n  ['clientes','Clientes'],\n  ['leads','Leads'],\n  ['parcerias','Parcerias'],\n  ['site','Site'],\n  ['marketing','Marketing'],\n  ['fornecedores','Fornecedores'],\n  ['calendario','Calendário'],\n  ['ebitda','EBITDA']\n];\n/* títulos de secção puramente visuais, inspirados na sidebar do Dynamics que o Diogo mostrou —\n   ao contrário do antigo grupo colapsável \"Estatísticas\", isto não esconde nada: todas as\n   páginas continuam sempre visíveis e a um clique, só ficam organizadas em blocos com título. */\nvar NAV_SECAO_INICIO={vendas:'Comercial',site:'Site & Marketing',fornecedores:'Gestão'};\nfunction renderNav(){\n  document.getElementById('nav').innerHTML=NAV.map(function(p){\n    var sec=NAV_SECAO_INICIO[p[0]];\n    return (sec?'<div class=\"nav-sec\">'+esc(sec)+'</div>':'')+\n      '<a data-p=\"'+p[0]+'\" href=\"#'+p[0]+'\">'+esc(p[1])+'</a>';\n  }).join('');\n  document.getElementById('nav').addEventListener('click',function(e){\n    var a=e.target.closest('a'); if(!a) return; e.preventDefault(); go(a.dataset.p);\n  });\n  var per=M.datas.length? M.datas[0].toLocaleDateString('pt-PT')+' a '+\n      M.datas[M.datas.length-1].toLocaleDateString('pt-PT') : '—';\n  document.getElementById('foot').innerHTML=\n    (LIVE?'Dados ao vivo':'Dados de '+esc(SNAP_DATE))+'<br>'+per;\n}\nfunction ativarNav(id){\n  document.querySelectorAll('#nav a').forEach(function(a){\n    a.classList.toggle('on',a.dataset.p===id);});\n}\nvar PAGINA='resumo';\nfunction go(id){\n  if(!PAGES[id]) id='resumo';\n  PAGINA=id; location.hash=id;\n  ativarNav(id);\n  var P=PAGES[id](M);\n  document.getElementById('ptitle').textContent=P.t;\n  document.getElementById('pdesc').textContent=P.d||'';\n  document.getElementById('body').innerHTML=P.html;\n  if(P.after) P.after();\n  window.scrollTo(0,0);\n  flush();\n}\nfunction refresh(){ go(PAGINA); }\n\n/* ---------- páginas ---------- */\nvar PAGES={};\n\n/* ---------- avisos ligados a vendas reais, clientes e faturas (para o Resumo) ---------- */\nfunction avisosVendasReais(M){\n  var out=[];\n  if(!M.DV||!M.DV.length) return out;\n  var P=diamPresets(M).mes;\n  var atual=diamanteAgg(M,P.de,P.ate);\n  var pAnt=diamPeriodoAnterior(P.de,P.ate);\n  var ant=diamanteAgg(M,pAnt.de,pAnt.ate);\n  var nomeMes=MESES_LONGOS[P.de.getMonth()];\n  if(ant.totalGeral>0){\n    var d=atual.totalGeral-ant.totalGeral,p=Math.abs(d)/ant.totalGeral*100;\n    if(d>0) out.push(['op','Vendas acima do ano passado em '+nomeMes,\n      'Já vendemos <b>'+eur(atual.totalGeral)+'</b> este mês, contra '+eur(ant.totalGeral)+\n      ' no mesmo período do ano passado. São <b>+'+nf(p,0)+'%</b>.']);\n    else if(d<0) out.push(['al','Vendas abaixo do ano passado em '+nomeMes,\n      'Vamos em <b>'+eur(atual.totalGeral)+'</b> este mês, contra '+eur(ant.totalGeral)+\n      ' no mesmo período do ano passado. Faltam <b>'+eur(Math.abs(d))+'</b> para igualar ('+\n      nf(p,0)+'% abaixo).']);\n    else out.push(['in','Vendas ao mesmo nível do ano passado',\n      'Exatamente <b>'+eur(atual.totalGeral)+'</b>, igual ao mesmo período do ano passado.']);\n  } else {\n    out.push(['in','Ainda sem o mesmo período do ano passado para comparar',\n      'Assim que houver dados de '+nomeMes+' do ano anterior, esta comparação passa a aparecer sozinha.']);\n  }\n  var topCat=atual.categorias[0];\n  if(topCat&&atual.totalGeral) out.push(['in','Categoria mais forte em '+nomeMes.toLowerCase(),\n    '<b>'+esc(topCat.k)+'</b> lidera com '+eur(topCat.total)+' ('+\n    nf(topCat.total/atual.totalGeral*100,0)+'% da receita do mês).']);\n  return out;\n}\nfunction avisosClientes(M){\n  var out=[];\n  if(!M.DC||!M.DC.length) return out;\n  var AT=clientesAtividade(M);\n  var risco=AT.filter(function(x){return x.estado==='risco';});\n  var dorm=AT.filter(function(x){return x.estado==='dormente';});\n  var valor=risco.concat(dorm).reduce(function(s,x){return s+x.total;},0);\n  if(risco.length+dorm.length)\n    out.push(['al',(risco.length+dorm.length)+' clientes por recuperar',\n      '<b>'+risco.length+'</b> em risco e <b>'+dorm.length+'</b> dormentes, com '+eur(valor)+\n      ' de valor histórico. Ver em <b>Clientes</b>.']);\n  var saz=clientesOportunidadesSazonais(M);\n  if(saz.length)\n    out.push(['op',saz.length+(saz.length===1?' cliente com época de compra a aproximar-se':\n      ' clientes com época de compra a aproximar-se'),\n      'Costumam comprar todos os anos por esta altura — bom momento para um contacto. Ver em '+\n      '<b>Clientes</b>.']);\n  return out;\n}\nfunction avisosFaturas(){\n  var out=[];\n  var vencidas=(M.FAT||[]).filter(function(x){\n    var v=parseDate(x.vencimento); return !x.pago&&v&&v<new Date();});\n  if(vencidas.length){\n    var tv=vencidas.reduce(function(s,x){return s+x.valor;},0);\n    out.push(['al',vencidas.length+' fatura'+(vencidas.length>1?'s':'')+' por pagar em atraso',\n      '<b>'+eur(tv)+'</b> já passaram do vencimento. Ver em <b>Fornecedores</b>.']);\n  }\n  return out;\n}\n\nPAGES['resumo']=function(M){\n  var temDiamante=M.DV&&M.DV.length>0;\n  var P=temDiamante?diamPresets(M).mes:null;\n  var atualMes=temDiamante?diamanteAgg(M,P.de,P.ate):null;\n  var pAnt=temDiamante?diamPeriodoAnterior(P.de,P.ate):null;\n  var antMes=temDiamante?diamanteAgg(M,pAnt.de,pAnt.ate):null;\n\n  var AT=(M.DC&&M.DC.length)?clientesAtividade(M):[];\n  var risco=AT.filter(function(x){return x.estado==='risco';});\n  var dorm=AT.filter(function(x){return x.estado==='dormente';});\n\n  var leadsAtivas=leadsGet().filter(leadAtiva);\n  var valorLeads=leadsAtivas.reduce(function(s,x){return s+euroNum(x.orcamento);},0);\n\n  var porPagar=(M.FAT||[]).filter(function(x){return !x.pago;});\n  var totalDivida=porPagar.reduce(function(s,x){return s+x.valor;},0);\n  var vencidasResumo=porPagar.filter(function(x){\n    var v=parseDate(x.vencimento); return v&&v<new Date();});\n\n  var h='<div class=\"kpis\">'+\n    kpi('Vendas este mês',temDiamante?eur(atualMes.totalGeral):'—',\n      temDiamante&&antMes.totalGeral?'que no mesmo período do ano passado':'sem histórico do Diamante',\n      temDiamante&&antMes.totalGeral?deltaTag(atualMes.totalGeral,antMes.totalGeral):'','pos')+\n    kpi('Leads em aberto',nf(leadsAtivas.length),valorLeads?eur(valorLeads)+' de potencial':'na pipeline',\n      '','info')+\n    kpi('Clientes a recuperar',nf(risco.length+dorm.length),'em risco + dormentes','',\n      (risco.length+dorm.length)?'warn':null)+\n    kpi('Faturas por pagar',eur(totalDivida),porPagar.length+(porPagar.length===1?' fatura':' faturas'),\n      '',vencidasResumo.length?'neg':(porPagar.length?'warn':null))+\n    '</div>';\n\n  /* ---- AVISOS: como vamos agora, em € reais ---- */\n  var avisos=(temDiamante?avisosVendasReais(M):[]).concat(avisosClientes(M))\n    .concat(avisosFaturas()).concat(avisosLeads()).concat(avisosMarketing());\n  h+='<h3 class=\"sec\">Avisos</h3>'+\n     '<p class=\"sec-d\">Vendas reais, clientes, faturas e leads — tudo o que precisa de atenção agora.</p>'+\n     '<div class=\"ins\">'+avisos.map(function(a){\n        return insight(a[0],a[1],a[2]);}).join('')+'</div>';\n\n  /* ---- SUGESTÕES: o que corrigir na loja física ---- */\n  var ins=[];\n  var fraco=M.segMat.slice().sort(function(a,b){return a.p-b.p;})[0];\n  if(fraco&&fraco.p<M.conv-10){\n    var perdF=M.V.filter(function(v){return !v.comprou&&v.material.indexOf(fraco.k)>=0&&\n      ['Não encontrou o que queria','Não temos o produto','Queria noutro material']\n        .indexOf(v.motivo)>=0;}).length;\n    ins.push(insight('al','O '+fraco.k.toLowerCase()+' é onde se perdem mais vendas',\n      '<b>'+fraco.n+' pessoas</b> procuraram '+esc(fraco.k.toLowerCase())+' e só <b>'+nf(fraco.p,0)+\n      '%</b> compraram, contra '+nf(M.conv,0)+'% na loja em geral.'+\n      (perdF?' Destas, '+perdF+' disseram que não encontraram o que queriam.':'')));\n  }\n  var forte=M.segMat.slice().sort(function(a,b){return b.p-a.p;})[0];\n  if(forte&&forte.p>=90&&forte.n>=10)\n    ins.push(insight('op','Quem procura '+forte.k.toLowerCase()+' compra quase sempre',\n      '<b>'+forte.n+' procuras</b> e <b>'+forte.c+' compras</b> ('+nf(forte.p,0)+'%). '+\n      'Pode valer a pena reforçar esta categoria.'));\n  if(pct(M.nContacto,M.nV)<30)\n    ins.push(insight('op','Falta recolher contactos',\n      'Só <b>'+nf(pct(M.nContacto,M.nV),0)+'%</b> das visitas deixam contacto. Sem contacto '+\n      'não há forma de voltar a chamar o cliente — é o que alimenta as <b>Leads</b>.'));\n  if(ins.length){\n    h+='<h3 class=\"sec\">Sugestões</h3>'+\n       '<p class=\"sec-d\">O que o registo de atendimento na loja mostra que vale a pena corrigir.</p>'+\n       '<div class=\"ins\">'+ins.slice(0,3).join('')+'</div>';\n  }\n\n  /* ---- gráfico principal: vendas reais mês a mês, este ano ---- */\n  if(temDiamante){\n    var hoje=new Date(),anoAtual=hoje.getFullYear();\n    var mesesAno=[]; for(var mi=0;mi<=hoje.getMonth();mi++) mesesAno.push(mi);\n    var porMesAno={};\n    M.DV.forEach(function(v){\n      if(v.data.getFullYear()===anoAtual) porMesAno[v.data.getMonth()]=(porMesAno[v.data.getMonth()]||0)+v.total;\n    });\n    h+='<div class=\"card\"><h3>Vendas mensais — '+anoAtual+'</h3>'+\n      '<p class=\"hint\">Receita real, a partir do histórico do Diamante</p>'+\n      '<div class=\"chartbox\"><canvas id=\"rv1\"></canvas></div></div>';\n    chart('rv1',{type:'bar',data:{labels:mesesAno.map(function(m){return MES[m];}),\n      datasets:[{data:mesesAno.map(function(m){return porMesAno[m]||0;}),\n        backgroundColor:INK,borderRadius:2}]},\n      options:Object.assign({},BASE,{plugins:{tooltip:{callbacks:{label:function(c){\n        return eur(c.parsed.y);}}}}})});\n  }\n\n  h+='<div class=\"card\"><h3>Visitas à loja e conversão por mês</h3>'+\n    '<p class=\"hint\">Registo de atendimento — quantas pessoas entraram e quantas compraram</p>'+\n    '<div class=\"chartbox\"><canvas id=\"c1\"></canvas></div></div>';\n  chart('c1',{type:'bar',data:{labels:M.mesesK.map(mlabel),datasets:[\n    {label:'Visitas',data:M.mesesK.map(function(k){return M.porMes[k].n;}),\n     backgroundColor:SAND,borderRadius:2},\n    {label:'Compraram',data:M.mesesK.map(function(k){return M.porMes[k].c;}),\n     backgroundColor:INK,borderRadius:2}]},\n    options:Object.assign({},BASE,{plugins:{legend:LEG}})});\n\n  return {t:'Resumo',d:'Vendas reais, leads, clientes e faturas — tudo o que é importante numa página.',html:h};\n};\n\n/* ---------------- VENDAS ---------------- */\nfunction presets(M){\n  var fim=M.datas[M.datas.length-1], ini=M.datas[0], hoje=new Date();\n  var inicioMes=new Date(hoje.getFullYear(),hoje.getMonth(),1);\n  return {\n    'mes':{nome:'Este mês',de:inicioMes,ate:hoje},\n    '30d':{nome:'Últimos 30 dias',de:new Date(hoje.getTime()-29*86400000),ate:hoje},\n    '90d':{nome:'Últimos 3 meses',de:new Date(hoje.getTime()-89*86400000),ate:hoje},\n    'ano':{nome:'Este ano',de:new Date(hoje.getFullYear(),0,1),ate:hoje},\n    'tudo':{nome:'Tudo',de:ini,ate:fim}\n  };\n}\nfunction filterBar(M){\n  var P=presets(M);\n  var h='<div class=\"filter\">';\n  Object.keys(P).forEach(function(k){\n    h+='<button data-preset=\"'+k+'\"'+(RANGE.nome===P[k].nome?' class=\"on\"':'')+'>'+\n       esc(P[k].nome)+'</button>';});\n  h+='<span class=\"sep\"></span><label>De</label><input type=\"date\" id=\"f-de\" value=\"'+\n     iso(RANGE.de)+'\"><label>a</label><input type=\"date\" id=\"f-ate\" value=\"'+iso(RANGE.ate)+'\">'+\n     '<button id=\"f-ok\" class=\"'+(RANGE.nome==='personalizado'?'on':'')+'\">Aplicar</button></div>';\n  return h;\n}\nfunction bindFilter(M){\n  var P=presets(M);\n  document.querySelectorAll('.filter [data-preset]').forEach(function(b){\n    b.addEventListener('click',function(){\n      var p=P[b.dataset.preset];\n      RANGE={de:p.de,ate:p.ate,nome:p.nome}; refresh();\n    });\n  });\n  var ok=document.getElementById('f-ok');\n  if(ok) ok.addEventListener('click',function(){\n    var de=parseDate(document.getElementById('f-de').value);\n    var ate=parseDate(document.getElementById('f-ate').value);\n    if(!de||!ate||de>ate) return;\n    RANGE={de:de,ate:ate,nome:'personalizado'}; refresh();\n  });\n}\n\n/* ---------- histórico real de vendas do Diamante (export manual) ---------- */\nfunction diamanteAgg(M,de,ate){\n  var d0=new Date(de.getFullYear(),de.getMonth(),de.getDate());\n  var d1=new Date(ate.getFullYear(),ate.getMonth(),ate.getDate(),23,59,59);\n  var linhas=(M.DV||[]).filter(function(v){return v.data>=d0&&v.data<=d1;});\n  var A={linhas:linhas,de:d0,ate:d1};\n\n  function agg(keyFn){\n    var m={};\n    linhas.forEach(function(v){\n      var k=keyFn(v); if(!k) return;\n      m[k]=m[k]||{k:k,qtd:0,total:0}; m[k].qtd+=(v.qtd||0); m[k].total+=v.total;\n    });\n    return Object.keys(m).map(function(k){return m[k];});\n  }\n  A.produtos=agg(function(v){return refSimplificada(v.sku)+' — '+tituloNome(v.desc);})\n    .sort(function(a,b){return b.total-a.total;});\n  A.categorias=agg(function(v){return v.categoria;}).sort(function(a,b){return b.total-a.total;});\n  A.materiais=agg(function(v){return v.material;}).sort(function(a,b){return b.total-a.total;});\n  A.fornecedores=agg(function(v){return v.fornecedor;}).sort(function(a,b){return b.total-a.total;});\n\n  A.totalGeral=linhas.reduce(function(s,v){return s+v.total;},0);\n  var docs={}; linhas.forEach(function(v){if(v.doc) docs[v.doc]=(docs[v.doc]||0)+v.total;});\n  A.nDocs=Object.keys(docs).length;\n  A.ticketMedio=A.nDocs?A.totalGeral/A.nDocs:0;\n  return A;\n}\nfunction diamLimites(M){\n  var datas=(M.DV||[]).map(function(v){return v.data;});\n  var hoje=new Date();\n  if(!datas.length) return {min:hoje,max:hoje};\n  return {min:new Date(Math.min.apply(null,datas)),max:hoje};\n}\nfunction diamPresets(M){\n  var L=diamLimites(M), hoje=new Date();\n  var inicioMes=new Date(hoje.getFullYear(),hoje.getMonth(),1);\n  var mesPassIni=new Date(hoje.getFullYear(),hoje.getMonth()-1,1);\n  var mesPassFim=new Date(hoje.getFullYear(),hoje.getMonth(),0);\n  var inicioAno=new Date(hoje.getFullYear(),0,1);\n  var anoPassIni=new Date(hoje.getFullYear()-1,0,1);\n  var anoPassFim=new Date(hoje.getFullYear()-1,11,31);\n  return {\n    mes:{nome:'Este mês',de:inicioMes,ate:hoje},\n    mesPassado:{nome:'Mês passado',de:mesPassIni,ate:mesPassFim},\n    ano:{nome:'Este ano',de:inicioAno,ate:hoje},\n    anoPassado:{nome:'Ano passado',de:anoPassIni,ate:anoPassFim},\n    tudo:{nome:'Tudo',de:L.min,ate:L.max}\n  };\n}\nfunction diamPeriodoAnterior(de,ate){\n  return {de:new Date(de.getFullYear()-1,de.getMonth(),de.getDate()),\n          ate:new Date(ate.getFullYear()-1,ate.getMonth(),ate.getDate())};\n}\nfunction diamFilterBar(M){\n  var P=diamPresets(M), R=UI.diamRange;\n  var h='<div class=\"filter\">';\n  Object.keys(P).forEach(function(k){\n    h+='<button data-diampreset=\"'+k+'\"'+(R.nome===P[k].nome?' class=\"on\"':'')+'>'+\n       esc(P[k].nome)+'</button>';});\n  h+='<span class=\"sep\"></span><label>De</label><input type=\"date\" id=\"d-de\" value=\"'+\n     iso(R.de)+'\"><label>a</label><input type=\"date\" id=\"d-ate\" value=\"'+iso(R.ate)+'\">'+\n     '<button id=\"d-ok\" class=\"'+(R.nome==='personalizado'?'on':'')+'\">Aplicar</button></div>';\n  return h;\n}\nfunction bindDiamFiltro(M){\n  var P=diamPresets(M);\n  document.querySelectorAll('[data-diampreset]').forEach(function(b){\n    b.addEventListener('click',function(){\n      var p=P[b.dataset.diampreset];\n      UI.diamRange={de:p.de,ate:p.ate,nome:p.nome}; refresh();\n    });\n  });\n  var ok=document.getElementById('d-ok');\n  if(ok) ok.addEventListener('click',function(){\n    var de=parseDate(document.getElementById('d-de').value);\n    var ate=parseDate(document.getElementById('d-ate').value);\n    if(!de||!ate||de>ate) return;\n    UI.diamRange={de:de,ate:ate,nome:'personalizado'}; refresh();\n  });\n}\n\nPAGES['vendas']=function(M){\n  if(!UI.diamRange) UI.diamRange=diamPresets(M).mes;\n  var h='';\n\n  if(!M.DV||!M.DV.length){\n    h+=pendingBox('Ainda sem histórico real de vendas',\n      'Esta página mostra receita, produtos, categorias, materiais e fornecedores a partir de um '+\n      'export do Diamante. Assim que houver um export (ficheiro de vendas), envia-o para ser '+\n      'carregado aqui.');\n    return {t:'Vendas',d:'Vendas reais por período.',html:h};\n  }\n\n  /* YTD sempre visível, independente do filtro escolhido abaixo — para comparar de relance */\n  var Pano=diamPresets(M).ano;\n  var ytd=diamanteAgg(M,Pano.de,Pano.ate);\n  var antYtdP=diamPeriodoAnterior(Pano.de,Pano.ate);\n  var ytdAnt=diamanteAgg(M,antYtdP.de,antYtdP.ate);\n  var temYtdAnt=ytdAnt.linhas.length>0;\n  h+='<div class=\"card\" style=\"margin-bottom:14px\">'+\n    '<h3>Vendas YTD (ano até à data)</h3>'+\n    '<p class=\"hint\">Sempre visível, independente do filtro abaixo — para comparar de relance</p>'+\n    '<div style=\"display:flex;align-items:baseline;gap:14px;flex-wrap:wrap\">'+\n    '<div style=\"font-family:var(--display);font-size:26px;font-weight:600\">'+eur(ytd.totalGeral)+'</div>'+\n    (temYtdAnt?deltaTag(ytd.totalGeral,ytdAnt.totalGeral):'')+\n    '<span class=\"hint\" style=\"margin:0\">'+(temYtdAnt?\n      'contra '+eur(ytdAnt.totalGeral)+' no mesmo período de '+(Pano.de.getFullYear()-1):\n      'ainda sem o mesmo período do ano passado para comparar')+'</span>'+\n    '</div></div>';\n\n  h+=diamFilterBar(M);\n  var R=UI.diamRange;\n  var D=diamanteAgg(M,R.de,R.ate);\n  var ant=diamanteAgg(M,diamPeriodoAnterior(R.de,R.ate).de,diamPeriodoAnterior(R.de,R.ate).ate);\n  var temAnterior=ant.linhas.length>0;\n\n  h+='<div class=\"kpis\">'+\n    kpi('Vendas (€)',eur(D.totalGeral),\n      temAnterior?'contra '+eur(ant.totalGeral)+' no mesmo período do ano passado':R.nome,\n      temAnterior?deltaTag(D.totalGeral,ant.totalGeral):'',\n      temAnterior?(D.totalGeral>=ant.totalGeral?'pos':'neg'):null)+\n    kpi('Peças vendidas',nf(D.linhas.reduce(function(s,v){return s+(v.qtd||0);},0)),'unidades')+\n    kpi('Nº de vendas',nf(D.nDocs),'documentos distintos')+\n    kpi('Ticket médio',eur(D.ticketMedio),'por venda')+\n    '</div>';\n\n  if(!temAnterior)\n    h+='<div class=\"pending\" style=\"margin-bottom:14px\"><h4>Comparação com o ano anterior</h4>'+\n      '<p>Ainda não há vendas registadas no mesmo período do ano passado dentro deste histórico '+\n      '(início em '+esc(diamLimites(M).min.toLocaleDateString('pt-PT'))+').</p></div>';\n\n  h+='<div class=\"g2\">'+\n    '<div class=\"card\"><h3>Produtos mais vendidos</h3><p class=\"hint\">Por receita, top 10, no período</p>'+\n      (D.produtos.length?'<table><thead><tr><th>Produto</th><th class=\"num\">Qt.</th>'+\n        '<th class=\"num\">Receita</th></tr></thead><tbody>'+D.produtos.slice(0,10).map(function(p){\n          return '<tr><td>'+esc(p.k)+'</td><td class=\"num\">'+nf(p.qtd)+'</td><td class=\"num\">'+\n            eur(p.total)+'</td></tr>';}).join('')+'</tbody></table>'\n        :'<div class=\"empty\">Sem vendas neste período.</div>')+'</div>'+\n    '<div class=\"card\"><h3>Categorias mais vendidas</h3><p class=\"hint\">Por receita, no período</p>'+\n      barList(D.categorias.slice(0,10).map(function(c){return{k:c.k,v:c.total};}),\n        function(i){return eur(i.v);})+\n      '<p class=\"hint\" style=\"margin-top:16px\">Por quantidade (peças)</p>'+\n      barList(D.categorias.slice().sort(function(a,b){return b.qtd-a.qtd;}).slice(0,10)\n        .map(function(c){return{k:c.k,v:c.qtd};}),\n        function(i){return nf(i.v)+' un.';})+'</div></div>';\n\n  h+='<div class=\"g2\">'+\n    '<div class=\"card\"><h3>Materiais mais vendidos</h3><p class=\"hint\">Por receita, no período</p>'+\n      barList(D.materiais.map(function(m){return{k:m.k,v:m.total};}),\n        function(i){return eur(i.v);})+\n      '<p class=\"hint\" style=\"margin-top:16px\">Por quantidade (peças)</p>'+\n      barList(D.materiais.slice().sort(function(a,b){return b.qtd-a.qtd;})\n        .map(function(m){return{k:m.k,v:m.qtd};}),\n        function(i){return nf(i.v)+' un.';})+'</div>'+\n    '<div class=\"card\"><h3>Ranking de fornecedores</h3><p class=\"hint\">Por receita, no período</p>'+\n      (D.fornecedores.length?'<table><thead><tr><th>Fornecedor</th><th class=\"num\">Qt.</th>'+\n        '<th class=\"num\">Receita</th></tr></thead><tbody>'+\n        D.fornecedores.slice(0,10).map(function(f){\n          return '<tr><td>'+esc(f.k)+'</td><td class=\"num\">'+nf(f.qtd)+'</td><td class=\"num\">'+\n            eur(f.total)+'</td></tr>';}).join('')+'</tbody></table>'\n        :'<div class=\"empty\">Sem fornecedor associado às peças vendidas neste período.</div>')+\n      '</div></div>';\n\n  h+=pendingBox('O que ainda falta, e depende do Diamante responder',\n    'Estes números vêm todos de um export manual do Diamante ('+\n    esc(diamLimites(M).min.toLocaleDateString('pt-PT'))+' até hoje). O que continua por fazer:'+\n    '<ul><li><b>Margem real por categoria/fornecedor</b> — falta o preço de custo de cada peça, '+\n    'que não vem neste export.</li>'+\n    '<li><b>Peças paradas</b> — precisa do stock atual por peça.</li>'+\n    '<li><b>Atualização automática</b> — enquanto o Diamante não confirma o acesso via SQL, '+\n    'isto só atualiza quando pedires um novo export.</li></ul>');\n\n  return {t:'Vendas',d:'Vendas reais por período, a partir do histórico do Diamante.',html:h,\n          after:function(){bindDiamFiltro(M);}};\n};\n\n/* ---------------- LOJA ---------------- */\nPAGES['loja']=function(M){\n  var S=stats(M,RANGE.de,RANGE.ate);\n  var h=filterBar(M);\n  h+='<div class=\"kpis\">'+\n    kpi('Visitas',nf(S.nV),'no período')+\n    kpi('Compraram',nf(S.conv,0)+'%',S.nC+' visitas','',S.nC?'pos':null)+\n    kpi('Saíram sem comprar',nf(S.nV-S.nC),nf(100-S.conv,0)+'% do total','',\n      (S.nV-S.nC)?'warn':null)+\n    kpi('Deixaram contacto',nf(S.nContacto),nf(pct(S.nContacto,S.nV),0)+'% das visitas','','info')+\n    '</div>';\n\n  h+='<div class=\"g2\">'+\n    '<div class=\"card\"><h3>Porque não compraram</h3>'+\n      '<p class=\"hint\">'+(S.nV-S.nC)+' visitas sem compra</p>'+\n      barList(S.motivos.slice(0,6))+'</div>'+\n    '<div class=\"card\"><h3>Quem compra mais</h3><p class=\"hint\">Por tipo de cliente</p>'+\n      convTable(S.segTipo,'Cliente',S.conv)+'</div></div>';\n\n  h+='<div class=\"g2\">'+\n    '<div class=\"card\"><h3>O que procuram</h3><p class=\"hint\">Categorias mais pedidas</p>'+\n      barList(S.segInt.slice(0,7).map(function(x){return{k:x.k,v:x.n};}))+'</div>'+\n    '<div class=\"card\"><h3>Conversão por material</h3>'+\n      '<p class=\"hint\">Onde o interesse não se transforma em venda</p>'+\n      convTable(S.segMat.slice().sort(function(a,b){return b.p-a.p;}),'Material',S.conv)+\n      '</div></div>';\n\n  var V=S.V,nV=S.nV;\n  var f=function(i){return nf(pct(i.v,nV),0)+'%';};\n  h+='<div class=\"g2\">'+\n    '<div class=\"card\"><h3>De onde vêm</h3><p class=\"hint\">País indicado no formulário</p>'+\n      barList(tally(V.map(function(v){return v.pais;}))\n        .filter(function(p){return p.k!=='Não indicado';}).slice(0,8),f)+'</div>'+\n    '<div class=\"card\"><h3>Idade</h3><p class=\"hint\">Escalão indicado no formulário</p>'+\n      barList(tally(V.map(function(v){return v.idade;}))\n        .filter(function(i){return i.k!=='Não indicado';}),f)+'</div></div>';\n\n  h+='<div class=\"card\"><h3>Que mercados compram mais</h3>'+\n    '<p class=\"hint\">Países com pelo menos 4 visitas registadas no período</p>'+\n    convTable(S.segPais,'País',S.conv)+'</div>';\n\n  h+=pendingBox('Perfis, não identidades',\n    'Este registo vem do <b>formulário de atendimento</b> preenchido na loja — quem entrou, de '+\n    'onde veio, o que procurava. São <b>perfis</b>, não se sabe se a mesma pessoa já tinha vindo. '+\n    'Quem interessa de facto — histórico de compras, valor, recorrência — está na página '+\n    '<b>Clientes</b>, a partir dos dados reais do Diamante.');\n\n  return {t:'Registo de atendimento',\n    d:'Quem entra, o que procura, e porque compra ou não.',html:h,\n          after:function(){bindFilter(M);}};\n};\n\n/* ---------- nomes do Diamante vêm em maiúsculas — passar a Nome Próprio ---------- */\nvar NOME_MINUSC=['de','da','do','das','dos','e'];\nfunction tituloNome(s){\n  if(!s) return s;\n  return String(s).trim().toLowerCase().split(/\\s+/).map(function(w,i){\n    if(i>0&&NOME_MINUSC.indexOf(w)>=0) return w;\n    return w.charAt(0).toUpperCase()+w.slice(1);\n  }).join(' ');\n}\n/* referências do Diamante trazem zeros de preenchimento no meio: 'B000009957' fica 'B9957' */\nfunction refSimplificada(sku){\n  if(!sku) return sku;\n  var m=String(sku).match(/^([^\\d]*)(\\d+)(.*)$/);\n  if(!m) return sku;\n  var semZeros=m[2].replace(/^0+(?=\\d)/,'');\n  return m[1]+semZeros+m[3];\n}\n\n/* ---------- atividade de clientes reais do Diamante (ativo/risco/dormente) ---------- */\nfunction clientesAtividade(M){\n  var porCliente={};\n  (M.DV||[]).forEach(function(v){\n    if(!v.cliente) return;\n    porCliente[v.cliente]=porCliente[v.cliente]||{total:0,ultima:null,datas:{}};\n    porCliente[v.cliente].total+=v.total;\n    if(!porCliente[v.cliente].ultima||v.data>porCliente[v.cliente].ultima)\n      porCliente[v.cliente].ultima=v.data;\n    porCliente[v.cliente].datas[iso(v.data)]=true;\n  });\n  return (M.DC||[]).map(function(c){\n    var hist=porCliente[c.codigo]||{total:0,ultima:null,datas:{}};\n    var nDatasCompra=Object.keys(hist.datas).length;\n    var semRecente=(c.c0+c.c1)===0, antigo=c.c3+c.c4+c.c5;\n    var estado = (c.c0>0||c.c1>0) ? 'ativo'\n      : (semRecente&&c.c2>0) ? 'risco'\n      : (semRecente&&c.c2===0&&antigo>0) ? 'dormente'\n      : 'sem_historico';\n    return {codigo:c.codigo,nome:tituloNome(c.nome),tel:c.tel,email:c.email,localidade:c.localidade,\n      total:hist.total,ultima:hist.ultima,estado:estado,\n      /* nº de datas distintas em que comprou, a partir do histórico detalhado do Diamante (2021+) —\n         base para distinguir clientes de compra única de clientes recorrentes */\n      nDatasCompra:nDatasCompra,recorrente:nDatasCompra>=2,\n      c0:c.c0,c1:c.c1,c2:c.c2,c3:c.c3,c4:c.c4,c5:c.c5,\n      comprasTotal:c.c0+c.c1+c.c2+c.c3+c.c4+c.c5};\n  });\n}\n\n/* ---------- padrão sazonal de compra por cliente (cross-selling por época) ---------- */\nfunction clientePadraoSazonal(compras){\n  if(!compras||compras.length<2) return null;\n  var porAnoMes={};\n  compras.forEach(function(v){\n    var ano=v.data.getFullYear(), mes=v.data.getMonth();\n    porAnoMes[ano]=porAnoMes[ano]||{};\n    porAnoMes[ano][mes]=true;\n  });\n  var anos=Object.keys(porAnoMes);\n  if(anos.length<2) return null; // precisa de pelo menos 2 anos distintos com compra\n  var contagemMes={};\n  anos.forEach(function(a){\n    Object.keys(porAnoMes[a]).forEach(function(m){ contagemMes[m]=(contagemMes[m]||0)+1; });\n  });\n  var melhorMes=null,melhorN=0;\n  Object.keys(contagemMes).forEach(function(m){\n    if(contagemMes[m]>melhorN){ melhorN=contagemMes[m]; melhorMes=+m; }\n  });\n  if(melhorN<2) return null; // só se repetiu num mês em pelo menos 2 anos diferentes\n  return {mes:melhorMes,anosComPadrao:melhorN,anosComCompra:anos.length};\n}\nfunction clienteSazonalStatus(padrao,compras){\n  var hoje=new Date(); hoje.setHours(0,0,0,0);\n  var anoAtual=hoje.getFullYear();\n  var comprouEsteAno=compras.some(function(v){\n    return v.data.getFullYear()===anoAtual&&v.data.getMonth()===padrao.mes;});\n  var comprouAnoPassado=compras.some(function(v){\n    return v.data.getFullYear()===anoAtual-1&&v.data.getMonth()===padrao.mes;});\n  var alvo=new Date(anoAtual,padrao.mes,1);\n  var dias=Math.round((alvo-hoje)/86400000);\n  if(dias<-31){ alvo=new Date(anoAtual+1,padrao.mes,1); dias=Math.round((alvo-hoje)/86400000); }\n  return {comprouEsteAno:comprouEsteAno,falhouAnoPassado:!comprouAnoPassado,dias:dias,alvo:alvo};\n}\nfunction clientesOportunidadesSazonais(M){\n  var porCliente={};\n  (M.DV||[]).forEach(function(v){\n    if(!v.cliente) return; (porCliente[v.cliente]=porCliente[v.cliente]||[]).push(v);\n  });\n  var AT=clientesAtividade(M), porCodigo={};\n  AT.forEach(function(x){ porCodigo[x.codigo]=x; });\n  var out=[];\n  Object.keys(porCliente).forEach(function(cod){\n    var compras=porCliente[cod];\n    var padrao=clientePadraoSazonal(compras);\n    if(!padrao) return;\n    var status=clienteSazonalStatus(padrao,compras);\n    if(status.comprouEsteAno) return; // já comprou este ano na época certa, sem aviso a dar\n    if(status.dias>60&&!status.falhouAnoPassado) return; // muito longe e não falhou o ciclo anterior\n    var cli=porCodigo[cod]; if(!cli) return;\n    out.push({codigo:cod,nome:cli.nome,tel:cli.tel,email:cli.email,total:cli.total,\n      estado:cli.estado,padrao:padrao,status:status});\n  });\n  return out.sort(function(a,b){return a.status.dias-b.status.dias;});\n}\n\n/* ---------------- CLIENTES ---------------- */\nvar CLIENTES_FILTROS=[\n  ['todos','Todos'],\n  ['1','Sem compras há 1+ ano'],\n  ['2','Sem compras há 2+ anos'],\n  ['3','Sem compras há 3+ anos']\n];\nfunction clientesFiltrar(AT,filtro){\n  if(filtro==='todos') return AT.filter(function(x){return x.comprasTotal>0;});\n  var n=+filtro; // '1' = sem compras há 1+ ano (este ano + ano passado), '2' = +ano-2, '3' = +ano-3\n  return AT.filter(function(x){\n    if(!x.comprasTotal) return false;\n    var semRecente=[x.c0,x.c1,x.c2,x.c3].slice(0,n+1)\n      .reduce(function(s,v){return s+(v||0);},0)===0;\n    return semRecente;\n  });\n}\n/* distinção pedida pelo Diogo: compraram uma só vez vs. voltaram a comprar (2+ datas distintas),\n   a partir do histórico detalhado do Diamante (2021+) */\nvar CLIENTES_REC_FILTROS=[\n  ['todos','Todos'],\n  ['1','Compraram 1 vez'],\n  ['2+','Recorrentes (2+ vezes)']\n];\nfunction clientesFiltrarRec(AT,filtro){\n  if(filtro==='1') return AT.filter(function(x){return x.nDatasCompra===1;});\n  if(filtro==='2+') return AT.filter(function(x){return x.nDatasCompra>=2;});\n  return AT;\n}\nfunction clienteCompras(M,codigo){\n  return (M.DV||[]).filter(function(v){return v.cliente===codigo;})\n    .sort(function(a,b){return b.data-a.data;});\n}\nfunction clienteResumoGostos(compras){\n  var cat=tally(compras.map(function(v){return v.categoria;}))[0];\n  var mat=tally(compras.map(function(v){return v.material;}))[0];\n  return {categoria:cat?cat.k:null,material:mat?mat.k:null};\n}\n\nPAGES['clientes']=function(M){\n  var h='';\n\n  if(!M.DC||!M.DC.length){\n    h+=pendingBox('De perfis para clientes com nome',\n      'Ainda não há um export de clientes do Diamante carregado. Assim que houver, esta página '+\n      'passa a mostrar quem compra, quem parou de comprar, e o valor histórico de cada cliente. '+\n      'Enquanto isso não existe, a página <b>Registo de Atendimento</b> mostra os perfis do '+\n      'formulário da loja, e as <b>Leads</b> ajudam a captar contactos.');\n    return {t:'Clientes',d:'Clientes reais do Diamante — quem compra, quem parou, e quanto valem.',html:h};\n  }\n\n  if(!UI.clientesFiltro) UI.clientesFiltro='1';\n  if(!UI.clientesRecFiltro) UI.clientesRecFiltro='todos';\n  var AT=clientesAtividade(M);\n  var ativos=AT.filter(function(x){return x.estado==='ativo';});\n  var risco=AT.filter(function(x){return x.estado==='risco';});\n  var dorm=AT.filter(function(x){return x.estado==='dormente';});\n  /* só entre quem tem histórico detalhado (2021+) faz sentido falar de 1 vez vs. recorrente */\n  var comHist=AT.filter(function(x){return x.nDatasCompra>0;});\n  var umaVez=comHist.filter(function(x){return x.nDatasCompra===1;});\n  var recorrentes=comHist.filter(function(x){return x.nDatasCompra>=2;});\n\n  h+='<p class=\"sec-d\">A partir da ficha de clientes exportada do Diamante — compras nos '+\n    'últimos 6 anos, por cliente.</p>';\n\n  h+='<div class=\"kpis\">'+\n    kpi('Clientes ativos',nf(ativos.length),'compraram este ano ou o passado','','pos')+\n    kpi('Em risco',nf(risco.length),'não compram há 2 anos','',risco.length?'warn':null)+\n    kpi('Dormentes',nf(dorm.length),'não compram há 3+ anos, mas já compraram','',\n      dorm.length?'neg':null)+\n    kpi('Valor a recuperar',eur(risco.concat(dorm).reduce(function(s,x){return s+x.total;},0)),\n      'histórico dos em risco + dormentes','',\n      (risco.length+dorm.length)?'warn':null)+\n    '</div>';\n\n  /* ---- distinção pedida: compraram uma vez vs. recorrentes (2+ datas distintas) ---- */\n  if(comHist.length){\n    h+='<div class=\"card\"><h3>Compra única vs. recorrente</h3>'+\n      '<p class=\"hint\">Entre os '+nf(comHist.length)+' clientes com histórico detalhado de venda '+\n      '(2021 em diante): quantos compraram numa única data e quantos voltaram em datas diferentes.</p>'+\n      barList([\n        {k:'Compraram 1 vez',v:umaVez.length},\n        {k:'Recorrentes (2+ datas distintas)',v:recorrentes.length}\n      ],function(i){return nf(i.v)+' · '+nf(pct(i.v,comHist.length),0)+'%';})+\n      '</div>';\n  }\n\n  /* ---- histórico de compras do cliente selecionado, já no topo ---- */\n  if(UI.clienteSelecionado){\n    var sel=AT.find(function(x){return x.codigo===UI.clienteSelecionado;});\n    var compras=clienteCompras(M,UI.clienteSelecionado);\n    if(sel){\n      var gosto=clienteResumoGostos(compras);\n      h+='<div class=\"card\"><h3>Histórico de compras — '+\n        esc(sel.nome||sel.codigo)+'</h3>'+\n        (compras.length?'<p class=\"hint\">'+compras.length+\n          (compras.length===1?' compra registada':' compras registadas')+\n          (gosto.categoria?'. Costuma comprar mais <b>'+esc(gosto.categoria.toLowerCase())+'</b>'+\n            (gosto.material?', em <b>'+esc(gosto.material.toLowerCase())+'</b>':''):'')+'.</p>'\n          :'<p class=\"hint\">Sem linhas de venda detalhadas para este cliente no histórico '+\n          'carregado (pode ter comprado antes de 2021).</p>')+\n        (compras.length?'<table><thead><tr><th>Data</th><th>Produto</th><th>Categoria</th>'+\n          '<th>Material</th><th class=\"num\">Valor</th></tr></thead><tbody>'+\n          compras.map(function(v){\n            return '<tr><td style=\"white-space:nowrap\">'+v.data.toLocaleDateString('pt-PT')+\n              '</td><td>'+esc(refSimplificada(v.sku)+' — '+tituloNome(v.desc))+'</td><td>'+esc(v.categoria)+'</td><td>'+esc(v.material)+\n              '</td><td class=\"num\">'+eur(v.total)+'</td></tr>';\n          }).join('')+'</tbody></table>':'')+\n        '<div class=\"actions\"><button class=\"btn ghost sm\" id=\"c-fechar\">Fechar</button></div></div>';\n    }\n  }\n\n  /* ---- padrão sazonal: quem costuma voltar nesta altura e pode estar a falhar ---- */\n  var saz=clientesOportunidadesSazonais(M);\n  if(saz.length){\n    h+='<div class=\"card\"><h3>Época habitual de compra a aproximar-se</h3>'+\n      '<p class=\"hint\">Clientes com um padrão claro (compraram no mesmo mês em pelo menos 2 anos '+\n      'diferentes) que ainda não compraram este ano nessa altura — bom momento para um contacto.</p>'+\n      '<table><thead><tr><th>Cliente</th><th>Contacto</th><th>Mês habitual</th>'+\n      '<th>Padrão</th><th>Faltou o ano passado?</th><th></th></tr></thead><tbody>'+\n      saz.slice(0,20).map(function(x){\n        var dias=x.status.dias;\n        var quando = dias<0 ? 'já passou este ano ('+Math.abs(dias)+'d)'\n          : dias===0 ? 'é este mês'\n          : 'daqui a '+dias+'d';\n        return '<tr class=\"clk\" data-cliente=\"'+esc(x.codigo)+'\">'+\n          '<td>'+esc(x.nome||x.codigo)+'</td>'+\n          '<td>'+esc([x.tel,x.email].filter(Boolean).join(' · ')||'—')+'</td>'+\n          '<td>'+esc(MESES_LONGOS[x.padrao.mes])+' — '+quando+'</td>'+\n          '<td>'+x.padrao.anosComPadrao+' de '+x.padrao.anosComCompra+' anos</td>'+\n          '<td>'+(x.status.falhouAnoPassado?'<span class=\"kbadge al\">sim</span>'\n            :'<span class=\"kbadge av\">não</span>')+'</td>'+\n          '<td class=\"num\">'+(x.total?eur(x.total):'—')+'</td></tr>';\n      }).join('')+'</tbody></table>'+\n      (saz.length>20?'<p class=\"hint\" style=\"margin-top:8px\">A mostrar os 20 mais próximos, '+\n        'de '+saz.length+' no total.</p>':'')+'</div>';\n  }\n\n  var lista=clientesFiltrarRec(clientesFiltrar(AT,UI.clientesFiltro),UI.clientesRecFiltro)\n    .sort(function(a,b){return b.total-a.total;});\n\n  h+='<div class=\"filter\" style=\"margin-bottom:8px\">'+CLIENTES_FILTROS.map(function(f){\n    return '<button data-cfiltro=\"'+f[0]+'\"'+(UI.clientesFiltro===f[0]?' class=\"on\"':'')+'>'+\n      esc(f[1])+'</button>';\n  }).join('')+'</div>'+\n  '<div class=\"filter\" style=\"margin-bottom:14px\">'+CLIENTES_REC_FILTROS.map(function(f){\n    return '<button data-crecfiltro=\"'+f[0]+'\"'+(UI.clientesRecFiltro===f[0]?' class=\"on\"':'')+'>'+\n      esc(f[1])+'</button>';\n  }).join('')+'</div>';\n\n  if(lista.length){\n    h+='<div class=\"card\"><h3>Clientes</h3>'+\n      '<p class=\"hint\">Ordenados pelo valor histórico. Duplo-clique num cliente para veres o que '+\n      'já comprou.</p>'+\n      '<table><thead><tr><th>Cliente</th><th>Telemóvel</th><th>Email</th><th>Estado</th>'+\n      '<th>Recorrência</th>'+\n      '<th class=\"num\">Nº de compras (6 anos)</th>'+\n      '<th class=\"num\">Valor histórico</th>'+\n      '<th>Data da última compra</th></tr></thead><tbody>'+\n      lista.slice(0,40).map(function(x){\n        var recTag = x.nDatasCompra>=2 ? '<span class=\"kbadge ok\">Recorrente</span>'\n          : x.nDatasCompra===1 ? '<span class=\"kbadge in\">1 vez</span>'\n          : '—';\n        return '<tr class=\"clk\" data-cliente=\"'+esc(x.codigo)+'\">'+\n          '<td>'+esc(x.nome||x.codigo)+'</td>'+\n          '<td style=\"white-space:nowrap\">'+esc(x.tel||'—')+'</td>'+\n          '<td>'+esc(x.email||'—')+'</td>'+\n          '<td><span class=\"kbadge '+(x.estado==='dormente'?'al':x.estado==='risco'?'av':'ok')+'\">'+\n            (x.estado==='dormente'?'Dormente':x.estado==='risco'?'Em risco':'Ativo')+'</span></td>'+\n          '<td>'+recTag+'</td>'+\n          '<td class=\"num\">'+nf(x.comprasTotal)+'</td>'+\n          '<td class=\"num\">'+(x.total?eur(x.total):'—')+'</td>'+\n          '<td style=\"white-space:nowrap\">'+\n            (x.ultima?x.ultima.toLocaleDateString('pt-PT'):'—')+'</td></tr>';\n      }).join('')+'</tbody></table>'+\n      (lista.length>40?'<p class=\"hint\" style=\"margin-top:8px\">A mostrar os 40 de maior valor, '+\n        'de '+lista.length+' no total.</p>':'')+'</div>';\n  } else {\n    h+='<div class=\"card\"><div class=\"empty\">Sem clientes neste filtro.</div></div>';\n  }\n\n  h+=pendingBox('O que ainda falta',\n    'A deteção de padrão sazonal usa só o <b>mês</b> em que cada cliente costuma comprar. O que '+\n    'falta é ligar isto a um <b>envio automático</b> de SMS/email de aviso antes da época — hoje '+\n    'tens de contactar à mão a partir desta lista. E dá para refinar: cruzar também com a '+\n    '<b>categoria</b> que cada cliente costuma levar nessa altura (ex: sempre um presente na '+\n    'mesma altura do ano).');\n\n  return {t:'Clientes',d:'Clientes reais do Diamante — quem compra, quem parou, e quanto valem.',\n          html:h,after:bindClientes};\n};\nfunction bindClientes(){\n  document.querySelectorAll('[data-cfiltro]').forEach(function(b){\n    b.addEventListener('click',function(){ UI.clientesFiltro=b.dataset.cfiltro; refresh(); });\n  });\n  document.querySelectorAll('[data-crecfiltro]').forEach(function(b){\n    b.addEventListener('click',function(){ UI.clientesRecFiltro=b.dataset.crecfiltro; refresh(); });\n  });\n  document.querySelectorAll('tr[data-cliente]').forEach(function(tr){\n    tr.addEventListener('dblclick',function(){\n      UI.clienteSelecionado = UI.clienteSelecionado===tr.dataset.cliente?null:tr.dataset.cliente;\n      refresh();\n    });\n  });\n  var fechar=document.getElementById('c-fechar');\n  if(fechar) fechar.addEventListener('click',function(){ UI.clienteSelecionado=null; refresh(); });\n}\n\n/* ---------------- PIPELINE DE LEADS ---------------- */\nvar LEAD_COLS=['data','origem','nome','contacto','produto','material','orcamento','prazo',\n               'notas','estado'];\nvar LEAD_TIT=['Data','Origem','Nome','Contacto','Procura','Material','Orçamento','Prazo',\n              'Notas','Estado'];\n\nfunction leadBadges(x){\n  var b='', dias=leadDiasParada(x);\n  if(leadAtiva(x)){\n    if(dias>=LEAD_URGENTE_DIAS) b+='<span class=\"kbadge al\">parada há '+dias+'d</span>';\n    else if(dias>=LEAD_AVISO_DIAS) b+='<span class=\"kbadge av\">'+dias+'d sem avanço</span>';\n    if(leadVencida(x)) b+='<span class=\"kbadge al\">orçamento vencido</span>';\n    else if(x.validadeAte&&diasEntre(todayISO(),x.validadeAte)<=3)\n      b+='<span class=\"kbadge av\">vence em '+diasEntre(todayISO(),x.validadeAte)+'d</span>';\n  }\n  return b;\n}\nfunction leadCard(x){\n  var i=ESTADOS.indexOf(x.estado);\n  var prev=i>0&&i<4?ESTADOS[i-1]:null, next=i>=0&&i<4?ESTADOS[i+1]:null;\n  var tom=LEAD_ESTADO_COR[x.estado]||'neu';\n  return '<div class=\"kcard tone-'+tom+'\">'+\n    '<div class=\"kc-top\"><span class=\"tagO '+esc(x.origem||'Loja')+'\">'+esc(x.origem||'Loja')+'</span>'+\n    '<button class=\"kx\" data-del=\"'+esc(x.id)+'\" title=\"apagar\">×</button></div>'+\n    '<div class=\"kc-nome\">'+esc(x.nome||'Sem nome')+'</div>'+\n    '<div class=\"kc-prod\">'+esc(x.produto||'—')+(x.material?' · '+esc(x.material):'')+'</div>'+\n    (euroNum(x.orcamento)?'<div class=\"kc-val\">'+eur(euroNum(x.orcamento))+'</div>':'')+\n    (x.contacto?'<div class=\"kc-contacto\">'+esc(x.contacto)+'</div>':'')+\n    '<div class=\"kc-badges\">'+leadBadges(x)+'</div>'+\n    '<div class=\"kc-acoes\">'+\n      (prev?'<button class=\"btn ghost sm\" data-mv=\"'+esc(x.id)+'\" data-to=\"'+prev+'\">‹</button>':'<span></span>')+\n      (next?'<button class=\"btn ghost sm\" data-mv=\"'+esc(x.id)+'\" data-to=\"'+next+'\">'+\n        (next==='ganha'?'✓ Ganha':(esc(ESTADO_NOME[next])+' ›'))+'</button>':'')+\n      (leadAtiva(x)?'<button class=\"btn ghost sm\" data-mv=\"'+esc(x.id)+'\" data-to=\"perdida\">✕</button>':'')+\n    '</div></div>';\n}\nPAGES['leads']=function(M){\n  var Lraw=leadsGet();\n  var _lx=separarLixo(Lraw);\n  if(_lx.vivos.length+_lx.lixo.length!==Lraw.length) leadsSet(_lx.vivos.concat(_lx.lixo));\n  var L=_lx.vivos, lixoLeads=_lx.lixo;\n  var porEstado={}; ESTADOS.forEach(function(e){porEstado[e]=0;});\n  L.forEach(function(x){ if(porEstado[x.estado]!=null) porEstado[x.estado]++; });\n  var ativasL=L.filter(leadAtiva);\n  var valor=ativasL.reduce(function(s,x){return s+euroNum(x.orcamento);},0);\n  var paradas=ativasL.filter(function(x){return leadDiasParada(x)>=LEAD_AVISO_DIAS;});\n\n  var h='<div class=\"kpis\">'+\n    kpi('Em aberto',nf(ativasL.length),'leads por fechar','','info')+\n    kpi('Valor potencial',eur(valor),'soma dos orçamentos em aberto')+\n    kpi('Paradas',nf(paradas.length),'sem avanço há '+LEAD_AVISO_DIAS+'+ dias','',\n      paradas.length?'warn':null)+\n    kpi('Ganhas',nf(porEstado.ganha),'converteram em venda','',porEstado.ganha?'pos':null)+\n    '</div>';\n\n  h+='<div class=\"addbar\"><h3>Pipeline</h3><div>'+\n     '<button class=\"btn ghost plus\" id=\"o-toggle\"><span>'+(UI.formOrc?'×':'+')+'</span>'+\n     (UI.formOrc?'Fechar orçamento':'Gerar orçamento')+'</button> '+\n     '<button class=\"btn plus\" id=\"l-toggle\"><span>'+(UI.formLead?'×':'+')+'</span>'+\n     (UI.formLead?'Fechar':'Nova lead')+'</button></div></div>';\n\n  if(UI.formOrc) h+=painelOrcamento();\n\n  if(UI.formLead){\n    h+='<div class=\"panel-add\"><div class=\"form\">'+\n      '<div class=\"field\"><label>Origem</label><select id=\"l-origem\">'+\n        ORIGENS.map(function(o){return '<option>'+o+'</option>';}).join('')+'</select></div>'+\n      '<div class=\"field\"><label>Nome</label><input id=\"l-nome\" placeholder=\"Nome do cliente\"></div>'+\n      '<div class=\"field\"><label>Contacto</label><input id=\"l-contacto\" placeholder=\"Telefone ou email\"></div>'+\n      '<div class=\"field\"><label>O que procura</label><input id=\"l-produto\" placeholder=\"Ex: aliança em ouro\"></div>'+\n      '<div class=\"field\"><label>Material</label><select id=\"l-material\">'+\n        ['','Ouro','Prata','Prata Dourada','Aço','Outro'].map(function(m){\n          return '<option>'+m+'</option>';}).join('')+'</select></div>'+\n      '<div class=\"field\"><label>Orçamento aproximado</label><input id=\"l-orcamento\" placeholder=\"Ex: 300\"></div>'+\n      '<div class=\"field\"><label>Validade do orçamento (dias)</label><input id=\"l-validade\" '+\n        'placeholder=\"Ex: 15\"></div>'+\n      '<div class=\"field wide\"><label>Notas</label><textarea id=\"l-notas\" '+\n        'placeholder=\"Ex: volta sábado, queria tamanho 14\"></textarea></div></div>'+\n      '<div class=\"actions\"><button class=\"btn\" id=\"l-add\">Guardar lead</button>'+\n      '<span class=\"msg\" id=\"l-msg\"></span></div></div>';\n  }\n\n  if(L.length){\n    h+='<p class=\"hint\">As setas avançam ou recuam a lead na pipeline. «✓» fecha como ganha, «✕» como perdida.</p>';\n    h+='<div class=\"kanban\">'+ESTADOS.map(function(e){\n      var col=L.filter(function(x){return x.estado===e;})\n        .sort(function(a,b){return leadDiasParada(b)-leadDiasParada(a);});\n      var dot=LEAD_ESTADO_COR[e]||'neu';\n      return '<div class=\"kcol\"><div class=\"kcol-h\"><span><span class=\"kdot '+dot+'\"></span>'+\n        esc(ESTADO_NOME[e])+'</span>'+\n        '<span class=\"kcol-n\">'+col.length+'</span></div><div class=\"kcol-b\">'+\n        (col.length?col.map(leadCard).join(''):'<div class=\"kempty\">—</div>')+\n        '</div></div>';\n    }).join('')+'</div>';\n    h+='<div class=\"card\" style=\"margin-top:14px\"><div class=\"actions\">'+\n      '<button class=\"btn ghost\" id=\"l-csv\">Descarregar CSV</button>'+\n      '<button class=\"btn ghost\" id=\"l-copy\">Copiar para a folha</button>'+\n      '<span class=\"msg\" id=\"l-msg2\"></span></div></div>';\n  } else {\n    h+='<div class=\"card\"><div class=\"empty\">Ainda não há leads. Usa o «+ Nova lead».</div></div>';\n  }\n\n  if(lixoLeads.length){\n    h+='<div class=\"card\" style=\"margin-top:14px\"><h3>Lixo ('+lixoLeads.length+')</h3>'+\n      '<p class=\"hint\">Apagadas nos últimos '+LIXO_DIAS+' dias — depois disso desaparecem em '+\n      'definitivo. Se apagaste alguma sem querer, restaura aqui.</p>'+\n      '<table><thead><tr><th>Nome</th><th>Apagada em</th><th></th></tr></thead><tbody>'+\n      lixoLeads.map(function(x){\n        return '<tr><td>'+esc(x.nome||'Sem nome')+'</td><td>'+\n          (x.apagadoEm?new Date(x.apagadoEm+'T00:00:00').toLocaleDateString('pt-PT'):'—')+'</td>'+\n          '<td class=\"num\"><button class=\"btn ghost sm\" data-restaurarl=\"'+esc(x.id)+'\">restaurar</button></td>'+\n          '</tr>';\n      }).join('')+'</tbody></table></div>';\n  }\n\n  h+=pendingBox('Sobre o envio do orçamento',\n    'O botão <b>Enviar por email</b> abre o teu programa de email já com tudo escrito — assunto, '+\n    'peças, valores e assinatura — bastando carregar em enviar. Sai da conta que tiveres '+\n    'configurada, por isso confirma que é a <i>'+esc(EMPRESA.email)+'</i>. O botão '+\n    '<b>Imprimir / PDF</b> gera o orçamento em papel timbrado com o logótipo, para anexares ou '+\n    'entregares em mão.<br><br>'+\n    'Para o painel enviar sozinho, sem abrir o programa de email, era preciso ligar a conta de '+\n    'email ao sistema. Dá-se feito quando quiseres — e a partir daí também passa a registar '+\n    'automaticamente que o orçamento foi enviado, e a avisar se o cliente não responder.');\n\n  h+=pendingBox('Juntar aqui as três origens',\n    'A pipeline já aceita leads de qualquer origem — o que falta é <b>trazê-las sozinhas</b>:'+\n    '<ul>'+\n    '<li><b>Loja</b> — funciona já, à mão pelo «+». O passo seguinte é um formulário Google, '+\n    'como o do registo de visitas, para se registar do telemóvel com o cliente à frente.</li>'+\n    '<li><b>Site</b> — os pedidos de contacto chegam a <i>geral@ourivesariasarmento.pt</i>. Dá para '+\n    'ler automaticamente essa caixa e criar uma lead por cada pedido. É o mais fácil dos três.</li>'+\n    '<li><b>WhatsApp</b> — é o único bloqueado. A aplicação WhatsApp Business normal não deixa '+\n    'ligar nada por fora; era preciso a versão paga da API através de um parceiro certificado da '+\n    'Meta. Até lá, as mensagens de WhatsApp entram à mão, escolhendo a origem no formulário.</li>'+\n    '</ul>'+\n    'Para já tudo fica guardado <b>neste computador</b>. Descarrega o CSV de vez em quando.');\n\n  return {t:'Pipeline de leads',\n    d:'Clientes interessados vindos da loja, do site e do WhatsApp, num só sítio.',\n    html:h,after:bindLeads};\n};\n\n/* ---------- orçamentos ---------- */\nvar EMPRESA={nome:'Ourivesaria Sarmento',morada:'Rua Áurea, Lisboa',\n  tel:'+351 213 426 774',email:'geral@ourivesariasarmento.pt',\n  site:'ourivesariasarmento.pt'};\nvar PECAS=['Anel','Aliança','Brincos','Fio ou Colar','Pendente','Pulseira','Alfinete',\n           'Botões de punho','Peça em prata','Outro'];\nvar MATERIAIS=['Ouro','Prata','Prata Dourada','Aço','Filigrana','Outro'];\n\nfunction orcTotal(itens){\n  return (itens||[]).reduce(function(s,i){\n    return s+euroNum(i.preco)*(parseInt(i.qtd,10)||1);},0);\n}\nfunction orcRef(){\n  var d=new Date();\n  return 'ORC'+d.getFullYear()+String(d.getMonth()+1).padStart(2,'0')+\n    String(d.getDate()).padStart(2,'0')+'-'+String(d.getHours())+String(d.getMinutes());\n}\nfunction orcTexto(c,itens,validade,notas){\n  var L=[];\n  L.push('Exmo(a). Sr(a). '+(c.nome||'Cliente')+',');\n  L.push('');\n  L.push('Conforme solicitado, segue o orçamento:');\n  L.push('');\n  itens.forEach(function(i){\n    var q=parseInt(i.qtd,10)||1;\n    L.push('• '+i.peca+(i.material?' em '+i.material:'')+\n      (i.desc?' — '+i.desc:'')+(q>1?'  ('+q+' un.)':'')+\n      '   '+eur(euroNum(i.preco)*q));\n  });\n  L.push('');\n  L.push('TOTAL: '+eur(orcTotal(itens)));\n  L.push('');\n  if(notas){ L.push(notas); L.push(''); }\n  L.push('Orçamento válido por '+(validade||15)+' dias.');\n  L.push('');\n  L.push('Com os melhores cumprimentos,');\n  L.push(EMPRESA.nome);\n  L.push(EMPRESA.morada+' · '+EMPRESA.tel);\n  L.push(EMPRESA.email+' · '+EMPRESA.site);\n  return L.join('\\n');\n}\nfunction orcDocumento(c,itens,validade,notas,ref){\n  var logo=document.getElementById('lg');\n  var img=(logo&&logo.getAttribute('src'))||'';\n  var hoje=new Date();\n  var val=new Date(hoje.getTime()+(parseInt(validade,10)||15)*86400000);\n  return '<div class=\"doc\">'+\n    '<div class=\"doc-top\">'+(img?'<img src=\"'+img+'\" alt=\"\">':'<div></div>')+\n      '<div class=\"doc-emp\">'+esc(EMPRESA.nome)+'<br>'+esc(EMPRESA.morada)+'<br>'+\n      esc(EMPRESA.tel)+'<br>'+esc(EMPRESA.email)+'</div></div>'+\n    '<h1>Orçamento</h1><div class=\"ref\">'+esc(ref)+' · '+hoje.toLocaleDateString('pt-PT')+'</div>'+\n    '<div class=\"para\">Cliente</div>'+\n    esc(c.nome||'—')+(c.email?'<br>'+esc(c.email):'')+(c.tel?'<br>'+esc(c.tel):'')+\n    '<div class=\"para\">Peças</div>'+\n    '<table><thead><tr><th>Peça</th><th>Material</th><th>Descrição</th>'+\n    '<th style=\"text-align:right\">Qt.</th><th style=\"text-align:right\">Valor</th></tr></thead><tbody>'+\n    itens.map(function(i){\n      var q=parseInt(i.qtd,10)||1;\n      return '<tr><td>'+esc(i.peca)+'</td><td>'+esc(i.material||'—')+'</td><td>'+\n        esc(i.desc||'—')+'</td><td style=\"text-align:right\">'+q+\n        '</td><td style=\"text-align:right\">'+eur(euroNum(i.preco)*q)+'</td></tr>';}).join('')+\n    '<tr class=\"tot\"><td colspan=\"4\">Total</td><td style=\"text-align:right\">'+\n    eur(orcTotal(itens))+'</td></tr></tbody></table>'+\n    (notas?'<div class=\"para\">Notas</div>'+esc(notas):'')+\n    '<div class=\"fim\">Orçamento válido até <b>'+val.toLocaleDateString('pt-PT')+\n    '</b>.<br>Valores em euros. Peças sujeitas a disponibilidade e a confirmação de medidas.<br>'+\n    esc(EMPRESA.nome)+' · '+esc(EMPRESA.morada)+' · '+esc(EMPRESA.tel)+' · '+\n    esc(EMPRESA.email)+'</div></div>';\n}\n\nfunction painelOrcamento(){\n  var O=UI.orc, itens=O.itens||[];\n  var h='<div class=\"panel-add\">'+\n    '<div class=\"form\">'+\n      '<div class=\"field\"><label>Nome do cliente</label><input id=\"o-nome\" value=\"'+\n        esc(O.nome||'')+'\" placeholder=\"Nome\"></div>'+\n      '<div class=\"field\"><label>Email do cliente</label><input id=\"o-email\" value=\"'+\n        esc(O.email||'')+'\" placeholder=\"email@exemplo.pt\"></div>'+\n      '<div class=\"field\"><label>Telefone</label><input id=\"o-tel\" value=\"'+\n        esc(O.tel||'')+'\" placeholder=\"Opcional\"></div>'+\n    '</div>';\n\n  if(itens.length){\n    h+='<table style=\"margin-top:18px\"><thead><tr><th>Peça</th><th>Material</th>'+\n      '<th>Descrição</th><th class=\"num\">Qt.</th><th class=\"num\">Valor</th><th></th></tr></thead>'+\n      '<tbody>'+itens.map(function(i,idx){\n        var q=parseInt(i.qtd,10)||1;\n        return '<tr><td>'+esc(i.peca)+'</td><td>'+esc(i.material||'—')+'</td><td>'+\n          esc(i.desc||'—')+'</td><td class=\"num\">'+q+'</td><td class=\"num\">'+\n          eur(euroNum(i.preco)*q)+'</td>'+\n          '<td class=\"num\"><button class=\"btn ghost sm\" data-oi=\"'+idx+'\">×</button></td></tr>';\n      }).join('')+\n      '<tr class=\"tot\" style=\"border-top:1.5px solid #141416\"><td colspan=\"4\"><b>Total</b></td>'+\n      '<td class=\"num\"><b>'+eur(orcTotal(itens))+'</b></td><td></td></tr></tbody></table>';\n  }\n\n  h+='<div class=\"form\" style=\"margin-top:18px\">'+\n      '<div class=\"field\"><label>Peça</label><select id=\"o-peca\">'+\n        PECAS.map(function(p){return '<option>'+p+'</option>';}).join('')+'</select></div>'+\n      '<div class=\"field\"><label>Material</label><select id=\"o-material\">'+\n        MATERIAIS.map(function(m){return '<option>'+m+'</option>';}).join('')+'</select></div>'+\n      '<div class=\"field\"><label>Descrição</label><input id=\"o-desc\" '+\n        'placeholder=\"Ex: com pérola, tamanho 14\"></div>'+\n      '<div class=\"field\"><label>Quantidade</label><input id=\"o-qtd\" value=\"1\"></div>'+\n      '<div class=\"field\"><label>Valor unitário (€)</label><input id=\"o-preco\" placeholder=\"Ex: 320\"></div>'+\n    '</div>'+\n    '<div class=\"actions\"><button class=\"btn ghost\" id=\"o-additem\">+ Adicionar peça</button></div>';\n\n  h+='<div class=\"form\" style=\"margin-top:8px\">'+\n      '<div class=\"field\"><label>Validade (dias)</label><input id=\"o-validade\" value=\"'+\n        esc(O.validade||'15')+'\"></div>'+\n      '<div class=\"field wide\"><label>Notas para o cliente</label><textarea id=\"o-notas\" '+\n        'placeholder=\"Ex: prazo de execução de 3 semanas\">'+esc(O.notas||'')+'</textarea></div>'+\n    '</div>';\n\n  h+='<div class=\"actions\">'+\n    '<button class=\"btn\" id=\"o-lead\">Guardar e criar lead</button>'+\n    '<button class=\"btn ghost\" id=\"o-email-btn\">Enviar por email</button>'+\n    '<button class=\"btn ghost\" id=\"o-print\">Imprimir / PDF</button>'+\n    '<button class=\"btn ghost\" id=\"o-copy\">Copiar texto</button>'+\n    '<span class=\"msg\" id=\"o-msg\"></span></div></div>';\n  return h;\n}\n\nfunction guardarCamposOrc(){\n  var g=function(id){var e=document.getElementById(id);return e?String(e.value).trim():'';};\n  UI.orc.nome=g('o-nome'); UI.orc.email=g('o-email'); UI.orc.tel=g('o-tel');\n  UI.orc.validade=g('o-validade')||'15'; UI.orc.notas=g('o-notas');\n}\n\nfunction bindOrcamento(){\n  var t=document.getElementById('o-toggle');\n  if(t) t.addEventListener('click',function(){\n    UI.formOrc=!UI.formOrc; if(UI.formOrc) UI.formLead=false; refresh();});\n\n  var addI=document.getElementById('o-additem');\n  if(addI) addI.addEventListener('click',function(){\n    guardarCamposOrc();\n    var g=function(id){var e=document.getElementById(id);return e?String(e.value).trim():'';};\n    var preco=g('o-preco');\n    if(!euroNum(preco)){\n      var m=document.getElementById('o-msg');\n      if(m){m.textContent='Indica o valor da peça.';m.style.color='#a4604f';} return;\n    }\n    UI.orc.itens=(UI.orc.itens||[]).concat([{peca:g('o-peca'),material:g('o-material'),\n      desc:g('o-desc'),qtd:g('o-qtd')||'1',preco:preco}]);\n    refresh();\n  });\n  document.querySelectorAll('[data-oi]').forEach(function(b){\n    b.addEventListener('click',function(){\n      guardarCamposOrc();\n      UI.orc.itens=(UI.orc.itens||[]).filter(function(_,i){return i!==+b.dataset.oi;});\n      refresh();});\n  });\n\n  function valido(){\n    guardarCamposOrc();\n    var m=document.getElementById('o-msg');\n    if(!(UI.orc.itens||[]).length){\n      if(m){m.textContent='Adiciona pelo menos uma peça.';m.style.color='#a4604f';}\n      return false;\n    }\n    return true;\n  }\n\n  var lead=document.getElementById('o-lead');\n  if(lead) lead.addEventListener('click',function(){\n    if(!valido()) return;\n    var O=UI.orc, ref=orcRef();\n    var L=leadsGet();\n    var dias=parseInt(O.validade,10)||15;\n    var venc=new Date(); venc.setDate(venc.getDate()+dias);\n    var validadeAte=venc.getFullYear()+'-'+String(venc.getMonth()+1).padStart(2,'0')+'-'+\n      String(venc.getDate()).padStart(2,'0');\n    L.push({id:'L'+Date.now(),data:new Date().toLocaleDateString('pt-PT'),origem:'Loja',\n      nome:O.nome,contacto:[O.email,O.tel].filter(Boolean).join(' · '),\n      produto:O.itens.map(function(i){return i.peca;}).join(', '),\n      material:O.itens.map(function(i){return i.material;})\n        .filter(function(v,i,a){return v&&a.indexOf(v)===i;}).join(', '),\n      orcamento:String(orcTotal(O.itens)),prazo:'',validadeAte:validadeAte,\n      notas:'Orçamento '+ref+(O.notas?' — '+O.notas:''),estado:'enviado',\n      atualizado:todayISO()});\n    leadsSet(L);\n    UI.orc={itens:[]}; UI.formOrc=false; refresh();\n  });\n\n  var mail=document.getElementById('o-email-btn');\n  if(mail) mail.addEventListener('click',function(){\n    if(!valido()) return;\n    var O=UI.orc, m=document.getElementById('o-msg');\n    if(!O.email){ if(m){m.textContent='Falta o email do cliente.';m.style.color='#a4604f';} return; }\n    var assunto='Orçamento · '+EMPRESA.nome;\n    var corpo=orcTexto(O,O.itens,O.validade,O.notas);\n    window.location.href='mailto:'+encodeURIComponent(O.email)+\n      '?subject='+encodeURIComponent(assunto)+'&body='+encodeURIComponent(corpo);\n    if(m){m.textContent='Abriu o teu programa de email com tudo preenchido.';m.style.color='#4a7c59';}\n  });\n\n  var pr=document.getElementById('o-print');\n  if(pr) pr.addEventListener('click',function(){\n    if(!valido()) return;\n    var O=UI.orc;\n    document.getElementById('orc-print').innerHTML=\n      orcDocumento(O,O.itens,O.validade,O.notas,orcRef());\n    window.print();\n  });\n\n  var cp=document.getElementById('o-copy');\n  if(cp) cp.addEventListener('click',function(){\n    if(!valido()) return;\n    var O=UI.orc;\n    copiar(orcTexto(O,O.itens,O.validade,O.notas),document.getElementById('o-msg'));\n  });\n}\n\nfunction bindLeads(){\n  bindOrcamento();\n  var t=document.getElementById('l-toggle');\n  if(t) t.addEventListener('click',function(){\n    UI.formLead=!UI.formLead; if(UI.formLead) UI.formOrc=false; refresh();});\n  var add=document.getElementById('l-add');\n  if(add) add.addEventListener('click',function(){\n    var g=function(id){var e=document.getElementById(id);return e?String(e.value).trim():'';};\n    var nome=g('l-nome'), produto=g('l-produto'), contacto=g('l-contacto');\n    if(!nome&&!contacto&&!produto){\n      var m=document.getElementById('l-msg');\n      if(m){m.textContent='Preenche pelo menos o nome ou o que procura.';m.style.color='#a4604f';}\n      return;\n    }\n    var L=leadsGet();\n    var validadeAte='';\n    var diasV=parseInt(g('l-validade'),10);\n    if(diasV){ var venc=new Date(); venc.setDate(venc.getDate()+diasV);\n      validadeAte=venc.getFullYear()+'-'+String(venc.getMonth()+1).padStart(2,'0')+'-'+\n        String(venc.getDate()).padStart(2,'0'); }\n    L.push({id:'L'+Date.now(),data:new Date().toLocaleDateString('pt-PT'),\n      origem:g('l-origem')||'Loja',nome:nome,contacto:contacto,produto:produto,\n      material:g('l-material'),orcamento:g('l-orcamento'),prazo:'',validadeAte:validadeAte,\n      notas:g('l-notas'),estado:'nova',atualizado:todayISO()});\n    leadsSet(L); UI.formLead=false; refresh();\n  });\n  document.querySelectorAll('[data-mv]').forEach(function(c){\n    c.addEventListener('click',function(){\n      var L=leadsGet();\n      L.forEach(function(x){ if(x.id===c.dataset.mv){\n        x.estado=c.dataset.to; x.atualizado=todayISO(); } });\n      leadsSet(L); refresh();\n    });\n  });\n  document.querySelectorAll('[data-del]').forEach(function(b){\n    b.addEventListener('click',function(){\n      if(!confirm('Apagar esta lead? Fica no Lixo '+LIXO_DIAS+' dias, com opção de restaurar.')) return;\n      var L=leadsGet();\n      L.forEach(function(x){ if(x.id===b.dataset.del){ x.apagado=true; x.apagadoEm=todayISO(); } });\n      leadsSet(L); refresh();\n    });\n  });\n  document.querySelectorAll('[data-restaurarl]').forEach(function(b){\n    b.addEventListener('click',function(){\n      var L=leadsGet();\n      L.forEach(function(x){ if(x.id===b.dataset.restaurarl){\n        delete x.apagado; delete x.apagadoEm; x.atualizado=todayISO(); } });\n      leadsSet(L); refresh();\n    });\n  });\n  var csv=document.getElementById('l-csv');\n  if(csv) csv.addEventListener('click',function(){\n    descarregar(csvLinhas(leadsGet().filter(function(x){return !x.apagado;}),LEAD_COLS,LEAD_TIT,','),\n      'leads_sarmento.csv');});\n  var cp=document.getElementById('l-copy');\n  if(cp) cp.addEventListener('click',function(){\n    copiar(csvLinhas(leadsGet().filter(function(x){return !x.apagado;}),LEAD_COLS,LEAD_TIT,'\\t'),\n      document.getElementById('l-msg2'));});\n}\n\n/* ---------------- PARCERIAS ---------------- */\nvar PARC_COLS=['criado','tipo','nome','contacto','valor','notas','estado'];\nvar PARC_TIT=['Criado','Tipo','Nome','Contacto','Valor estimado','Notas','Estado'];\n\nfunction parceriaCard(x){\n  var i=PARC_ESTADOS.indexOf(x.estado);\n  var prev=i>0&&i<4?PARC_ESTADOS[i-1]:null, next=i>=0&&i<4?PARC_ESTADOS[i+1]:null;\n  var dias=parceriaDiasParada(x);\n  var badges='';\n  if(parceriaEmAberto(x)&&dias>=PARC_AVISO_DIAS)\n    badges='<span class=\"kbadge al\">parada há '+dias+'d</span>';\n  var tag=parceriaTipoTag(x.tipo);\n  var tom=PARC_ESTADO_COR[x.estado]||'neu';\n  return '<div class=\"kcard tone-'+tom+'\" draggable=\"true\" data-drag=\"'+esc(x.id)+'\">'+\n    '<div class=\"kc-top\"><span class=\"tagO '+tag+'\">'+esc(tag)+'</span>'+\n    '<button class=\"kx\" data-delp=\"'+esc(x.id)+'\" title=\"apagar\">×</button></div>'+\n    '<div class=\"kc-nome\">'+esc(x.nome||'Sem nome')+'</div>'+\n    (euroNum(x.valor)?'<div class=\"kc-val\">'+eur(euroNum(x.valor))+' estimado</div>':'')+\n    (x.contacto?'<div class=\"kc-contacto\">'+esc(x.contacto)+'</div>':'')+\n    (x.notas?'<div class=\"kc-prod\">'+esc(x.notas)+'</div>':'')+\n    '<div class=\"kc-badges\">'+badges+'</div>'+\n    '<div class=\"kc-acoes\">'+\n      (prev?'<button class=\"btn ghost sm\" data-mvp=\"'+esc(x.id)+'\" data-to=\"'+prev+'\">‹</button>':'<span></span>')+\n      (next?'<button class=\"btn ghost sm\" data-mvp=\"'+esc(x.id)+'\" data-to=\"'+next+'\">'+\n        (next==='ativa'?'✓ Ativa':(esc(PARC_ESTADO_NOME[next])+' ›'))+'</button>':'')+\n      (x.estado!=='perdida'?'<button class=\"btn ghost sm\" data-mvp=\"'+esc(x.id)+'\" data-to=\"perdida\">✕</button>':'')+\n    '</div></div>';\n}\n\nPAGES['parcerias']=function(M){\n  var Praw=PARCERIAS.get();\n  var _lxp=separarLixo(Praw);\n  if(_lxp.vivos.length+_lxp.lixo.length!==Praw.length) PARCERIAS.set(_lxp.vivos.concat(_lxp.lixo));\n  var P=_lxp.vivos, lixoParcerias=_lxp.lixo;\n  var porEstado={}; PARC_ESTADOS.forEach(function(e){porEstado[e]=0;});\n  P.forEach(function(x){ if(porEstado[x.estado]!=null) porEstado[x.estado]++; });\n  var ativas=P.filter(function(x){return x.estado==='ativa';});\n  var naoPerdidas=P.length-porEstado.perdida;\n  var taxaFecho=naoPerdidas?pct(ativas.length,naoPerdidas):0;\n  var hoje=new Date();\n  var novasMes=P.filter(function(x){\n    var d=parseDate(x.criado);\n    return d&&d.getFullYear()===hoje.getFullYear()&&d.getMonth()===hoje.getMonth();\n  });\n  var paradas=P.filter(function(x){return parceriaEmAberto(x)&&parceriaDiasParada(x)>=PARC_AVISO_DIAS;});\n\n  var h='<div class=\"kpis\">'+\n    kpi('Parcerias ativas',nf(ativas.length),'a gerar valor','',ativas.length?'pos':null)+\n    kpi('Novas este mês',nf(novasMes.length),'criadas em '+MESES_LONGOS[hoje.getMonth()],'','info')+\n    kpi('Taxa de fecho',nf(taxaFecho,0)+'%','das não perdidas, já ativas')+\n    kpi('Paradas',nf(paradas.length),'sem avanço há '+PARC_AVISO_DIAS+'+ dias','',\n      paradas.length?'warn':null)+\n    '</div>';\n\n  h+='<div class=\"addbar\"><h3>Pipeline de parcerias</h3><div>'+\n     '<button class=\"btn plus\" id=\"p-toggle\"><span>'+(UI.formParceria?'×':'+')+'</span>'+\n     (UI.formParceria?'Fechar':'Nova parceria')+'</button></div></div>';\n\n  if(UI.formParceria){\n    h+='<div class=\"panel-add\"><div class=\"form\">'+\n      '<div class=\"field\"><label>Tipo</label><select id=\"p-tipo\">'+\n        PARC_TIPOS.map(function(t){return '<option>'+esc(t)+'</option>';}).join('')+'</select></div>'+\n      '<div class=\"field\"><label>Nome da empresa</label><input id=\"p-nome\" '+\n        'placeholder=\"Ex: Hotel Avenida Palace\"></div>'+\n      '<div class=\"field\"><label>Contacto</label><input id=\"p-contacto\" '+\n        'placeholder=\"Nome, telefone ou email\"></div>'+\n      '<div class=\"field\"><label>Valor estimado (€)</label><input id=\"p-valor\" placeholder=\"Opcional\"></div>'+\n      '<div class=\"field wide\"><label>Notas</label><textarea id=\"p-notas\" '+\n        'placeholder=\"Ex: interessados numa coleção de prata exclusiva para o spa\"></textarea></div></div>'+\n      '<div class=\"actions\"><button class=\"btn\" id=\"p-add\">Guardar parceria</button>'+\n      '<span class=\"msg\" id=\"p-msg\"></span></div></div>';\n  }\n\n  if(P.length){\n    h+='<p class=\"hint\">Arrasta cada cartão para a coluna certa (ou usa as setas). «✓ Ativa» fecha '+\n      'como parceria em funcionamento, «✕» como perdida. Cada estado tem a sua cor.</p>';\n    h+='<div class=\"kanban\">'+PARC_ESTADOS.map(function(e){\n      var col=P.filter(function(x){return x.estado===e;})\n        .sort(function(a,b){return parceriaDiasParada(b)-parceriaDiasParada(a);});\n      var dot=PARC_ESTADO_COR[e]||'neu';\n      return '<div class=\"kcol\"><div class=\"kcol-h\"><span><span class=\"kdot '+dot+'\"></span>'+\n        esc(PARC_ESTADO_NOME[e])+'</span>'+\n        '<span class=\"kcol-n\">'+col.length+'</span></div>'+\n        '<div class=\"kcol-b\" data-coldrop=\"'+e+'\">'+\n        (col.length?col.map(parceriaCard).join(''):'<div class=\"kempty\">—</div>')+\n        '</div></div>';\n    }).join('')+'</div>';\n    h+='<div class=\"card\" style=\"margin-top:14px\"><div class=\"actions\">'+\n      '<button class=\"btn ghost\" id=\"p-csv\">Descarregar CSV</button>'+\n      '<button class=\"btn ghost\" id=\"p-copy\">Copiar para a folha</button>'+\n      '<span class=\"msg\" id=\"p-msg2\"></span></div></div>';\n  } else {\n    h+='<div class=\"card\"><div class=\"empty\">Ainda não há parcerias registadas. '+\n      'Usa o «+ Nova parceria».</div></div>';\n  }\n\n  if(lixoParcerias.length){\n    h+='<div class=\"card\" style=\"margin-top:14px\"><h3>Lixo ('+lixoParcerias.length+')</h3>'+\n      '<p class=\"hint\">Apagadas nos últimos '+LIXO_DIAS+' dias — depois disso desaparecem em '+\n      'definitivo. Se apagaste alguma sem querer, restaura aqui.</p>'+\n      '<table><thead><tr><th>Nome</th><th>Apagada em</th><th></th></tr></thead><tbody>'+\n      lixoParcerias.map(function(x){\n        return '<tr><td>'+esc(x.nome||'Sem nome')+'</td><td>'+\n          (x.apagadoEm?new Date(x.apagadoEm+'T00:00:00').toLocaleDateString('pt-PT'):'—')+'</td>'+\n          '<td class=\"num\"><button class=\"btn ghost sm\" data-restaurarp=\"'+esc(x.id)+'\">restaurar</button></td>'+\n          '</tr>';\n      }).join('')+'</tbody></table></div>';\n  }\n\n  h+=pendingBox('Como medir o retorno real de cada parceria',\n    'Por agora, o valor de cada parceria é uma <b>estimativa preenchida à mão</b> — ainda não há '+\n    'forma automática de saber quantos clientes um hotel realmente trouxe, ou quanto rendeu uma '+\n    'coleção exclusiva com um fornecedor. O caminho mais realista para isso ficar automático é '+\n    'aproveitar o campo <i>\"como nos conheceu\"</i> do Registo de atendimento e cruzá-lo com o nome '+\n    'da parceria, assim que houver respostas suficientes a apontarem para hotéis ou coleções '+\n    'específicas.');\n\n  return {t:'Parcerias',\n    d:'Fornecedores para coleções exclusivas e negócios locais para referenciamento, numa pipeline própria.',\n    html:h,after:bindParcerias};\n};\n\nfunction bindParcerias(){\n  var t=document.getElementById('p-toggle');\n  if(t) t.addEventListener('click',function(){ UI.formParceria=!UI.formParceria; refresh(); });\n\n  var add=document.getElementById('p-add');\n  if(add) add.addEventListener('click',function(){\n    var g=function(id){var e=document.getElementById(id);return e?String(e.value).trim():'';};\n    var nome=g('p-nome'), contacto=g('p-contacto'), notas=g('p-notas');\n    if(!nome){\n      var m=document.getElementById('p-msg');\n      if(m){m.textContent='Indica pelo menos o nome da empresa.';m.style.color='#a4604f';}\n      return;\n    }\n    var P=PARCERIAS.get();\n    P.push({id:'P'+Date.now(),criado:todayISO(),tipo:g('p-tipo')||PARC_TIPOS[0],\n      nome:nome,contacto:contacto,valor:g('p-valor'),notas:notas,estado:'lead',\n      atualizado:todayISO()});\n    PARCERIAS.set(P); UI.formParceria=false; refresh();\n  });\n\n  document.querySelectorAll('[data-mvp]').forEach(function(c){\n    c.addEventListener('click',function(){\n      var P=PARCERIAS.get();\n      P.forEach(function(x){ if(x.id===c.dataset.mvp){\n        x.estado=c.dataset.to; x.atualizado=todayISO(); } });\n      PARCERIAS.set(P); refresh();\n    });\n  });\n\n  /* arrastar cartões entre colunas (pedido pelo Diogo 2026-07-29) — as setas continuam a\n     funcionar, isto é só um atalho alternativo, mais rápido para mover várias de uma vez */\n  function moverParceriaPara(id,novoEstado){\n    var P=PARCERIAS.get(), mudou=false;\n    P.forEach(function(x){\n      if(x.id===id&&x.estado!==novoEstado){ x.estado=novoEstado; x.atualizado=todayISO(); mudou=true; }\n    });\n    if(mudou) PARCERIAS.set(P);\n    return mudou;\n  }\n  document.querySelectorAll('.kanban [draggable=\"true\"]').forEach(function(card){\n    card.addEventListener('dragstart',function(ev){\n      if(ev.dataTransfer){ ev.dataTransfer.setData('text/plain',card.dataset.drag);\n        ev.dataTransfer.effectAllowed='move'; }\n      card.classList.add('dragging');\n    });\n    card.addEventListener('dragend',function(){ card.classList.remove('dragging'); });\n  });\n  document.querySelectorAll('[data-coldrop]').forEach(function(col){\n    col.addEventListener('dragover',function(ev){\n      ev.preventDefault();\n      if(ev.dataTransfer) ev.dataTransfer.dropEffect='move';\n      col.classList.add('dragover');\n    });\n    col.addEventListener('dragleave',function(){ col.classList.remove('dragover'); });\n    col.addEventListener('drop',function(ev){\n      ev.preventDefault();\n      col.classList.remove('dragover');\n      var id=ev.dataTransfer?ev.dataTransfer.getData('text/plain'):null;\n      if(id&&moverParceriaPara(id,col.dataset.coldrop)) refresh();\n    });\n  });\n\n  document.querySelectorAll('[data-delp]').forEach(function(b){\n    b.addEventListener('click',function(){\n      if(!confirm('Apagar esta parceria? Fica no Lixo '+LIXO_DIAS+' dias, com opção de restaurar.')) return;\n      var P=PARCERIAS.get();\n      P.forEach(function(x){ if(x.id===b.dataset.delp){ x.apagado=true; x.apagadoEm=todayISO(); } });\n      PARCERIAS.set(P); refresh();\n    });\n  });\n  document.querySelectorAll('[data-restaurarp]').forEach(function(b){\n    b.addEventListener('click',function(){\n      var P=PARCERIAS.get();\n      P.forEach(function(x){ if(x.id===b.dataset.restaurarp){\n        delete x.apagado; delete x.apagadoEm; x.atualizado=todayISO(); } });\n      PARCERIAS.set(P); refresh();\n    });\n  });\n  var csv=document.getElementById('p-csv');\n  if(csv) csv.addEventListener('click',function(){\n    descarregar(csvLinhas(PARCERIAS.get().filter(function(x){return !x.apagado;}),PARC_COLS,PARC_TIT,','),\n      'parcerias_sarmento.csv');});\n  var cp=document.getElementById('p-copy');\n  if(cp) cp.addEventListener('click',function(){\n    copiar(csvLinhas(PARCERIAS.get().filter(function(x){return !x.apagado;}),PARC_COLS,PARC_TIT,'\\t'),\n      document.getElementById('p-msg2'));});\n}\n\n/* ---------------- FORNECEDORES ---------------- */\n/* Faturas a fornecedores (2026-07-30): a fonte passou a ser a Google Sheet partilhada\n   FORN_FATURAS_URL, não mais o localStorage do navegador — assim o Diogo e o superior dele\n   marcam pago/por pagar diretamente na folha (partilhada como editor) e veem sempre os mesmos\n   dados, em vez de cada um só ver o que escreveu no seu próprio computador. Esta página só lê\n   e resume; para adicionar uma fatura ou marcar como paga, usa-se o botão que abre a folha. */\nvar FAT_COLS=['fornecedor','numero','data','vencimento','valor','pago','notas'];\nvar FAT_TIT=['Fornecedor','Nº Fatura','Data','Vencimento','Valor','Pago','Notas'];\n\nPAGES['fornecedores']=function(M){\n  var F=M.FAT||[];\n  var temFolha=F.length>0;\n  var d0=new Date(RANGE.de.getFullYear(),RANGE.de.getMonth(),RANGE.de.getDate());\n  var d1=new Date(RANGE.ate.getFullYear(),RANGE.ate.getMonth(),RANGE.ate.getDate(),23,59,59);\n  var noPeriodo=F.filter(function(x){\n    var d=parseDate(x.data); return d&&d>=d0&&d<=d1;});\n  var hoje=new Date(); hoje.setHours(0,0,0,0);\n\n  var porPagar=F.filter(function(x){return !x.pago;});\n  var totalDivida=porPagar.reduce(function(s,x){return s+x.valor;},0);\n  var vencidas=porPagar.filter(function(x){\n    var v=parseDate(x.vencimento); return v&&v<hoje;});\n  var totalVencido=vencidas.reduce(function(s,x){return s+x.valor;},0);\n  var pagoPeriodo=noPeriodo.filter(function(x){return x.pago;})\n    .reduce(function(s,x){return s+x.valor;},0);\n  var fornecedores=[];\n  F.forEach(function(x){ if(x.fornecedor&&fornecedores.indexOf(x.fornecedor)<0)\n    fornecedores.push(x.fornecedor); });\n  fornecedores.sort();\n\n  var h='<div class=\"addbar\"><h3>Faturas a fornecedores</h3><div>'+\n    '<a class=\"btn plus\" href=\"'+esc(FORN_FATURAS_URL)+'\" target=\"_blank\" rel=\"noopener\">'+\n    '<span>↗</span>Abrir a folha partilhada</a></div></div>'+\n    '<p class=\"hint\">É lá que se adiciona cada fatura e se marca pago/por pagar — o Diogo e o '+\n    'superior dele editam os dois na mesma folha, e esta página só mostra o resumo, sempre '+\n    'atualizado.</p>';\n\n  h+=filterBar(M);\n\n  if(!temFolha){\n    h+=pendingBox('Ainda sem faturas na folha',\n      'Abre a folha partilhada (botão acima) e adiciona a primeira fatura — Fornecedor, Nº, '+\n      'Data, Vencimento, Valor, e \"Sim\"/\"Não\" na coluna Pago. Assim que houver uma linha, esta '+\n      'página passa a mostrar a dívida total e o peso de cada fornecedor automaticamente.');\n    return {t:'Fornecedores',d:'Faturas em dívida e peso de cada fornecedor.',\n      html:h,after:function(){bindFilter(M);bindFaturas();}};\n  }\n\n  h+='<div class=\"kpis\">'+\n    kpi('Por pagar',eur(totalDivida),porPagar.length+(porPagar.length===1?' fatura':' faturas'),\n      '',porPagar.length?'warn':null)+\n    kpi('Já vencido',eur(totalVencido),vencidas.length+' em atraso','',vencidas.length?'neg':null)+\n    kpi('Pago no período',eur(pagoPeriodo),'faturas com data no período','',\n      pagoPeriodo?'pos':null)+\n    kpi('Fornecedores',nf(fornecedores.length),'com faturas registadas')+\n    '</div>';\n\n  /* resumo por fornecedor */\n  if(fornecedores.length){\n    var resumo=fornecedores.map(function(nome){\n      var fs=F.filter(function(x){return x.fornecedor===nome;});\n      var tot=fs.reduce(function(s,x){return s+x.valor;},0);\n      var div=fs.filter(function(x){return !x.pago;})\n                .reduce(function(s,x){return s+x.valor;},0);\n      return {k:nome,n:fs.length,total:tot,divida:div};\n    }).sort(function(a,b){return b.total-a.total;});\n\n    h+='<div class=\"g2\">'+\n      '<div class=\"card\"><h3>Peso de cada fornecedor</h3>'+\n        '<p class=\"hint\">Total faturado, todas as faturas registadas</p>'+\n        barList(resumo.map(function(r){return{k:r.k,v:r.total};}),\n          function(i){return eur(i.v);})+'</div>'+\n      '<div class=\"card\"><h3>Quanto se deve a cada um</h3>'+\n        '<p class=\"hint\">Clica num fornecedor para ver só as faturas dele</p>'+\n        '<table><thead><tr><th>Fornecedor</th><th class=\"num\">Faturas</th>'+\n        '<th class=\"num\">Total</th><th class=\"num\">Em dívida</th></tr></thead><tbody>'+\n        resumo.map(function(r){\n          return '<tr style=\"cursor:pointer\" data-forn=\"'+esc(r.k)+'\"><td>'+\n            (UI.filtroForn===r.k?'<b>'+esc(r.k)+'</b>':esc(r.k))+'</td>'+\n            '<td class=\"num\">'+r.n+'</td><td class=\"num\">'+eur(r.total)+'</td>'+\n            '<td class=\"num\"'+(r.divida>0?' style=\"color:#a4604f;font-weight:600\"':'')+'>'+\n            (r.divida>0?eur(r.divida):'—')+'</td></tr>';}).join('')+\n        '</tbody></table></div></div>';\n  }\n\n  var lista=F.slice().sort(function(a,b){\n    var da=parseDate(a.vencimento)||parseDate(a.data)||new Date(0);\n    var db=parseDate(b.vencimento)||parseDate(b.data)||new Date(0);\n    return (a.pago?1:0)-(b.pago?1:0) || da-db;});\n  if(UI.filtroForn) lista=lista.filter(function(x){return x.fornecedor===UI.filtroForn;});\n\n  h+='<div class=\"addbar\"><h3>'+(UI.filtroForn?'Faturas de '+esc(UI.filtroForn):'Faturas')+\n     '</h3>'+(UI.filtroForn?'<button class=\"btn ghost sm\" id=\"f-limpar\">ver todas</button>':'')+\n     '</div>';\n\n  if(lista.length){\n    h+='<div class=\"card\"><p class=\"hint\">O estado de cada fatura vem da folha partilhada — '+\n      'para mudar, marca \"Sim\" ou \"Não\" na coluna Pago lá. Por pagar primeiro, as mais antigas '+\n      'no topo.</p>'+\n      '<table><thead><tr><th style=\"width:34px\">Pago</th><th>Fornecedor</th><th>Nº</th>'+\n      '<th>Data</th><th>Vencimento</th><th class=\"num\">Valor</th></tr></thead><tbody>'+\n      lista.map(function(x){\n        var v=parseDate(x.vencimento);\n        var atraso=!x.pago&&v&&v<hoje;\n        return '<tr class=\"'+(x.pago?'paga':'')+(atraso?' vencida':'')+'\">'+\n          '<td><span class=\"chk ro'+(x.pago?' on':'')+'\" title=\"'+(x.pago?'Pago':'Por pagar')+\n            '\"></span></td>'+\n          '<td>'+esc(x.fornecedor||'—')+\n            (x.notas?'<br><span style=\"color:#9d9890;font-size:11.5px\">'+esc(x.notas)+'</span>':'')+\n            '</td>'+\n          '<td>'+esc(x.numero||'—')+'</td>'+\n          '<td>'+(parseDate(x.data)?parseDate(x.data).toLocaleDateString('pt-PT'):'—')+'</td>'+\n          '<td class=\"venc\">'+(v?v.toLocaleDateString('pt-PT'):'—')+\n            (atraso?' <span class=\"kbadge al\">em atraso</span>':\n              (x.pago?' <span class=\"kbadge ok\">pago</span>':' <span class=\"kbadge av\">por pagar</span>'))+\n            '</td>'+\n          '<td class=\"num\">'+eur(x.valor)+'</td>'+\n          '</tr>';}).join('')+'</tbody></table>'+\n      '<div class=\"actions\"><button class=\"btn ghost\" id=\"f-csv\">Descarregar CSV</button>'+\n      '<span class=\"msg\" id=\"f-msg2\"></span></div></div>';\n  } else {\n    h+='<div class=\"card\"><div class=\"empty\">Sem faturas deste fornecedor.</div></div>';\n  }\n\n  h+=pendingBox('O que falta: as vendas por fornecedor',\n    'Esta página já responde a <b>quanto se deve e a quem</b>, partilhado entre quem tiver '+\n    'acesso à folha — e isso não depende de ninguém.<br><br>'+\n    'O que falta é o outro lado: <b>quanto é que cada fornecedor nos fez vender</b>. Isso está no '+\n    'Diamante, que sabe de que fornecedor veio cada peça vendida. Com essa ligação, esta página '+\n    'passa a cruzar as duas coisas e a responder ao que interessa mesmo: <b>quais os fornecedores '+\n    'que vendem muito e custam pouco</b>, quais os que ocupam stock sem rodar, e onde há margem '+\n    'para negociar melhores condições.');\n\n  return {t:'Fornecedores',d:'Faturas em dívida e peso de cada fornecedor.',\n    html:h,after:function(){bindFilter(M);bindFaturas();}};\n};\n\nfunction bindFaturas(){\n  var lim=document.getElementById('f-limpar');\n  if(lim) lim.addEventListener('click',function(){ UI.filtroForn=''; refresh(); });\n  document.querySelectorAll('[data-forn]').forEach(function(r){\n    r.addEventListener('click',function(){\n      UI.filtroForn = UI.filtroForn===r.dataset.forn ? '' : r.dataset.forn; refresh();});\n  });\n  var csv=document.getElementById('f-csv');\n  if(csv) csv.addEventListener('click',function(){\n    descarregar(csvLinhas(M.FAT||[],FAT_COLS,FAT_TIT,','),'faturas_fornecedores.csv');});\n}\n\n/* ---------------- MARKETING ---------------- */\nvar CAMP_COLS=['data','tipo','assunto','destinatarios','aberturas','cliques','vendas','notas'];\nvar CAMP_TIT=['Data','Tipo','Assunto','Destinatários','Aberturas %','Cliques %',\n              'Vendas €','Notas'];\n\nPAGES['marketing']=function(M){\n  var C=CAMPANHAS.get().slice().sort(function(a,b){\n    return (parseDate(b.data)||0)-(parseDate(a.data)||0);});\n  var h='<div class=\"kpis\">';\n  TIPOS_CAMP.slice(0,2).forEach(function(tipo){\n    var u=ultimoEnvio(tipo);\n    var dias=u?diasDesde(parseDate(u.data)):null;\n    var alvo=CADENCIA[tipo];\n    h+=kpi('Última '+(tipo==='SMS'?'SMS':'newsletter'),\n      dias==null?'—':'há '+dias+'d',\n      dias==null?'sem envios registados'\n        :(dias>alvo?'já passou o objetivo de '+alvo+' dias':'dentro do objetivo de '+alvo+' dias'));\n  });\n  var esteAno=C.filter(function(x){\n    var d=parseDate(x.data); return d&&d.getFullYear()===new Date().getFullYear();});\n  var comAb=C.filter(function(x){return euroNum(x.aberturas)>0;});\n  var mediaAb=comAb.length?comAb.reduce(function(s,x){return s+euroNum(x.aberturas);},0)/comAb.length:0;\n  h+=kpi('Envios este ano',nf(esteAno.length),'newsletters, SMS e campanhas')+\n     kpi('Abertura média',comAb.length?nf(mediaAb,1)+'%':'—',\n         comAb.length?'em '+comAb.length+' envios':'ainda sem dados')+\n     '</div>';\n\n  var av=avisosMarketing();\n  if(av.length) h+='<div class=\"ins\">'+av.map(function(a){\n    return insight(a[0],a[1],a[2]);}).join('')+'</div>';\n\n  h+='<div class=\"addbar\"><h3>Envios</h3><button class=\"btn plus\" id=\"c-toggle\">'+\n     '<span>'+(UI.formCamp?'×':'+')+'</span>'+(UI.formCamp?'Fechar':'Registar envio')+\n     '</button></div>';\n\n  if(UI.formCamp){\n    h+='<div class=\"panel-add\"><div class=\"form\">'+\n      '<div class=\"field\"><label>Tipo</label><select id=\"c-tipo\">'+\n        TIPOS_CAMP.map(function(t){return '<option>'+t+'</option>';}).join('')+'</select></div>'+\n      '<div class=\"field\"><label>Data do envio</label><input type=\"date\" id=\"c-data\" value=\"'+\n        iso(new Date())+'\"></div>'+\n      '<div class=\"field wide\"><label>Assunto</label><input id=\"c-assunto\" '+\n        'placeholder=\"Ex: Coleção Timeless — novidades de julho\"></div>'+\n      '<div class=\"field\"><label>Destinatários</label><input id=\"c-dest\" placeholder=\"Ex: 1200\"></div>'+\n      '<div class=\"field\"><label>Aberturas (%)</label><input id=\"c-ab\" placeholder=\"Ex: 24\"></div>'+\n      '<div class=\"field\"><label>Cliques (%)</label><input id=\"c-cl\" placeholder=\"Ex: 3\"></div>'+\n      '<div class=\"field\"><label>Vendas atribuídas (€)</label><input id=\"c-vd\" placeholder=\"Ex: 450\"></div>'+\n      '<div class=\"field wide\"><label>Notas</label><textarea id=\"c-notas\" '+\n        'placeholder=\"Ex: enviada às 10h, terça-feira\"></textarea></div></div>'+\n      '<div class=\"actions\"><button class=\"btn\" id=\"c-add\">Guardar envio</button>'+\n      '<span class=\"msg\" id=\"c-msg\"></span></div></div>';\n  }\n\n  if(C.length){\n    h+='<div class=\"card\"><p class=\"hint\">Mais recentes primeiro.</p>'+\n      '<table><thead><tr><th>Data</th><th>Tipo</th><th>Assunto</th>'+\n      '<th class=\"num\">Destinatários</th><th class=\"num\">Aberturas</th>'+\n      '<th class=\"num\">Vendas</th><th></th></tr></thead><tbody>'+\n      C.map(function(x){\n        var d=parseDate(x.data);\n        return '<tr><td>'+(d?d.toLocaleDateString('pt-PT'):'—')+'</td>'+\n          '<td><span class=\"tagO\">'+esc(x.tipo)+'</span></td>'+\n          '<td>'+esc(x.assunto||'—')+\n            (x.notas?'<br><span style=\"color:#9d9890;font-size:11.5px\">'+esc(x.notas)+'</span>':'')+\n            '</td>'+\n          '<td class=\"num\">'+(euroNum(x.destinatarios)?nf(euroNum(x.destinatarios)):'—')+'</td>'+\n          '<td class=\"num\">'+(euroNum(x.aberturas)?nf(euroNum(x.aberturas),1)+'%':'—')+'</td>'+\n          '<td class=\"num\">'+(euroNum(x.vendas)?eur(euroNum(x.vendas)):'—')+'</td>'+\n          '<td class=\"num\"><button class=\"btn ghost sm\" data-cdel=\"'+esc(x.id)+\n            '\">apagar</button></td></tr>';}).join('')+'</tbody></table>'+\n      '<div class=\"actions\"><button class=\"btn ghost\" id=\"c-csv\">Descarregar CSV</button>'+\n      '<button class=\"btn ghost\" id=\"c-copy\">Copiar para a folha</button>'+\n      '<span class=\"msg\" id=\"c-msg2\"></span></div></div>';\n  } else {\n    h+='<div class=\"card\"><div class=\"empty\">Ainda não há envios registados. '+\n       'Usa o «+ Registar envio».</div></div>';\n  }\n\n  h+=pendingBox('Porque é preenchido à mão, para já',\n    'A newsletter e os SMS são enviados por ferramentas de fora, que ainda não estão ligadas aqui. '+\n    'Registando cada envio nesta página — leva meia dúzia de segundos — o painel passa a avisar-te '+\n    'no <b>Resumo</b> quando estiver na altura de enviar outra, e mostra se a última correu melhor '+\n    'ou pior que a média.<br><br>'+\n    'O objetivo está definido em <b>'+CADENCIA['Newsletter']+' dias para newsletters</b> e <b>'+\n    CADENCIA['SMS']+' dias para SMS</b> — o SMS é mais intrusivo, por isso menos frequente. '+\n    'Diz-me se quiseres outros intervalos.<br><br>'+\n    'Se me disseres que ferramenta usam para a newsletter, vejo se dá para trazer as aberturas e '+\n    'os cliques automaticamente, como já acontece com o WooCommerce e o Analytics.');\n\n  return {t:'Marketing',d:'Newsletters e SMS: quando foram, como correram, e quando enviar outra.',\n    html:h,after:bindCampanhas};\n};\n\nfunction bindCampanhas(){\n  var t=document.getElementById('c-toggle');\n  if(t) t.addEventListener('click',function(){ UI.formCamp=!UI.formCamp; refresh(); });\n  var add=document.getElementById('c-add');\n  if(add) add.addEventListener('click',function(){\n    var g=function(id){var e=document.getElementById(id);return e?String(e.value).trim():'';};\n    var assunto=g('c-assunto'), data=g('c-data');\n    if(!data){\n      var m=document.getElementById('c-msg');\n      if(m){m.textContent='Indica a data do envio.';m.style.color='#a4604f';} return;\n    }\n    var C=CAMPANHAS.get();\n    C.push({id:'C'+Date.now(),data:data,tipo:g('c-tipo')||'Newsletter',assunto:assunto,\n      destinatarios:g('c-dest'),aberturas:g('c-ab'),cliques:g('c-cl'),vendas:g('c-vd'),\n      notas:g('c-notas')});\n    CAMPANHAS.set(C); UI.formCamp=false; refresh();\n  });\n  document.querySelectorAll('[data-cdel]').forEach(function(b){\n    b.addEventListener('click',function(){\n      CAMPANHAS.set(CAMPANHAS.get().filter(function(x){return x.id!==b.dataset.cdel;}));\n      refresh();});\n  });\n  var csv=document.getElementById('c-csv');\n  if(csv) csv.addEventListener('click',function(){\n    descarregar(csvLinhas(CAMPANHAS.get(),CAMP_COLS,CAMP_TIT,','),'marketing_sarmento.csv');});\n  var cp=document.getElementById('c-copy');\n  if(cp) cp.addEventListener('click',function(){\n    copiar(csvLinhas(CAMPANHAS.get(),CAMP_COLS,CAMP_TIT,'\\t'),document.getElementById('c-msg2'));});\n}\n\n/* ---------------- SITE ---------------- */\nPAGES['site']=function(M){\n  var S=siteStats(M,RANGE.de,RANGE.ate);\n  var temAnalytics = M.RD.length>0;\n\n  var h=filterBar(M);\n\n  if(!temAnalytics){\n    h+=pendingBox('Analytics detalhado ainda não sincronizado',\n      'Falta correr o script de sincronização para esta secção ter dados de páginas, tempo, '+\n      'origens e dispositivos. Avisa quando quiseres o código.');\n    h+='<div class=\"kpis\">'+kpi('Encomendas',nf(M.Eok.length),'válidas')+\n      kpi('Vendas online',eur(M.recOnline),'no período')+'</div>';\n    return {t:'Site',d:'Tráfego e vendas da loja online.',html:h};\n  }\n\n  h+='<div class=\"kpis\">'+\n    kpi('Sessões',nf(S.sessoes),'no período')+\n    kpi('Duração média',S.duracao?Math.round(S.duracao)+'s':'—','por sessão')+\n    kpi('Envolvimento',S.engaj?nf(S.engaj,0)+'%':'—','sessões com interação real')+\n    kpi('Vendas online',eur(M.recOnline),M.Eok.length+(M.Eok.length===1?' encomenda':' encomendas'))+\n    '</div>';\n\n  /* insights automáticos */\n  var ins=[];\n  var topProd=S.produtos[0];\n  if(topProd) ins.push(insight('in','Produto mais visto: '+esc(topProd.titulo||topProd.url),\n    '<b>'+nf(topProd.views)+' visualizações</b>'+(topProd.tempo?', com '+topProd.tempo+\n    's em média por visita':'')+'.'));\n  var maisTempo=S.paginas.filter(function(p){return p.views>=4;})\n    .sort(function(a,b){return b.tempo-a.tempo;})[0];\n  if(maisTempo&&maisTempo.tempo>=30) ins.push(insight('op','Página que mais retém atenção',\n    '<b>'+esc(maisTempo.titulo||maisTempo.url)+'</b> prende os visitantes em média <b>'+\n    maisTempo.tempo+'s</b> — bem acima da média do site. Vale a pena olhar o que está a funcionar '+\n    'lá e repetir a fórmula noutras páginas.'));\n  var mob=S.dispositivos.filter(function(d){return d.k==='mobile';})[0];\n  var totDisp=S.dispositivos.reduce(function(s,d){return s+d.v;},0);\n  if(mob&&totDisp) ins.push(insight('in','A maioria acede pelo telemóvel',\n    '<b>'+nf(pct(mob.v,totDisp),0)+'%</b> das sessões são em telemóvel. Vale a pena verificar se o '+\n    'site e o processo de compra são fáceis de usar num ecrã pequeno.'));\n  if(S.engaj&&S.engaj<50) ins.push(insight('al','Envolvimento abaixo do desejável',\n    'Só <b>'+nf(S.engaj,0)+'%</b> das sessões têm interação real (mais de uma página ou mais de '+\n    '10 segundos). Pode indicar que quem chega não encontra logo o que procura.'));\n  if(ins.length) h+='<div class=\"ins\">'+ins.join('')+'</div>';\n\n  h+='<div class=\"g2\">'+\n    '<div class=\"card\"><h3>Sessões por dia</h3><p class=\"hint\">Google Analytics</p>'+\n      '<div class=\"chartbox\"><canvas id=\"s1\"></canvas></div></div>'+\n    '<div class=\"card\"><h3>De onde vêm as visitas</h3><p class=\"hint\">Por sessões, no período</p>'+\n      barList(S.origens.map(function(o){return{k:ORIGEM_NOME[o.k]||o.k,v:o.v};}))+\n      '</div></div>';\n\n  chart('s1',{type:'line',data:{labels:S.rdDias.map(function(k){\n    var d=parseDate(k);return d.getDate()+' '+MES[d.getMonth()];}),\n    datasets:[{data:S.rdDias.map(function(k){return S.porDia[k].sessoes;}),\n      borderColor:INK,backgroundColor:'rgba(20,20,22,.06)',fill:true,tension:.3,\n      pointRadius:2,pointBackgroundColor:INK,borderWidth:2}]},options:BASE});\n\n  h+='<div class=\"g2\">'+\n    '<div class=\"card\"><h3>Produtos mais vistos</h3>'+\n      '<p class=\"hint\">Páginas de ficha de produto — top 8</p>'+\n      (S.produtos.length?'<table><thead><tr><th>Produto</th><th class=\"num\">Views</th>'+\n        '<th class=\"num\">Tempo médio</th></tr></thead><tbody>'+\n        S.produtos.slice(0,8).map(function(p){\n          return '<tr><td>'+esc(p.titulo||p.url)+'</td><td class=\"num\">'+nf(p.views)+\n            '</td><td class=\"num\">'+p.tempo+'s</td></tr>';}).join('')+'</tbody></table>'\n        :'<div class=\"empty\">Sem visitas a fichas de produto neste período.</div>')+'</div>'+\n    '<div class=\"card\"><h3>Categorias mais vistas</h3><p class=\"hint\">Top 8</p>'+\n      barList(S.categorias.slice(0,8).map(function(p){\n        return{k:p.titulo||p.url,v:p.views};}))+'</div></div>';\n\n  h+='<div class=\"g2\">'+\n    '<div class=\"card\"><h3>Em que aparelho</h3><p class=\"hint\">Por sessões, no período</p>'+\n      barList(S.dispositivos.map(function(d){return{k:DISP_NOME[d.k]||d.k,v:d.v};}))+'</div>'+\n    '<div class=\"card\"><h3>Outras páginas vistas</h3><p class=\"hint\">Fora de produtos e categorias</p>'+\n      barList(S.outras.slice(0,8).map(function(p){return{k:p.titulo||p.url,v:p.views};}))+\n      '</div></div>';\n\n  if(M.E.length) h+='<div class=\"card\"><h3>Encomendas</h3>'+\n    '<p class=\"hint\">Atualizadas automaticamente de hora a hora</p>'+\n    '<table><thead><tr><th>Data</th><th>Cliente</th><th>Produto</th><th class=\"num\">Valor</th>'+\n    '</tr></thead><tbody>'+M.E.slice().sort(function(a,b){return b.data-a.data;})\n      .map(function(e){return '<tr><td>'+e.data.toLocaleDateString('pt-PT')+'</td><td>'+\n        esc(e.cliente)+'</td><td>'+esc(e.produtos)+'</td><td class=\"num\">'+eur(e.valor)+\n        '</td></tr>';}).join('')+'</tbody></table></div>';\n\n  return {t:'Site',d:'Tráfego, produtos e origem das visitas ao site.',\n    html:h,after:function(){bindFilter(M);}};\n};\n\n/* ---------------- CALENDÁRIO ---------------- */\nfunction nthWeekday(ano,mes,semana,n){ /* mes 0-11, semana 0=Dom..6=Sáb, n-ésima ocorrência */\n  var d=new Date(ano,mes,1), off=(semana-d.getDay()+7)%7;\n  return new Date(ano,mes,1+off+(n-1)*7);\n}\nfunction lastWeekday(ano,mes,semana){\n  var last=new Date(ano,mes+1,0), diff=(last.getDay()-semana+7)%7;\n  return new Date(ano,mes,last.getDate()-diff);\n}\nfunction datasComerciais(ano){\n  return [\n    {nome:'Dia dos Namorados',data:new Date(ano,1,14)},\n    {nome:'Dia do Pai',data:new Date(ano,2,19)},\n    {nome:'Dia da Mãe',data:nthWeekday(ano,4,0,1)},\n    {nome:'Black Friday',data:lastWeekday(ano,10,5)},\n    {nome:'Natal',data:new Date(ano,11,25)}\n  ];\n}\nfunction proximasDatasComerciais(n){\n  var hoje=new Date(); hoje.setHours(0,0,0,0);\n  var todas=datasComerciais(hoje.getFullYear()).concat(datasComerciais(hoje.getFullYear()+1))\n    .filter(function(d){return d.data>=hoje;})\n    .sort(function(a,b){return a.data-b.data;});\n  return todas.slice(0,n||6);\n}\nfunction calendarioISO(d){\n  return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+\n    String(d.getDate()).padStart(2,'0');\n}\n/* padrão sazonal a partir das vendas reais do Diamante (não do registo de atendimento,\n   que é só para uso dentro da própria página Registo de atendimento) */\nfunction calendarioSazonal(M){\n  var porMes={};\n  (M.DV||[]).forEach(function(v){\n    var m=v.data.getMonth();\n    porMes[m]=porMes[m]||{total:0,qtd:0,cats:{},mats:{},anos:{}};\n    porMes[m].total+=v.total; porMes[m].qtd+=(v.qtd||0);\n    porMes[m].anos[v.data.getFullYear()]=1;\n    if(v.categoria) porMes[m].cats[v.categoria]=(porMes[m].cats[v.categoria]||0)+v.total;\n    if(v.material) porMes[m].mats[v.material]=(porMes[m].mats[v.material]||0)+v.total;\n  });\n  function topKey(obj){\n    var ks=Object.keys(obj);\n    return ks.length?ks.sort(function(a,b){return obj[b]-obj[a];})[0]:null;\n  }\n  return Object.keys(porMes).map(Number).sort(function(a,b){return a-b;}).map(function(m){\n    var d=porMes[m];\n    return {mes:m,total:d.total,qtd:d.qtd,nAnos:Object.keys(d.anos).length,\n      topCategoria:topKey(d.cats),topMaterial:topKey(d.mats)};\n  });\n}\n\n/* vendas por época comercial (Natal, Dia da Mãe, etc.) — para ajudar a decidir campanhas e\n   coleções: nestas alturas do ano, o que se vende mais, em que categoria e em que material.\n   Somam-se todos os anos do histórico Diamante, não só o mais recente. */\nvar EPOCAS_ANO=[\n  {nome:'Dia dos Namorados (jan–fev)',meses:[0,1]},\n  {nome:'Dia do Pai (março)',meses:[2]},\n  {nome:'Dia da Mãe (abr–mai)',meses:[3,4]},\n  {nome:'Verão (jun–ago)',meses:[5,6,7]},\n  {nome:'Natal (out–dez)',meses:[9,10,11]}\n];\nfunction vendasPorEpoca(M){\n  return EPOCAS_ANO.map(function(ep){\n    var linhas=(M.DV||[]).filter(function(v){return ep.meses.indexOf(v.data.getMonth())>=0;});\n    var total=linhas.reduce(function(s,v){return s+v.total;},0);\n    var anos={}; linhas.forEach(function(v){anos[v.data.getFullYear()]=1;});\n    function topN(keyFn,n,comQtd){\n      var m={};\n      linhas.forEach(function(v){\n        var k=keyFn(v); if(!k) return;\n        m[k]=m[k]||{k:k,v:0,qtd:0}; m[k].v+=v.total;\n        if(comQtd) m[k].qtd+=(v.qtd||0);\n      });\n      return Object.keys(m).map(function(k){return m[k];})\n        .sort(function(a,b){return b.v-a.v;}).slice(0,n||5);\n    }\n    return {nome:ep.nome,total:total,nAnos:Object.keys(anos).length,\n      categorias:topN(function(v){return v.categoria;},5),\n      materiais:topN(function(v){return v.material;},5),\n      produtos:topN(function(v){return refSimplificada(v.sku)+' — '+tituloNome(v.desc);},8,true)};\n  });\n}\n\n/* cor por urgência da tarefa (pedido pelo Diogo 2026-07-29): vermelho=já passou a data,\n   âmbar=hoje ou nos próximos 3 dias, sem cor=mais tempo ou sem data marcada */\nfunction todoUrgenciaTom(x,hojeISO){\n  if(!x.data) return null;\n  if(x.data<hojeISO) return 'neg';\n  if(diasEntre(hojeISO,x.data)<=3) return 'warn';\n  return null;\n}\nPAGES['calendario']=function(M){\n  var TD=CALENDARIO.get();\n  var hojeISO=todayISO();\n  var pendentes=TD.filter(function(x){return !x.feito;})\n    .sort(function(a,b){\n      /* por ordem de urgência: mais atrasada/mais próxima primeiro; sem data vai para o fim,\n         já que não tem prazo a pressionar */\n      if(!a.data&&!b.data) return 0;\n      if(!a.data) return 1;\n      if(!b.data) return -1;\n      return a.data.localeCompare(b.data);\n    });\n  var feitas=TD.filter(function(x){return x.feito;})\n    .sort(function(a,b){return (b.data||'').localeCompare(a.data||'');});\n  var atrasadas=pendentes.filter(function(x){return x.data&&x.data<hojeISO;});\n\n  var h='<div class=\"kpis\">'+\n    kpi('Por fazer',nf(pendentes.length),'tarefas na lista')+\n    kpi('Atrasadas',nf(atrasadas.length),'com data já passada')+\n    kpi('Concluídas',nf(feitas.length),'no total')+\n    kpi('Próxima data comercial',proximasDatasComerciais(1)[0].nome,\n      proximasDatasComerciais(1)[0].data.toLocaleDateString('pt-PT'))+\n    '</div>';\n\n  h+='<div class=\"card\"><h3>Datas comerciais a aproveitar</h3>'+\n    '<p class=\"hint\">Épocas fortes para joalharia — vale a pena preparar campanha e stock com antecedência</p>'+\n    '<div class=\"datescom\">'+proximasDatasComerciais(6).map(function(d){\n      var dias=diasEntre(hojeISO,calendarioISO(d.data));\n      return '<div class=\"dc\"><div class=\"dc-n\">'+esc(d.nome)+'</div>'+\n        '<div class=\"dc-d\">'+d.data.toLocaleDateString('pt-PT',{day:'2-digit',month:'long'})+'</div>'+\n        '<div class=\"dc-f\">'+(dias===0?'é hoje':dias===1?'amanhã':'faltam '+dias+' dias')+'</div></div>';\n    }).join('')+'</div></div>';\n\n  h+='<div class=\"addbar\"><h3>To-do</h3><div>'+\n     '<button class=\"btn plus\" id=\"c-toggle\"><span>'+(UI.formTodo?'×':'+')+'</span>'+\n     (UI.formTodo?'Fechar':'Nova tarefa')+'</button></div></div>';\n\n  if(UI.formTodo){\n    h+='<div class=\"panel-add\"><div class=\"form\">'+\n      '<div class=\"field\"><label>Data</label><input type=\"date\" id=\"c-data\" value=\"'+hojeISO+'\"></div>'+\n      '<div class=\"field wide\"><label>Tarefa</label><input id=\"c-tarefa\" '+\n        'placeholder=\"Ex: preparar montra para o Dia da Mãe\"></div></div>'+\n      '<div class=\"actions\"><button class=\"btn\" id=\"c-add\">Guardar</button>'+\n      '<span class=\"msg\" id=\"c-msg\"></span></div></div>';\n  }\n\n  if(pendentes.length){\n    h+='<p class=\"hint\" style=\"margin-bottom:8px\">Ordenadas por urgência — vermelho já passou a '+\n      'data, âmbar é para os próximos 3 dias.</p>';\n    h+='<div class=\"card\"><table><thead><tr><th>Data</th><th>Tarefa</th><th></th><th></th></tr></thead>'+\n      '<tbody>'+pendentes.map(function(x){\n        var atraso=x.data&&x.data<hojeISO;\n        var tom=todoUrgenciaTom(x,hojeISO);\n        return '<tr'+(tom?' class=\"urg-'+tom+'\"':'')+'><td class=\"tdata\">'+\n          (tom?'<span class=\"kdot '+tom+'\"></span>':'')+\n          (x.data?new Date(x.data+'T00:00:00').toLocaleDateString('pt-PT'):'—')+\n          (atraso?' <span class=\"kbadge al\">atrasada</span>':'')+'</td>'+\n          '<td>'+esc(x.tarefa)+'</td>'+\n          '<td><span class=\"chk\" data-todo=\"'+esc(x.id)+'\"></span></td>'+\n          '<td class=\"num\"><button class=\"btn ghost sm\" data-deltodo=\"'+esc(x.id)+'\">apagar</button></td>'+\n          '</tr>';}).join('')+'</tbody></table></div>';\n  } else {\n    h+='<div class=\"card\"><div class=\"empty\">Sem tarefas por fazer. Usa o «+ Nova tarefa».</div></div>';\n  }\n\n  if(feitas.length){\n    h+='<div class=\"card\" style=\"margin-top:14px\"><h3>Concluídas</h3>'+\n      '<table><tbody>'+feitas.slice(0,15).map(function(x){\n        return '<tr style=\"color:#9d9890;text-decoration:line-through\"><td>'+\n          (x.data?new Date(x.data+'T00:00:00').toLocaleDateString('pt-PT'):'—')+'</td>'+\n          '<td>'+esc(x.tarefa)+'</td>'+\n          '<td class=\"num\"><button class=\"btn ghost sm\" data-todo=\"'+esc(x.id)+\n            '\" style=\"text-decoration:none\">reabrir</button></td></tr>';}).join('')+\n      '</tbody></table></div>';\n  }\n\n  var saz=calendarioSazonal(M);\n  h+='<div class=\"card\" style=\"margin-top:14px\"><h3>Padrões sazonais de compra</h3>'+\n    '<p class=\"hint\">A partir das vendas reais (Diamante) — que categoria e material vendem mais '+\n    'em cada mês, somando todos os anos do histórico</p>';\n  if(saz.length){\n    h+='<table><thead><tr><th>Mês</th><th class=\"num\">Receita</th><th class=\"num\">Peças</th>'+\n      '<th>Categoria mais forte</th><th>Material mais forte</th></tr></thead><tbody>'+\n      saz.map(function(s){\n        return '<tr><td>'+MESES_LONGOS[s.mes]+' <span class=\"hint\" style=\"margin:0\">('+\n          s.nAnos+(s.nAnos===1?' ano':' anos')+')</span></td>'+\n          '<td class=\"num\">'+eur(s.total)+'</td><td class=\"num\">'+nf(s.qtd)+'</td>'+\n          '<td>'+esc(s.topCategoria||'—')+'</td><td>'+esc(s.topMaterial||'—')+'</td></tr>';\n      }).join('')+'</tbody></table>'+\n      '<p class=\"hint\" style=\"margin-top:10px\">Histórico de vendas do Diamante desde '+\n      esc(diamLimites(M).min.toLocaleDateString('pt-PT'))+' — como já cobre vários anos, esta '+\n      'tabela já reflete um padrão real por mês (ex: se se vende mais ouro em maio todos os anos), '+\n      'não uma estimativa. O número entre parêntesis é a quantos anos diferentes esse mês já foi '+\n      'observado.</p>';\n  } else {\n    h+='<div class=\"empty\">Ainda sem histórico de vendas do Diamante para analisar.</div>';\n  }\n  h+='</div>';\n\n  var ep=vendasPorEpoca(M);\n  h+='<div class=\"card\" style=\"margin-top:14px\"><h3>Vendas por época do ano</h3>'+\n    '<p class=\"hint\">Para ajudar a decidir campanhas e coleções — ex: para o Natal, o que costuma '+\n    'vender mais? Mais colares ou anéis, mais em ouro ou em prata? Soma de todos os anos do '+\n    'histórico, por isso já é um padrão, não uma estimativa de um único ano.</p>';\n  if(ep.some(function(e){return e.total>0;})){\n    h+='<div class=\"g2\">'+ep.map(function(e){\n      if(!e.total) return '<div class=\"card\"><h3>'+esc(e.nome)+'</h3>'+\n        '<div class=\"empty\">Sem vendas registadas nesta época.</div></div>';\n      return '<div class=\"card\"><h3>'+esc(e.nome)+'</h3>'+\n        '<p class=\"hint\">'+eur(e.total)+' no total, '+e.nAnos+(e.nAnos===1?' ano':' anos')+\n        ' de histórico</p>'+\n        '<p class=\"hint\" style=\"margin-top:6px\">Categorias</p>'+\n        barList(e.categorias.map(function(c){return{k:c.k,v:c.v};}),\n          function(i){return nf(pct(i.v,e.total),0)+'% · '+eur(i.v);})+\n        '<p class=\"hint\" style=\"margin-top:14px\">Materiais</p>'+\n        barList(e.materiais.map(function(c){return{k:c.k,v:c.v};}),\n          function(i){return nf(pct(i.v,e.total),0)+'% · '+eur(i.v);})+\n        '<p class=\"hint\" style=\"margin-top:14px\">Produtos mais procurados</p>'+\n        (e.produtos.length?'<table><thead><tr><th>Produto</th><th class=\"num\">Qt.</th>'+\n          '<th class=\"num\">Receita</th></tr></thead><tbody>'+e.produtos.map(function(p){\n            return '<tr><td>'+esc(p.k)+'</td><td class=\"num\">'+nf(p.qtd)+'</td>'+\n              '<td class=\"num\">'+eur(p.v)+'</td></tr>';}).join('')+'</tbody></table>'\n          :'<div class=\"empty\">Sem produtos nesta época.</div>')+\n        '</div>';\n    }).join('')+'</div>';\n  } else {\n    h+='<div class=\"empty\">Ainda sem histórico de vendas do Diamante para analisar.</div>';\n  }\n  h+='</div>';\n\n  return {t:'Calendário',\n    d:'Datas comerciais importantes, tarefas por fazer e sinais de sazonalidade.',\n    html:h,after:bindCalendario};\n};\n\nfunction bindCalendario(){\n  var t=document.getElementById('c-toggle');\n  if(t) t.addEventListener('click',function(){ UI.formTodo=!UI.formTodo; refresh(); });\n  var add=document.getElementById('c-add');\n  if(add) add.addEventListener('click',function(){\n    var g=function(id){var e=document.getElementById(id);return e?String(e.value).trim():'';};\n    var tarefa=g('c-tarefa');\n    if(!tarefa){\n      var m=document.getElementById('c-msg');\n      if(m){m.textContent='Escreve a tarefa.';m.style.color='#a4604f';}\n      return;\n    }\n    var TD=CALENDARIO.get();\n    TD.push({id:'C'+Date.now(),data:g('c-data')||todayISO(),tarefa:tarefa,feito:false});\n    CALENDARIO.set(TD); UI.formTodo=false; refresh();\n  });\n  document.querySelectorAll('[data-todo]').forEach(function(c){\n    c.addEventListener('click',function(){\n      var TD=CALENDARIO.get();\n      TD.forEach(function(x){ if(x.id===c.dataset.todo) x.feito=!x.feito; });\n      CALENDARIO.set(TD); refresh();\n    });\n  });\n  document.querySelectorAll('[data-deltodo]').forEach(function(b){\n    b.addEventListener('click',function(){\n      CALENDARIO.set(CALENDARIO.get().filter(function(x){return x.id!==b.dataset.deltodo;}));\n      refresh();\n    });\n  });\n}\n\n/* ---------------- EBITDA ---------------- */\nPAGES['ebitda']=function(M){\n  var linhas=[['Vendas',M.recOnline],['Custo dos produtos',null],['Publicidade',null],\n    ['Envios',null],['Salários',null],['Renda',null],['Outros custos',null]];\n  var h='<div class=\"card\"><h3>Resultado por canal</h3>'+\n    '<p class=\"hint\">Vendas menos todos os custos</p>'+\n    '<table class=\"pl\"><thead><tr><th>Rubrica</th><th class=\"num\">Loja</th>'+\n    '<th class=\"num\">Site</th></tr></thead><tbody>'+\n    linhas.map(function(l){\n      return '<tr><td>'+esc(l[0])+'</td><td class=\"num\" style=\"color:#9d9890\">—</td>'+\n        '<td class=\"num\">'+(l[1]==null?'<span style=\"color:#9d9890\">—</span>':eur(l[1]))+\n        '</td></tr>';}).join('')+\n    '<tr style=\"border-top:1.5px solid #141416;font-weight:700\"><td>EBITDA</td>'+\n    '<td class=\"num\" style=\"color:#9d9890\">—</td><td class=\"num\" style=\"color:#9d9890\">—</td></tr>'+\n    '</tbody></table></div>';\n\n  h+=pendingBox('Faltam duas coisas para esta página funcionar',\n    'O <b>custo de cada produto</b>, que está na ficha de cada artigo no Diamante — dá a margem '+\n    'real de cada peça. E os <b>custos fixos do mês</b> (renda, salários, publicidade, envios), '+\n    'que têm de ser escritos uma vez por mês na folha própria. '+\n    'Esta segunda parte não depende de ninguém — pode ser feita já.');\n\n  return {t:'EBITDA',d:'Quanto sobra depois de todos os custos.',html:h};\n};\n\nwindow.addEventListener('hashchange',function(){\n  var id=location.hash.replace('#','');\n  if(id&&PAGES[id]&&id!==PAGINA) go(id);\n});\n</script>\n</body>\n</html>\n"}
